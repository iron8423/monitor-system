# 后端验收套件（tools/acceptance）

把《双人分工实施方案v2》§9 那份 demo 验收脚本里**后端可独立验证**的部分，做成可重复跑的断言脚本。
M5 的判据是「`docker compose up` 一键过验收脚本」——这里就是那份脚本的后端部分。

## 怎么跑

```bash
# 跑在当前已启动的后端上（默认 http://localhost:8080/api/v1）
tools/acceptance/run-all.sh

# 另起一个全新后端（H2 空库，端口 18080），跑完自动停
tools/acceptance/run-all.sh --fresh

# 单独跑某个套件（同样吃 BASE 环境变量）
BASE=http://localhost:18080/api/v1 tools/acceptance/04-alarm.sh
```

前置：后端已就绪（`cd backend && ./mvnw spring-boot:run`）；需要 `bash`、`curl`、`python3`。
可覆盖的环境变量：`BASE`、`INGEST_KEY`、`ADMIN_USER`、`ADMIN_PASS`、`FRESH_PORT`、`MVNW_OPTS`。

退出码：`0` 全过；`1` 有断言失败；`2` 环境问题（后端起不来/登录不上）——CI 里可据此区分「代码坏了」和「环境没起」。

### 为什么 `--fresh` 是另开端口而不是重启 8080

重启 8080 时若旧 JVM 没退干净，新进程会因端口占用**悄悄退出**，而健康检查由旧进程应答——
于是你在一份陈旧代码/脏库上跑完整套验收还全绿。换个新端口就没这种「看起来起来了」：
端口被占则新进程直接启动失败，健康检查永远等不到，脚本明确报错。
（这个坑踩过：有一次"重启"实际没生效，在旧代码上跑了好一阵才发现。）

`--fresh` 还会顺带 `rm -rf backend/data/media`。理由：库是 `jdbc:h2:mem`（进程一停就没了）、
种子也不含 `media` 行，所以后端一退出，本机这个目录里的文件就**定义上全是孤儿**——拥有它们的那张表
已经不存在了。不清的话每跑一轮就攒一批（`06-media.sh` 每轮传 3 张 1×1 测试图，而影像改逻辑删除后
**盘上文件按设计保留**，套件删了行也带不走文件）：实测跑够多轮后攒了 60 个。
清理前是 60、清完第一轮 3、再跑一轮仍是 3——**不再累积**。容器形态走 compose 的命名卷，不受影响。

## 套件 ↔ 验收脚本 8 条 的对应关系

| 套件 | 断言数 | 覆盖验收第几条 | 内容 |
|---|---|---|---|
| `01-archive-auth.sh` | 24 | 第 7 条 | 档案可读（**2 项目 / 9 测点** / 每点 2 测项）、新建测点立即可见、鉴权与 404 边界、**档案时间带 `+08:00`**、**读出来的时间能原样写回去** |
| `02-ingest-idempotency.sh` | 34 | 第 1、2 条 | 双测项消息拆 2 行、同 messageId 重发整条去重、上报即可查、批量混装、未知设备/点号拒收、`X-Ingest-Key`、SUSPECT 不入告警、**带时区往返**、**collectTime 非法即拒收**、**latest 次排序键**、**长点号 33–64 字符能建也能收（B-13）** |
| `03-query.sh` | 27 | 第 1 条 | latest 同刻测项重组、ISO 带时区、series 默认/指定测项、三种时间入参写法、hour/day 分桶、单位口径、400/404/401、**同刻多行时 latest 与 summary 取同一行**、**概览的最大形变只认 `defo_mm`（灌大速率不动它）** |
| `04-alarm.sh` | 47 | 第 3、4 条 | 超限触发、警情详情与时间线、处置链 confirm→dispatch→research→resolve、终态拒处置、负向 lte 触发、自动恢复、**防刷屏**、**等级升级（escalate）**、规则类型收敛、鉴权、**角色 → 处置动作越权 403**、**跑完不留自建规则** |
| `05-realtime.sh` | 12 | 第 1、3 条 | SSE `?token=` 订阅、measurement/alarm 事件、处置后状态推送、设备最近上报时间回写、SSE 鉴权 |
| `06-media.sh` | 32 | 第 6 条 | 上传返不透明编码 mediaId、客户端文件名不落盘（防路径穿越）、拒收非图片、列表裸数组、内容读取与字节一致、`?token=` 取图、**删除走逻辑删除（盘上留文件）+ 角色边界 + 套件回收自传的图** |
| `07-device-alarm.sh` | 26 | 第 5 条 | 停报设备判离线后**自动生成设备告警**、设备告警复用同一套处置/留痕管线、**设备告警必须计入项目概览的「未解除警情」**、恢复上报自动解除、从未上报的设备不告警、`alarmType` 过滤 |
| `08-simulator.sh` | 17 | 第 1、2、3 条 | **补上验收链的第一环**：`tools/radar_simulator` 造数 → 落库 → 立即可查、模拟时钟（`--step-minutes` 决定相邻间隔）、幂等、质量闸门（SUSPECT vs 对照组）、超限→升级→恢复全链、模拟器自身参数校验 |
| `09-data-quality.sh` | 49 | 第 5 条 | **数据可信度 / 数据延迟告警**：坏质量占比超阈值 → 开警情（快照成因与判据参数、触发备注写实际观测数）、恢复即自动解除、**判不了时（FAULT/掉线）不许动已有警情**、**两种成因互不掩盖**、延迟上报、**回补历史不算延迟**、鉴权 |
| `10-scope.sh` | 107 | 第 7 条（前半） | **项目数据范围隔离**：admin 全量 vs 李敏只见项目 1（列表/分页/详情/概览/latest/series/影像/维护记录逐条对照）、直达项目 B 全部 403、**警情按 point 与 device 两侧都滤**、处置动作也过范围、无归属设备只对 ADMIN 可见、`outsider` 对照组一切为 0（fail-closed）、**全局告警规则是刻意的例外**、**SSE 推送也按订阅者过滤（三条真实订阅 + 正反两侧）**、套件自身回收 |

合计 **10 套件 / 375 条断言**（2026-09-11 由 175 → 188 → 193：188 那次加的是 `04-alarm.sh` ⑪
的 13 条角色越权断言，193 这次加的是 `07-device-alarm.sh` ①-b/②/③ 的 5 条 alertCount 口径断言；
2026-09-14 的 193 → 201 是 `02-ingest-idempotency.sh` ⑫ 的 7 条 B-13 断言，
外加 `04-alarm.sh` 末尾 1 条「跑完不留自建规则」的套件自身卫生断言；
同日 201 → 212 是 `06-media.sh` ⑦/⑧ 的 11 条影像删除断言；
同日 212 → **266** 是新增的 `09-data-quality.sh` 49 条，加 `01-archive-auth.sh` ⑧ 的 5 条
「档案时间能写回去」断言，见下；
同日 266 → **359** 是新增的 `10-scope.sh` 93 条（项目数据隔离）；
同日 359 → **375** 是 `10-scope.sh` ⑪ 的 14 条 **SSE 按订阅者过滤**断言，加 `03-query.sh` ⑩ 的 2 条
「概览的最大形变只认 `defo_mm`」断言（B 侧同步单 §3.2 提出的那一条，见下）。
`01-archive-auth.sh` 的断言数不变，但 ② 的期望值随 V8 的第二个项目改成 2 项目 / 9 测点）。
**H2 空库（`--fresh`）实测 375/375**（逐套件：01→24、02→34、03→27、04→47、05→12、06→32、07→26、08→17、09→49、10→107）。
compose 的 PostgreSQL 形态：**07 套件 26/26**、
**04 套件 47/47**（含新增的自身卫生断言）与 **02 套件 34/34**；
**10-scope 的核心断言在 compose PG 上以浏览器脚本的形式复跑过 9/9 绿**
（`/tmp/fe-verify/scope.js` ①，admin 2 项目 / 李敏 1 / outsider 0，直达项目 2 得 403）；
整套在 PG 上做全量复跑需要一份空库——01 套件断言「种子 2 项目 / 9 测点」，而演示库累计了历史临时点，
直接跑会红在那一条上（不是代码问题）。**10-scope 不受这条影响**：它的跨用户对照一律用**差值**
（`admin 看到的条数 − 李敏看到的条数 == 项目 2 的规模`），脏库只是让两边一起变大，差值不变——
这是刻意的，否则演示库一被污染就会表现成「隔离坏了」，而真正坏掉的往往是库。库不同则执行计划不同，有些缺陷只会在其中一个上现形，
所以两个都要跑（详见 `03-query.sh` ⑨ 那条注释）。

> **01 ⑧ 是「时间能读出来却写不回去」的回归锚点**（2026-09-14 发现）。序列化被
> `JacksonTimeConfig` 统一补成带偏移的 `+08:00`，反序列化却还是 Jackson 默认实现——**只认不带偏移**
> 的写法。于是「GET 到的时间原样 PUT 回去」直接 400，表现成「接口收下了但字段改不动」（请求根本没进方法）。
> 影响 `Device.lastReportTime` 与 `BaseEntity.createdAt/updatedAt`（后者 7 个 CRUD 控制器都继承）。
> 断言在修之前实测红（原样回传与带偏移两条都红）。**注意**：这个 400 的响应体只有
> 「请求参数格式错误」，没有任何字段线索——发现它靠的是把 `-o /dev/null` 去掉看 HTTP 状态码，
> 排查时别再把它吞掉。

> **07 ①-b 是 B-14 的回归锚点**：告警有两条来源，挂的字段不同——`POINT` 类型只写 `point_id`、
> `DEVICE` 类型只写 `device_id`（另一侧为 NULL）。`ProjectSummaryService.activeAlarmCount` 曾只判
> `point_id IN (...)`，而 SQL 里 `NULL IN (...)` 不成立，于是**设备告警一条也数不进来**。这套断言
> 在改回旧写法时**实测会红**（①-b 与 ② 失败、③ 因 0==0 仍过）。它依赖 ⓪ 里把临时设备**绑定到本项目
> 的临时测点**——不绑的话该设备对本项目就是「无主」的，数不到它不是缺陷而是对的。

> **08 为什么单独存在**：01–07 全部用 curl 直接合成报文，等于**绕过了链子的起点**——链路后半段验得很扎实，
> 但「模拟器」这一环从没被跑过（`tools/radar_csv_replay/` 是回放器不是生成器，要真雷达 CSV）。
> 08 用 `tools/radar_simulator/radar_simulator.py` 把起点补上，断言它造出来的数据确实能走完整条链。

> **10 的两条写法要求**（第一次跑踩出来的，都在脚本头部写着）。
>
> **① 每一条「看不到」都要配一条「看得到」作对照。** 只断言 403 的话，一个把所有人
> 都拒掉的实现会让整页断言全绿——那种绿比红更糟。所以 ① 钉住管理员的全量视角、
> ④ 钉住非管理员对自己项目的正常读取、⑦ 用「同一个角色、同一个动作、只有警情归属不同」
> 把角色校验这个干扰项排除掉（OPERATOR 本来就允许 confirm，04 已单独验过角色矩阵）。
>
> **② 跨用户的条数比较用差值，不用绝对值。** 本套件跑在其它套件之后，绝对值断言会被
> 「前面某套件没回收干净」带红，把「隔离坏了」和「库脏了」混成一件事。差值
> （admin − 李敏 == 项目 2 的规模：1 项目 / 1 场景 / 1 对象 / 2 测点 / 4 测项 / 1 组织）
> 只依赖两边看的是不是同一个库。例外是 `outsider` 的 0（与残留无关）与 ⑫ 的基线回归。
>
> **⑪ 是「查询挡住了不够，推送也得挡」。** 隔离只做在 REST 侧的话，服务端照样把别的项目的
> 警情（含 `pointCode` / `deviceCode`）推进不该看到它的浏览器——界面看不见这个测点，告警横幅却弹了出来。
> 这一节开**三条真实订阅**（admin / 李敏 / outsider），正反两侧都断言。
> 两个写法上的要点：**① 只认 `event: alarm` 块**里带的业务码——measurement 事件的载荷里也有
> `pointCode`，只 grep 业务码的话，「只滤了警情、没滤测值」的实现照样能绿；
> **② 负向断言前面必须先有一条正向对照**（管理员收到了 = 事件确实发出去了），
> 否则所有「没收到」都可以由一个 401 的订阅解释。
> 证伪记录：把 `SseBroadcaster.doBroadcastScoped` 的判定改成恒真（即退回「推全库」），
> 这一节 **6 条负向断言全红、正向对照全绿**（107 → 101 通过 / 6 失败）。
>
> ⚠️ **`login` 打印的是裸 token，不是整个 `Authorization` 头。** 本仓约定是
> `TOKEN=$(login)` 之后再自己拼 `AUTH="Authorization: Bearer $TOKEN"`；`01` 就是这么写的。
> 10 第一版写成了 `AUTH=$(login)`，于是头是坏的、admin 的每条请求都 401，
> 而 401 的响应体**没有 `data` 键**，下游 helper 一律回标记串——**标记串与标记串相比还会「通过」**，
> 50 条失败里混着几条假绿。现在套件开头有一段**前提自检**：三个身份逐个打 `/auth/me` 验 `code==0`，
> 不过就 `exit 2`（环境问题），把「红成一片」和「隔离真的坏了」在输出上分开。
> 同理，凡「建出来了吗」这类断言不许用 `[ -n "$X" ]`（错误标记也是非空），走 `is_id`；
> `mediaId` 是不透明编码 `M1024` 而非数字，另有格式判断。

验收 8 条里**后端套件覆盖不到**的：
- 第 8 条 `docker compose up` + 数据不丢——**已由 B-3/B-4 落地并实测**（见根 `README.md`），但那是编排层的事，
  不进本套件的断言。**备份恢复**那一半已落地：`tools/backup/pg-backup.sh` / `pg-restore.sh`，
  配套的实测脚本 `tools/backup/selftest.sh`（要 compose 的 PG 起着，所以不并进 `run-all.sh`
  ——01–09 全部跑在 H2 空库上、不碰 docker）；
- 第 1/3/5/6/7 条的**前端呈现**部分——前端已落地五个工作台页面与 `/screen` 3D 大屏、并已在真实浏览器里点过
  （见 `frontend/README.md`）。第 6 条的**照片/影像**已于 2026-09-14 闭合（测点详情「影像」页签、
  `/media` 总览页、3D 大屏点击浮窗三处，浏览器实测 18/18，见 `docs/daily/2026-09-14-A.md`）。
  仍未做的呈现是断连演示在画面上的「瞬间变化」——大屏与曲线目前靠轮询刷新，
  SSE 只有连接状态、尚无页面级消费；
- 第 5 条的**真实断连演示**（拔线/停模拟器）——套件用「lastReportTime 落在 5 分钟窗口外」等价构造，
  不必真等 5 分钟；判据本身仍是 `DeviceStatusPolicy.OFFLINE_MINUTES`，与实际断连同一条。
  （模拟器 `--inject-outage` 可以做**真的**断连演示，但要真等满 5 分钟——那是业务判据，不走近路；
  该路径已手工验证过，未进自动套件。）

## 三个刻意的设计

**1. 造数一律落在「本次新建的临时测点」上。**
种子规则 `repeat_suppress_seconds=300`：同点同规则在 5 分钟内只要产生过警情（**含已解除的**）就不再触发。
沿用固定种子点会让「5 分钟内重跑」必然查无警情——那不是代码错，是防刷屏真的生效了。
固定点 + 固定 collectTime 还会让数据跨轮堆积在同一时刻，`latest` 取到哪一行随轮次变化。
每轮新建临时测点，断言因此可以精确到条数，套件**可重复执行**（156 条时实测同库连跑三轮 156/156；
08 加入后仍按同一约定取 `P-SIM-<RUN_ID>`，`--fresh` 空库连跑两次均 **175/175**；
升到 188 条后 `--fresh` 复跑 **188/188**，升到 193 条后 **193/193**）。

> ✅ **临时测点号现可到 64 字符**（2026-09-14 起）。此前这里写着「务必 <= 32 字符」——因为
> `monitor_point.code` 是 VARCHAR(64) 而 `measurement.point_code` 只有 VARCHAR(32)，
> 33–64 字符的点号**建得出来、一上报就 500**（B-13）。V5 已把两处对齐到 64，建档侧也加了
> `@Size(max=64)` 兜底，所以「能建」与「能收」现在是同一个上限。`02` ⑫ 有回归锚点
> （把列宽改回 32 实测 3 条断言转红）。
临时测点在脚本末尾**经 `recycle_point`** 回收；回收失败不影响结论。07 另外新建临时**设备**（同样末尾回收）。

> ⚠️ **临时测点必须先结警情再删**（2026-09-14 起，`lib.sh` 的 `recycle_point()`）。
> `monitor_point` 有 `deleted` 逻辑删除列，`alarm` 表**没有**「测点被删则警情失效」这回事——
> 直接删点会把该点上的未解除警情变成**孤儿**：指向一个查不到的测点，**却照样排在告警中心的待办队列里**，
> 点进去也画不出曲线。此前每跑一轮就多攒几条，演示库里累计了 18 条（16 待确认），
> 把值班工作台整个占满。回收责任在造数方而不是后端：按「点是否已删」过滤会改掉契约语义
> （警情是历史事实，不该因档案变动而消失），这是用户定过的案。

08 比这更进一步：④ 的分组**必须落在不同测点上**。对照组会真的产生警情，而同一 (测点,规则) 在 300s 内
产生过警情就不再触发（防刷屏），把质量闸门与对照组放在同一点上，后一段断言会被前一段带偏——
不是代码错，是防刷屏真的生效了。

**2. 上报函数一律 messageId 打头。**
`ingest_id` / `ingest2` / `ingest_recv` 三个上报函数签名统一，就是被坑出来的：
曾经有过一个自动生成 messageId 的 `ingest`，与 `ingest_id` 参数个数不同，
结果被按后者的签名调用，messageId 被当成点号、点号被当成时间，整条请求变成垃圾。

**3. 07 / 09 套件的等待时长与扫描间隔。**
设备离线是 `DeviceAlarmMonitor` 定时扫描出来的（默认间隔 10s，见 `monitor.device-offline.sweep-ms`），
数据质量同理走 `DataQualityMonitor`（`monitor.data-quality.sweep-ms`），套件一律轮询等待（上限 40s），
不赌固定 `sleep`。`run-all.sh --fresh` 会把两个间隔都调成 2s 省时间；
非 fresh 跑在别人起好的后端上时吃默认 10s，套件同样能过（轮询上限 40s 就是为它留的）。

**4. 09 灌数据必须带「本批确实落库」的自检。**
`flood` 每次要传一个**互不相同的 tag**（`q1`/`q2`/…），因为它把 tag 拼进 messageId。
幂等键是 `device_id + message_id + metric_code`，两次调用用同一批 messageId 的话，
第二批整批判 `DUPLICATE`——而**重复上报照样返回 HTTP 200**，唯一能看出这件事的地方是信封里的
`accepted`。第一版没带 tag，于是「灌了数据但一条没落库」，表现成后面一串莫名其妙的红
（坏质量占比越不过阈值、恢复后警情不解除），查了半天以为是扫描逻辑的问题。
现在 `flood` 把每批的 `accepted` 累加进 `FLOOD_ACCEPTED`，每个调用点都紧跟一条
「本批…确实落库」的断言——这个失败模式从此不可能静默。

## 两个会造成「假绿」的坑

**1. 角色断言必须用 `login_as <用户名>`，不能用 `login <用户名>`。**
`login()` 不收参数（永远 ADMIN），且失败即 `exit 2`；`login_as` 才是拿别的角色的。
写成 `login operator` 会**静默拿到一个管理员 token**，于是 `403` 期望全部命中、断言条条亮绿，
而你要验的角色边界一次都没被验过——比红更糟。06 的删除角色断言就是踩在这里（见脚本内注释）。

**2. `object_key` 是对外逻辑键，不是文件系统路径。**
它长这样：`media/P-HK01/<uuid>.png`，比盘上路径多一个 `media/` 前缀
（那个前缀是命名空间，不对应任何目录）。拿它直接拼路径会查一个不存在的文件，
把「文件还在」误判成「文件没了」。盘上真实路径是 `<UPLOAD_DIR>/<测点号>/<uuid>.<ext>`，
即 `"/app/data/media/" + ${object_key#media/}`。06 ⑦ 里那句剥前缀的写法就是为此。

## 已知未实现 / 待定（本套件不断言）

| 项 | 状态 | 说明 |
|---|---|---|
| 规则类型 `RATE`/`CHANGE` | 接口拒收 | 只实现 `THRESHOLD`；`windowMinutes` 保留字段但不参与判定（见 04 的 ⑧） |
| 设备告警的 SSE 推送 | 07 未断言，10 ⑪ 已覆盖 | 引擎会推 `alarm` 事件；07 只断言 REST 侧，但 10 ⑪ 开的三条订阅里含**设备告警**一侧（无归属设备的离线告警只到 ADMIN），归属链与测点不同 |
| 概览 `maxDeformationMm` 的测项中立化 | **刻意不做**（见 03 ⑩） | 只统计 `defo_mm`，不随主测项切换、也不统计后加的测项。要做需先给 `metric` 档案加一列量纲类别——`unit` 猜不出来（`defo_mm` 是 `mm`、`rate_mm_d` 是 `mm/d`，`contains("mm")` 会把速率算进去）。契约 §3 已写明 |
| 设备告警进不进「警情中心」的默认视图 | 产品待定 | 后端已按 `alarmType` 可筛；前端默认展示哪一类由 B 定 |
| 低电量告警 | **未实现**（用户定案：暂不做） | `DeviceStatusPolicy.isLowBattery`（阈值 20）只在设备列表里用于**展示**，没有任何告警生产者。要做需先定「持续多久算告警」「回充到多少算恢复」 |
| **测点/测项级**数据中断告警 | **未实现** | 与设备离线（整台设备不报数）不同，这是「设备在报数、但某个测点/某个测项断了」。判据尚未定义（「断了」是按该测项的期望上报频率算，还是按其他测点的相对间隔算？两者在演示数据上都各有说不通的地方）。**设备级**的数据质量异常与延迟上报已于 2026-09-14 落地，见 `09-data-quality.sh` |

