# 阶段 A · 冗余与死代码清单（2026-09-22）

> 三档判定：**必须改**（阻断后续开发或造成误判）/ **建议改**（成本低、收益明确）/ **可以不动**（有明确用途）。
> 每条都带证据；证据来自静态盘点（`work/audit/raw/*.json`）+ 人工核对。
> 未经你与 A 划勾前，我不动任何一行代码。

## A. 前端

| # | 问题 | 证据 | 判定 | 建议动作 |
|---|---|---|---|---|
| A1 | **158 MB 预览资产**堆在 `frontend/public/models/`：g19、g19-2m、hires、hires-flat、v2-lit、wide、z19、powerplant、powerplant-big、powerplant-clean | 逐个目录统计；只有 6 个目录被迁移登记 | 必须改 | 保留 `-clean` 与 `-big` 各一份作对照，其余移出仓库或加清理脚本；**预览资产一律不入库**（Google 影像不得再分发） |
| A2 | `renderProfile.js` 的 lit 档会 `globe.show=false`，一开就把远景层藏掉、虚空回归；`visual-compare.html` 是同一批试验的遗留 | `renderProfile.js:36-38`、`frontend/public/visual-compare.html` | 必须改 | 修 lit 档（保留椭球/远景）或直接删档 + 删对比页 |
| A3 | 顶栏在覆盖资产时显示"资产 未核对" | ScreenView 的 assetProblem 分支 | 必须改 | 覆盖模式下改显示"预览资产"，与真故障区分 |
| A4 | api 层 2 个函数从定义起就没有调用者：`pointBaselineHistory`、`deviceStatus` | `work/audit/raw/usage.json`（47 个函数，2 个零引用） | 建议改 | 删除或标注 deprecated |
| A5 | `AdminView.vue` 绕过 api 层直接 `http.get/put/post/delete`，路径由 tab 配置拼（`${tab.path}/${id}`） | `AdminView.vue:257/436/471` | 建议改 | 收敛到 api 层，顺带获得统一的错误/权限处理 |
| A6 | 四个"大块头"单体文件：ScreenView 67 KB、DeviceDrawer 36 KB、digitalTwinScene 35 KB、PointsView 33 KB | 文件体积 + 引用数均为 1 | 建议改 | 按职责拆分（HUD/交互/场景装配分开），这是后续加需求的主要成本来源 |
| A7 | 废弃开关与死常量：`VITE_CAMERA_RANGE_SCALE`、`VITE_MAX_ZOOM_DISTANCE`（已被 `MAX_ZOOM_FACTOR` 取代） | `.env.local` 注释 + createViewer 代码 | 建议改 | 清理并写进迁移说明 |
| A8 | 演示向功能的使用面待定：时间轴回放（replay store 17 KB + timeline 9 KB）、地面热力图（heatmapLayer 9.6 KB + heatScale）、多场景切换、`/ops`、`/help` | 审计清单 D 组 + 用户此前未验收 E/H/I 组 | 可以不动 | **由你与 A 定"面向客户的最小集"后再决定**，不要现在删 |

## B. 后端与接口

| # | 问题 | 证据 | 判定 | 建议动作 |
|---|---|---|---|---|
| B1 | "取最新值"有 3 条路径：`/points/{id}/latest`、`/projects/{id}/points/latest`、`/projects/{id}/summary` 里的 `maxDeformation` | 三向差集 + 审计清单 C 组 | 建议改 | 统一到一个 Service 实现，对外形状可保留 2 个（单点/批量） |
| B2 | `BaseCrudController` 给**每个**资源都提供了 POST/PUT/DELETE 通用端点（含 `/users`、`/points`、`/projects`…） | `common/base/BaseCrudController.java` | 建议改 | 按资源显式声明允许的写操作，避免"能改表就能改一切" |
| B3 | 后端有、**前端零入口**的 5 个：`GET /ops/retention`、`GET /health`、`GET /health/ready`、`POST /ingest/measurements`、`GET/PUT/DELETE /maintenance-records/{id}` | 全库搜索零命中（`ops/retention` 等）；maintenance 只有列表与新建在用（DeviceDrawer） | 可以不动 | 前两个是运维/编排用途、ingest 是设备侧、maintenance 单条读写界面未做——写进契约文档说明入口，别当死代码删 |
| B4 | `/users/{id}`、`/devices/{id}`、`/alarm-rules/{id}`、`/points/{id}` 等**只通过 AdminView 通用 CRUD 可达**（路径由 `${tab.path}/${id}` 拼出，静态扫不到） | `AdminView.vue:257/436/471`；tab 覆盖 projects/users/scenes/objects/points/metrics/devices/alarm-rules | 可以不动 | 与 B2 一起考虑收紧；注意 `/organizations` 没有 tab，写操作确实无界面入口 |
| B6 | 表与实体是否对得上（孤儿表） | 19 张表 ↔ 19 个实体，**零孤儿** | 可以不动 | 无需动作（这条是"审过了，没问题"） |
| B7 | 重复能力：曲线 2 条（单点/批量） | `/points/{id}/series` 与 `/points/series` | 可以不动 | 批量是性能优化，语义清晰，保留 |

## C. 工程卫生（当前最大风险）

| # | 问题 | 证据 | 判定 | 建议动作 |
|---|---|---|---|---|
| C1 | 工作区 24 项未整理：18 未跟踪（含 10 个模型目录、farfield、4 个新工具、visual-compare.html）+ 5 已改未提交 + 1 已提交但工作区被删 | `git status --short` | 必须改 | 先分类：入库存代码 / 加 .gitignore 的预览资产 / 删除的临时物 |
| C2 | `docs/给A的同步_三维观感与影像接入_20260921.md` 已提交（a472396）但工作区被删 | `git status` 首行 | 必须改 | 确认是误删还是有意移除，二者都要在提交里说清 |
| C3 | `backend/.vscode/` 与 `docs/需求文档_v2.0_流程跑通版_20260914.md` 未跟踪（后者是你 9/14 起就保留未提交的） | `git status` | 建议改 | IDE 噪音加 gitignore；需求文档由你决定去留 |
| C4 | 旧的一次性脚本仍散在 `work/`（`patch_glb_texture.py` 等，已被 `tools/terrain_asset/replace_texture.py` 取代） | 两份脚本功能重叠，且旧脚本有"贴图必须是最后一个 bufferView"的错误假设 | 建议改 | 标注作废，避免下次又用旧的 |

## D. 文档

| # | 问题 | 证据 | 判定 | 建议动作 |
|---|---|---|---|---|
| D1 | `docs/` 里两份大清单可能重叠：`monitor-system_系统完善与改进清单.md`（130 KB）与 `全项目功能与工具复查清单_20260918.md`（61 KB） | 文件体积 + 标题 | 建议改 | 合并成一份"事实源"，过程记录移出 `docs/` |
| D2 | 对话里引用过但盘上不存在的文件（如那份"12 项验收指标"） | 全盘搜索无该文件 | 必须改 | 要么补齐落盘，要么在文档里删掉引用（我建议补齐） |

## 汇总：必须改的只有 6 条

| 序号 | 事项 | 工时 |
|---|---|---|
| 1 | C1 工作区 24 项分类整理（入库存 / 忽略 / 删除） | 1h |
| 2 | C2 那份被删的文档定性 | 10min |
| 3 | A1 预览资产治理（移出仓库 + 清理脚本 + 入 .gitignore） | 1h |
| 4 | A2 lit 档与 `visual-compare.html` 处理 | 30min |
| 5 | A3 "资产未核对"文案修正 | 15min |
| 6 | D2 补齐缺失文档，消除不存在引用 | 30min |

## 建议改（第二批，按收益排序）

A4 api 层死函数 → A5 AdminView 收敛到 api 层 → B1 取最新值统一 → B2 收紧通用写端点 → A6 两个大文件拆分 → A7 废弃开关清理 → C4 旧脚本标注 → D1 文档合并。

## 重构路线图（每批一个提交、跑一遍回归）

1. **批 1（收口）**：上面 6 条"必须改"。验收：`git status` 干净、前端构建通过、大屏刷新无报错。
2. **批 2（口径）**：A4/A5/B1/B2 + A7。验收：后端 731 条断言全绿、前端 selfcheck 通过。
3. **批 3（结构）**：A6 拆 ScreenView / digitalTwinScene；D1 文档合并。验收：三维大屏功能逐条复验（按 40 条清单）+ 另外三个演示场景无回归。
4. **待定**：A8 演示向功能的去留——需要你与 A 先定"面向客户的最小集"，再决定删还是保留为开关。
