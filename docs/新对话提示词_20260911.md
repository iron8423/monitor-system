# 新对话提示词（2026-09-11）· 角色 B（前端）

> 用法：开新对话，把下面 **```之间的整段** 复制粘贴即可，agent 读完就能接上全部进度。

```
你是我在做“通用监测管理系统”实习项目的协作助手，我是角色 B。

请先读这几份文件（在 C:\Users\ASUS\Desktop\实习\监测系统\）：
- docs\全景与交接_20260911.md   ← 现状与交接（先读这份）
- docs\message-contract.md      ← 雷达消息契约（唯一事实源）
- docs\B侧接口契约_M0.md        ← 后端接口契约（09-10 已同步）

现状（关键）：
- 阶段 1 后端【已由 A 全部做完】：Spring Boot 3.5 / Java 21，含 auth(JWT/RBAC)、档案 CRUD、设备状态、
  telemetry/ingest、alarm(告警状态机)、media、SSE(/stream)、latest/series/summary；Docker Compose + Postgres
  验证通过；验收套件 tools/acceptance（8 套 / 173 断言全过）；雷达模拟器 tools/radar_simulator。
- 我现在要做的是【阶段 2 前端】：Vue3 + Element Plus + ECharts + CesiumJS 的 3D 大屏（重点/门面）+ 位移分析页
  + 告警中心 + 管理端 + 无人机影像挂点。frontend/ 目前是空的。
- 后端跑法：cd backend; .\mvnw.cmd spring-boot:run（H2，端口 8080）；登录 admin/123456。

请你带我一步步做前端：
1) 先搭前端工程骨架（Vite+Vue3）+ 登录（对接 POST /api/v1/auth/login，token 放 header）；
2) 总览/测点页（GET /points、/points/{id}/latest、/points/{id}/series、/projects/{id}/summary）；
3) 【重点】Cesium 3D 大屏：真实地形 + 监测点标点（按状态变色）+ 点击弹窗看最新值 + 时间轴/热力图（对齐
   github.com/hawk86104/three-vue-tres 与 github.com/CesiumGS/cesium 的效果）；
4) 告警中心（GET /alarms、POST /alarms/{id}/actions）+ 管理端；5) 媒体挂点（POST /media）。

每步给我能跑的代码 + 怎么验证。遇到 git 网络问题提醒我走代理（http://127.0.0.1:7890）。
```
