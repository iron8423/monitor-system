# monitor-system 系统完善与改进清单

> **2026-09-18 说明**：本文是一份**历史对账**，保留当年的原文与证据，不再更新。
> 项目在 2026-09-18 清理过一次 `docs/`：协作与过程类文档（双人分工实施方案、给 A 的同步、
> 全景与交接、M0 对表/接口冻结、后续阶段工作清单、逐文件替换清单、每日日志、旧需求基线归档等）
> 已从工作区删除，**内容仍在 git 历史里**（`git log --diff-filter=D --name-only` 可回溯）。
> 因此本文里指向这些文件的引用会悬空，属预期。
> **当前待办请看 [`docs/全项目功能与工具复查清单_20260918.md`](全项目功能与工具复查清单_20260918.md)**——
> 它对本文 55 条逐条做了对账（哪些仍在、哪些已修、哪些是多场景之后新出现的）。

日期：2026-09-16  
适用版本：当前交付的 V2 双雷达数字孪生版本（数据库迁移至 V16）。  
范围：前端、后端、数字孪生、数据接入、安全、部署运维与测试交付。

**当前系统的主要业务模块已经有了，但还需要一轮生产化完善。** 优先处理数据正确性、账号权限和运行可靠性，再补管理界面与性能，模型美化可以靠后。

> **2026-09-17 晚补记**：个人中心（只读资料 + 修改本人密码）落地，见第 13 条的更正；
> 数据库迁移已到 **V17**。自动化覆盖随之变为 **14 套件 / 493 条后端断言**、前端 `selfcheck` **73 条**——
> 第 50 行里的 13 / 480 / 67 是当天**早些时候**的实测值，按本清单惯例保留、不覆盖。
>
> 另记一笔**改密带来的策略冲突**（读者会撞上）：新密码强制 ≥ 8 位，而演示账号口令是 6 位（`123456`），
> 所以「把口令改回 123456」在 API 上做不到。这不是缺陷，是口令策略与演示种子之间的真实落差；
> 验收套件 `14-password-change.sh` 因此只在一次性实例（`run-all.sh --fresh`）上做真实轮换。
>
> **2026-09-17 晚补记（第 01 条的一个可复现成因）**：第 01 条记的现场症状「大屏打不开」始终没有复现过，
> 但当晚查到了**另一条**通往「大屏什么都没有」的可复现路径，与取包无关：`GET /projects` 原先
> **没有 ORDER BY**，返回顺序由执行计划决定——实测同一份数据把「西江水泥采空区」（`assetType=NONE`，
> 没配数字孪生场景）排在了「清远山地边坡」（配了 GLB 山体）前面，而前端 `stores/monitor.js` 用
> `projects[0]` 当默认项目，于是大屏默认打开那个没场景的项目，屏幕上只剩「场景未配置 / 离线底色」。
> 已修：`ProjectController#ordered` 固定按 id 升序（`BaseCrudController` 留了排序钩子），
> 大屏另外补了「未配场景」的说明条与一键切到已配置场景的项目；回归断言落在 `01-archive-auth.sh` ⑨。
> **这与第 01 条原本说的「升级后资源加载」不是同一件事**：那条是分包 404（重新加载即可），
> 这条是**服务端列表顺序**——不重新加载也能整天看到空白大屏。

下面依据当前交付源码整理，未在用户服务器上逐项复现：

- **已确认**：源码中可以直接看到的缺口，不代表已在现场复现事故。
- **待验证**：存在风险，需要通过测试确认。
- **待定位**：已有用户故障反馈，但原因尚未确认。
- **功能补齐**：为了实际交付和日常使用需要增加的能力。
- **已修复**（2026-09-17 起新增）：本条已改完，并附上了复现/验收证据。**原缺口描述一律保留不删**——
  它记录的是「改之前是什么样」，是日后复查与反向验证（改动前会不会红）的依据，不是过期内容。
- **部分完成**（2026-09-17 起新增）：本条改了一部分。正文里**已做**写清改了哪半、**仍缺**写清剩下哪半，
  状态列不再升级为「已修复」，直到「仍缺」那一半也落地——半个修复不该读起来像一个修复。

本文是待办清单，不是已完成修改记录。第一节为 P0；其余条目按模块归类，具体实施顺序见文末。

## 一、P0：正式生产前优先解决

| 编号 | 涉及端 | 状态 | 需要完善的内容 |
|---|---|---|---|
| 01 | 前端 | **已修复**（2026-09-17，`check-p0-stage1.mjs` 项 32–35 模拟网络断言 + 源码级取证；**原始现场症状未复现**，见下） | **大屏打不开的问题闭环**：检查菜单跳转、直接访问 `/screen`、刷新、登录过期和升级后资源加载。当前没有确认已修复。**⚠ 本条与前 12 条里的其它条不同：原始现场症状（用户报「大屏打不开」）至始至终没有复现过**，所以下面是**机制已完整**的代码证据，不是「故障已排除」的证据。这两件事必须分开读——码在、路径通，不等于当初那次打不开就是它们修好的。<br><br>**已做**（五个子项逐条对照，均**无需改动**，是既有实现）：① **菜单跳转**——`frontend/src/router/index.js:27-32` 记着一条踩过的坑：角色兜底页**必须**在 `AppLayout` 内且不带 `meta.roles`，**不能**用 `/screen`；理由是 `/screen` 是顶层路由、不套布局，用户点侧栏「工作台」会被送过去，布局整个卸载、没有菜单，而大屏上的「退出大屏」又 `push('/home')` 跳回来——**用户在页面里出不来，只能改地址栏**。这正是「点菜单反而打不开」的成因，已被这条注释与 `FALLBACK_HOME` 钉住。② **直接访问 `/screen` 与刷新**——`/screen` 是顶层路由（`router/index.js:51-55`）；服务端深链接由 `frontend/nginx.conf:44-49` 的 SPA 回落承接（`try_files $uri $uri/ /index.html`），注释写明「少了这条，深链接一律 404」。③ **升级后资源加载**——这里才是「代码升级了但表现没变」的真正来源，已按文件名是否带哈希分成两档：`nginx.conf:13-18` 构建产物带内容哈希 → `expires 1y; immutable`；`nginx.conf:20-27` Cesium 运行时资源（Workers/Assets/Widgets/ThirdParty）**文件名不带哈希 → `no-cache`**，注释逐字写出后果「浏览器拿旧缓存去配新产物，大屏加载到不匹配的 Worker/贴图」；`nginx.conf:29-36` 生产数字孪生模型同理 `no-cache` + 每次验证 ETag；`nginx.conf:38-42` HTML 入口 `no-cache`，避免更新后继续引用旧的哈希分包。④ **登录过期**——`frontend/src/api/http.js:61-79` 收口 401，且刻意**只排除登录接口本身**（`isLoginRequest`），注释写明「不能图省事排除整个 `/auth/**`：`/auth/me` 是受保护接口，token 过期时正是它返回 401」。⑤ **图片请求的 401 是另一条路**——`frontend/src/api/monitor.js:259-260` 记着「图片加载失败**不走 axios 拦截器**，控制台里只有一串裸 401、没有任何提示」，所以带 `?token=` 取图这条路单独处理了。<br><br>**证据**：**除了源码级取证，本条另有一组可执行的自动化断言**——`frontend/scripts/check-p0-stage1.mjs` 的**项 32–35** 就是照本条的五个子项写的（本轮复跑该脚本 **35 项全部通过**，基线 25）：**项 32**「分包加载失败可见提示，取消不刷新」直接打③（升级后资源加载：它**故意**让动态 import 抛错——运行时会打印一行 `[router] 页面加载失败 /screen?case=p0`，那是**测试内部的模拟**，不是真失败）、**项 33**「确认重试保留目标路径与查询参数」（重试不许把用户丢回首页）、**项 34**「已登录访问大屏路由可正常完成」直接打②（直接访问 `/screen`）、**项 35**「未登录大屏访问仍跳转登录，保留返回地址」直接打④（登录过期）。**项 32 的 `?case=p0` 与项 33 的重试**合起来正是「升级后资源加载」这条路径的完整断言。①（菜单跳转）没有单独用例，但它由项 34 的路由可达性与 `FALLBACK_HOME` 的源码约定共同覆盖。<br><br>**⚠ 但要分清证据的性质**：项 32–35 跑在**模拟网络**下（该脚本用可替换的 axios adapter + 插件桩，不加载 `.vue`、不起真实浏览器），所以它们证明的是**路由与加载失败处理逻辑**是对的，**不是**「当初那次打不开就是它」。脚本自己在结尾也印着这句：「P0 第 1 批：35 项全部通过（**模拟网络；未替代现场浏览器与真实后端验收**）」。本项目里「原始现场症状从未复现」这个事实**没有变**。<br><br>**边界与仍缺**：① 上述五条是**当前代码具备的机制**，不是「原始故障的根因认定」；若现场再报一次，仍应从 Network/Console 从头查，不能拿这一行当结案。② `nginx.conf` 的三档缓存策略只在**容器部署形态**下生效；开发期走 `vite.config.js`，行为不同，不要拿 dev 的表现为准。③ 第 21 条（网络错误提示写死 `localhost:8080`，与 8088 反代部署不一致）是**同一个「症状在部署形态下才出现」的家族**，仍未修——`frontend/src/api/http.js:60` 与 `frontend/src/views/LoginView.vue:36` 两处命中，那一条留着。 |
| 02 | 前端 | **已修复**（2026-09-17，源码级取证 + `selfcheck` 既有断言） | **实时数据防止时间回退**：目前 SSE 直接覆盖最新值，晚到的旧数据可能覆盖新数据；需要比较采集时间，并明确同时间数据的处理规则。**已做**：合并规则收在 `frontend/src/stores/monitor.js:33-48` 一对纯函数里——`collectMillis` 把 `collectTime` 解析成毫秒（缺字段/空白串 → `NaN`，交给下一步按「不可比」处理），`preferSnapshot(current, incoming, atRequestStart)` 给出**三档**判定：① incoming 时间不可比（`NaN`）→ 保留 current（**这一档是 fail-safe 的关键**：解析不出来的时间不许当成「最新」）；② current 不可比、或 incoming 更新 → 取 incoming；③ **时间相同**时，只有 `current === atRequestStart`（即「这份快照是我这次请求发出时的样子」）才采纳快照——注释 `:39-43` 逐字写明理由「最新值是一条观测，而不是不同时间测项的拼盘。旧时间永不覆盖；无版本号的同时间 SSE 由先到值暂时占位，再由服务器快照对账。同时间快照仅在请求期间本地未更新时采用，**避免在途 HTTP 覆盖刚到的 SSE**」。唯一调用点 `monitor.js:405`（`map[id] = preferSnapshot(map[id], row, atRequestStart[id])`）。<br><br>**证据**：**本条是本清单里自动化覆盖最厚的一条**——`frontend/scripts/check-p0-stage1.mjs` 的**项 01–21 整节**就是为它写的（本轮复跑 **35 项全部通过**）。直接打本条合并规则的：**项 01** 晚到 SSE 不覆盖新值、**项 02** 同一批无序消息取最大采集时间、**项 03** 同时间冲突 SSE 不按到达顺序覆盖、**项 04** 同一时刻**不同时区表示**不会误判为更新（这条防的是字符串比较冒充时间比较）、**项 05** 缺失和非法采集时间均忽略（对应 `collectMillis` 返回 `NaN` 那一档）、**项 09** HTTP 请求期间到达的新 SSE 不被旧响应覆盖、**项 10** 在途同时间冲突快照不得覆盖刚到的 SSE、**项 11** 后续稳定快照对账同时间值**并补齐附加字段**、**项 12** 空快照或坏时间不能擦掉已有测值、**项 21** 1000 条乱序批次保留采集时间最大的一条（规模下的同一不变式）。相邻但不同条的还有项 06–08、13–20（首屏快照缺失、未知测点、项目过滤、代际守卫、错误降级）。另有 `selfcheck.mjs` §① 的 store 层用例，本轮复跑 **67 条 / 失败 0**（基线 35 → 67）。<br><br>**证据的反面（反向验证实做）**：把 `isCurrent` 改成恒 `return true` → `selfcheck` 3 条失败、乱序那条显示 `— B,A`（晚到的 A 覆盖了 B），退出码 1；实做后按快照逐字节还原。<br><br>**边界与仍缺**：同时间且「快照不是我请求时的那一份」时，**由先到值占位**——这是刻意的取舍（无版本号就没法在全序上判先后），代价是极端同刻并发下最终值取决于到达顺序；要根治需要服务端给每行一个单调版本号（`sequence` 字段在 `NON_METRIC_KEYS` 里存在但当前不参与此判定），记为**待契约变更**。 |
| 03 | 后端 | **已修复**（2026-09-17，源码级取证 + `02-ingest-idempotency.sh`／`08-simulator.sh` 相关断言） | **历史补报与实时告警分离**：目前历史数据也进入告警评估，可能用旧高值触发当前告警，或用旧低值解除当前告警。**已做**：闸门就是 `backend/src/main/java/com/monitor/telemetry/service/IngestService.java:248` 那一句 `if (ingestMode == IngestMode.REALTIME) {`——**整段只在实时模式下执行**，补报（`BACKFILL`）落库后直接跳过，因此 `:275` 的 `alarmEngine.evaluate(...)` 在补报路径上根本到不了。同一道闸后面还连着另外两件事，都是同一个道理：`:254-256` 推进设备在线心跳（注释「**仅实时消息推进设备在线心跳。补报不能把离线设备"救活"**」，回写 `last_report_time`），`:250-252` 的 SSE 广播（注释「实时推送必须排在告警评估之前：提交后先有测值、再有警情」）。模式来源是显式的：`IngestRequest.java:16/29` 定义了 `{"ingestMode":"BACKFILL"}` 这种批量/补报形，并写明「默认保持既有实时上报行为；**历史导入必须显式传 BACKFILL**」——所以默认不会误挡。另有一处**闸中闸**：`:272` `if (!"disappeared".equalsIgnoreCase(m.getState()))` 把「目标失联」的观测排除出评估，注释 `:260-271` 给了完整理由与**为什么闸设在这里而不是 `deriveQuality` 里**（① 设备**显式**声明的 `quality` 优先于推导，标 SUSPECT 挡不住 `quality:"VALID" + state:"disappeared"`；② 标 SUSPECT 会连带被 `DataQualityPolicy.isBadQuality` 计入坏样本，持续报失联的设备会冒出一条语义错误的「数据质量异常」告警）。<br><br>**证据**：`tools/acceptance/02-ingest-idempotency.sh`（43 条）含「未知设备/点号拒收」「SUSPECT 不入告警」；`08-simulator.sh`（26 条）含「超限→升级→恢复全链」。**补报侧的直接断言（BACKFILL 不触发告警）本轮**未找到独立用例，见下。<br><br>**边界与仍缺**：**上面这条结论本轮是从源码读出来的，不是从一条专门的反向用例跑出来的**——即「把 `if (ingestMode == REALTIME)` 去掉，必须有一条断言红」这件事目前**没有**牙齿。已有的两个套件覆盖的是旁边那两件事（幂等、升级/恢复链），不构成对本闸门的反向验证。补一条「BACKFILL 发一个超阈值测值 → 未解除警情恰好 0 条」的断言是下一步该做的（红法明确：去掉 `:248` 那句就变 1 条）。 |
| 04 | 前后端 | **已修复**（2026-09-17，源码级取证 + `selfcheck` 既有断言） | **同一测点多告警的正确展示**：前端每个测点只保存一条告警；解除其中一条时可能清掉该点告警颜色。应按告警 ID 保存，再汇总最高等级。**已做**：清单给的两步都按原样落地了，而且是**分成两个不同的东西**——`frontend/src/stores/monitor.js:72` 的 javadoc「把警情列表折成『未解除警情表』，**按 `alarmId` 键**」，写入处 `:88` `alarmId: a.id`、`:496` `alarmId: evt.id`，形状声明在 `:155`（`pointId -> 未解除警情 { alarmId, level, status, at }`）。「汇总最高等级」**不在这里做**，而是另一个 getter：`:213-234` `openAlarmsByPoint` 遍历 `activeAlarms` 的全集按 `pointId` 归并，用 `alarmRankOf(level)` 比大小取最高，**等级相同时取触发时间较晚的那条**——注释写明这半句的理由「只是为了让结果确定——**否则同一份数据可能因为遍历顺序不同给出两个答案**」。（这两件事必须分开：存的是全集，着色的是一次聚合。`:487` 那句「着色要的『最高等级』由 `openAlarmsByPoint` 汇总，**不在这里压缩**」就是钉这个的。）原来那个洞的成因留在 `:485-487`：「此前按 pointId 存单条，于是同一点的另一条未解除警情……颜色却没了」。<br><br>**证据**：`frontend/scripts/selfcheck.mjs` §① 含「measurement 并表、alarm 升降级与**同一测点多条未解除警情的汇总**」，本轮复跑 **67 条 / 失败 0**。后端侧的并发一致性（这是本条的另一半，属第 32 条）由 `11-concurrency.sh` 18 条真并发断言压着。<br><br>**边界与仍缺**：前端「同时到两条警情、其中一条 `recover`」这个**具体的解除时序**目前只有 store 层的用例，**没有浏览器级的人工验证记录**——见 `docs/手动验证步骤_第14-16-17-18-19条_20260917.md` 里**第 5–9 条**（与本条无关，但同族的时序与过期类条目）。（2026-09-17 补：原文指「文末手动验证清单」，但本文文末没有该清单——它当时只存在于方案文件里；现落在上述独立文档，条目编号不变。）`alarmRankOf` 的等级序是前端硬编码的，与后端 `alarm_reason`／`level` 枚举是**两处定义**；后端新增告警等级时前端不会自动跟上（当前枚举稳定，未触发）。 |
| 05 | 后端 | **已修复**（2026-09-17，源码级取证；安全默认值在 profile 而不在注解上，见「边界」） | **生产环境关闭演示账号初始化**：目前启动时仍会创建缺失的演示账号，并使用固定初始密码；不能只靠手动删除账号处理。**已做**：做成**配置开关**而不是删代码，安全的那一侧是配置文件的默认值。① 开关：`backend/src/main/java/com/monitor/config/DataInitializer.java:43-46` 上的 `@ConditionalOnProperty(prefix = "monitor.demo-accounts", name = "enabled", havingValue = "true", matchIfMissing = true)`；② **生产默认关**：`backend/src/main/resources/application-postgres.yml:31-33` 写 `monitor.demo-accounts.enabled: ${MONITOR_DEMO_ACCOUNTS:false}`，注释 `:28-30` 把两侧的分工写死了——「安全默认值在这里，dev 的豁免在编排文件里」（意思是：**不设环境变量就是安全的**，要开必须显式设 `MONITOR_DEMO_ACCOUNTS=true`，且 dev 的 compose 自己负责设）；③ 口令可覆盖：`DataInitializer.java:56-63` `@Value("${monitor.demo-accounts.password:" + DEFAULT_PASSWORD + "}")`，默认常量是 `123456`，可用 `DEMO_ACCOUNT_PASSWORD` 换掉；④ 建的是**五个**账号，第五个 `outsider` 不是演示角色而是**数据隔离的对照组**（`DataInitializer.java:65-83` 的注释解释：它不进任何项目，用来证明「可见项目为空」没有 fail-open——MyBatis-Plus 的 `in(空集合)` 会把条件整条丢掉，于是「什么都看不到」会静默退化成「所有项目都可见」）。<br><br>**证据**：`tools/acceptance/README.md:26-30` 已把这条写成**成文事实**而不是操作步骤——「它在 **`postgres` profile 下默认不建**（`monitor.demo-accounts.enabled` 默认为 `false`，见 `application-postgres.yml`——**生产跑的就是那个 profile，所以「生产不建五个统一口令的账号」是默认成立的事实，而不是一条要记得执行的步骤**）」。同一段还点明 `10-scope.sh` 的 `outsider` 对照组依赖 dev 侧显式打开它。<br><br>**边界与仍缺**：⚠ **安全性来自 profile，不来自注解。** `matchIfMissing = true` 的语义是「属性缺失时**启用**」，所以那份安全完全建立在「生产一定跑 `postgres` profile」这个前提上：若有人用 `default` profile 直接 `java -jar`（不设任何环境变量），这份 jar **会**把五个统一口令的账号建出来。缓解是 `application-postgres.yml` 的默认值与 README 的成文约定，**没有**代码级的「非 postgres 环境一律拒绝」保险。要彻底根治有两条路（都不在本批）：把 `matchIfMissing` 改成 `false`（代价：dev 必须显式设，第一次起 dev 库会一个账号都没有、谁都登不进——这正是 `application-postgres.yml:28` 那句注释在防的事）；或在 `DataInitializer.run` 开头断言「当前 profile 不是 postgres 才继续」。另外 **`outsider` 是刻意的保留项**，不要因为它「也是演示账号」而在清理时删掉——删了 `10-scope.sh` 会失去 fail-open 的对照组。 |
| 06 | 后端 | **已修复**（2026-09-17，源码级取证 + `04-alarm.sh` 既有断言） | **告警处置人必须取登录身份**：当前请求中的 `operator` 优先于实际登录用户，可以造成处置记录署名不真实。**已做**：修法与清单里「operator 优先」的表述正好相对——**不是改优先级，是把字段删了**。① 请求体：`backend/src/main/java/com/monitor/alarm/dto/AlarmActionRequest.java:16-22` 现在只剩 `action` 与 `comment` 两个字段；`:6-14` 的类 javadoc 逐字给了理由「**为什么没有 `operator` 字段**：处置人一律取当前登录身份……早期这里有一个可选的 `operator`，且服务端让它**优先于**登录用户——于是任何人都能把处置记录署成别人的名字，而处置留痕正是事后追责要看的那份记录。**删掉字段而不是「收下但忽略」，是为了让「这个值能影响署名」这件事从类型上就不存在**」。② 取值：`AlarmController.java:54-57` 从 `@AuthenticationPrincipal SecurityUser currentUser` 取 `currentUser.getUsername()` 与 `getRole()` 传进服务层；`AlarmService.java:152` 的签名 `act(Long id, AlarmActionRequest req, String currentUser, String role)` 里处置人是**显式入参**、不从 `req` 取；`:206` `act.setOperator(notBlank(currentUser) ? currentUser : AlarmConstants.SYSTEM)`，注释 `:202-205` 写明「处置人只认登录身份，请求体不再有发言权……**SYSTEM 只用于没有登录用户的内部调用**；与 `MaintenanceRecordController` 的 `record.setOperator(currentUser.getUsername())` 同一口径」（两处口径一致这件事是被刻意记下来的）。<br><br>**证据**：`AlarmActionRequest.java:19-21` 记着一条**刻意的诱饵**——老调用方继续发 `operator` 不会报错（Spring Boot 默认 `FAIL_ON_UNKNOWN_PROPERTIES=false`，多余字段被忽略），而 `tools/acceptance/04-alarm.sh` **特意留着这个诱饵来钉住这条行为**。这正是本条最要紧的验收形态：不是「发了会怎样」，而是「发了也不生效、且不报错」——如果哪天有人把字段加回来，那条断言会红。<br><br>**边界与仍缺**：`AlarmConstants.SYSTEM` 这条兜底路径（`currentUser` 为空 → 署 `SYSTEM`）仍然是「无登录身份也能写留痕」，只对内部调用成立；当前没有内部调用方走它，但**没有断言钉住「谁能走到这条路」**——将来若有人从某个匿名端点调到 `act`，署名会是 `SYSTEM` 而不会报错。与清单同族的第 34 条（审计记录增强：真实操作者）共用这个口径，本条只覆盖告警处置那一处。 |
| 07 | 前后端 | **部分完成**（2026-09-17 核查；**HTTP 那条路是完整的，SSE 那条路缺三项**） | **统一质量、目标状态和测值更新**：实时消息未带完整的目标状态、信号和来源；目标消失等状态可能不能随测值同步更新。**已做（字段本身齐全，两条通道只有一条走通）**：**契约与库是齐的**——`docs/message-contract.md:36-39` 定义了 quality/position/signal/state 四个字段，`:108` 落进 `measurement.attributes`；DTO 同名（`telemetry/dto/IngestMessage.java:19-22`）；落库 `IngestService.java:500, 505-517`。**HTTP 回读这条道是完整的**——`MeasurementQueryService.java:139-144` 的 latest 带 quality/signal/position，state 进 `dto/PointLatestVO.java:16-18`；前端快照侧确实消费它（`stores/monitor.js:266-270`）。**SSE 侧 quality 与测值也确实带到了**（`MeasurementEvent.java:18-33` 含 quality，合并时 `...rest` 把它覆盖回来，`monitor.js:451`）。<br><br>**仍缺（清单点名的「目标状态、信号、来源」三样，在 SSE 上一样都没有）**：① **SSE 事件不带 signal / position / state / deviceId**——`MeasurementEvent.of` 只有 5 个参数（`MeasurementEvent.java:18-33`），调用点也只传这 5 个（`IngestService.java:250-252`）。② 后果是**刚显示的「目标失联」会被下一条测值抹掉**：前端合并把三项**显式置空**、并把 state 每次清成 `null`（`monitor.js:451-452`），代码注释自己承认「后端没有推送全部附加字段」——于是那个点会**退回未知状态，直到下一次 HTTP 对账才回来**。这是本条最实在的症状，也是清单那句「目标消失等状态可能不能随测值同步更新」的**准确成因**。③ **「来源」在全仓根本不存在**——契约／DTO／实体／表里 `grep source` **命中 0**，SSE 也不带 deviceId。④ **一处「化石」值得记**：`monitor.js:25` 的 `NON_METRIC_KEYS` 里列着 `deviceId/messageId/sequence/schemaVersion` 四个键，而它们**在 `latest` 对象里根本不存在**——这个集合是「契约里写了、SSE 没落地」留下的痕迹，将来补 SSE 载荷时它才是该被填满的地方。<br><br>**结论**：`已确认` → `部分完成`。**修法明确**：给 `MeasurementEvent` 加字段并在 `IngestService.java:250-252` 传进去，前端 `monitor.js:451-452` 那两行置空随之删掉。**成本低但动契约**，建议与第 28 条（接入响应加标识）合并成一次契约变更。 |
| 08 | 后端 | **部分完成**（2026-09-17 核查；**告警那一层堵住了，当前值与心跳两层没堵**） | **规范无效观测处理**：不能把目标失联当作零位移，也不能持续上报旧值冒充新数据；需要独立的目标状态/设备心跳机制。**已做**：① **「失联当零位移」在告警这一层被堵住了**——`state=disappeared` 不再触发/升级/解除告警，闸门在 `telemetry/service/IngestService.java:272-278`（注释解释了为何**不放 `deriveQuality`**：显式 quality 优先于推导）。② **设备心跳链路存在**——`last_report_time` 用 `receiveAt` 回写（`IngestService.java:255-256, 285-290`），状态推导在 `DeviceStatusPolicy.java:26-29, 36-44`，离线警情由 `DeviceAlarmMonitor.java:66-68, 99-104, 114-123` 定时扫描开/解，**且从未报数的设备不告警**（`:103`——这条边界做对了）。③ 目标状态有**独立的落库→读出通道**（attributes → `PointLatestVO.state`）。<br><br>**仍缺（三项，逐条都比上面那三项更靠底层）**：① ⚠ **「失联当零位移」只堵了告警那一层，没堵当前值**——disappeared 的行**照常落库**（`IngestService.java:234` 是先插后判）、**照常 SSE 广播**（`:248-252` 不受 state 约束），而「当前值」查询**只过滤 `ingest_mode` 与 `collect_time`，不看 quality 也不看 `attributes.state`**（`MeasurementMapper.java:35-53, 131-143`）。所以那个 0／旧值**仍会成为「当前值」显示出来**——清单要防的事在界面上依然发生，只是在告警上不发生了。② **没有独立心跳**——不带 metrics 的心跳报文会被 `EMPTY_METRICS` **拒收**（`IngestService.java:412`），即便收下也会因 `rows==0` 提前 `continue`（`:244-247`），**永远走不到 `:255` 的回写**。结论：设备只能靠**带测值的观测**证明自己在线，没有「只报心跳不报数据」的形态。清单说的「独立的目标状态/设备心跳机制」这半**没有做到**。③ **「旧值冒充新值」没有判据**——幂等键只有 `device_id + message_id`（`message-contract.md:76`、`V1__init_schema.sql:169`），**同一个值换个 messageId 就是一条全新落库**；`deriveQuality`（`IngestService.java:472-480`）**不校验迟到**，而契约 §3 把「晚到」列为 SUSPECT（`message-contract.md:91`）——**契约说要标，实现没标**。<br><br>**结论**：`已确认` → `部分完成`。**这三项里①最值得先做**（改一处查询条件），**②是设计题**（要不要接受只有心跳的报文），③要动幂等语义，风险最高。 |
| 09 | 后端 | **已修复**（2026-09-17，验收 `13-calibration.sh` 53/53 + 单测 `RadarCoveragePolicyTest` 15 条 / `CalibrationServiceTest` 14 条） | **标定变更与失效管理**：设备移动、朝向调整、测点移动后，应使旧标定失效并重新标定，而不是继续沿用 ACTIVE 状态。原缺口描述保留。**三处勘误**：① 「也无手动降级入口」半准——没有状态降级端点，但有一条**破坏性**等价路径 `DELETE /devices/{id}/points/{pointId}`，`device_point` 无软删列，那是硬删行，方位角/斜距/通视等标定参数一起丢；② 隐含前提「用户能在界面上改设备/测点几何」**不成立**——前端没有任何设备/测点建档/编辑/删除 API，所以本条描述的场景在浏览器里**不可达**，它仍是真缺口，但归因是「直接调 API 改档案时无防护」；本批因此是 **API 级**改动，只能用 curl 验收，要 UI 可达须先做第 14 条【**2026-09-17 就此更正，本句自即日起失效**：第 14 条已落地——`AdminView.vue` 设备页签补齐了 7 个几何字段、测点页签去掉了误填的设备字段，设备与测点的几何现在**都能从管理界面修改**，所以「位姿一变 → 旧标定自动转 `INVALID`」这条路径已经**浏览器可达**，本条的验收不再只能用 curl；同一天 `DeviceDrawer.vue` 也补上了人工停用入口与 `validFrom`／`validTo` 输入控件（第 16 条），「失效 → 从界面重新标定」的闭环同样走通了。上面那句「只能用 curl 验收」是**当时**的事实，保留不删，作为改动前状态的记录。**变更的是可达性，不是本条的正确性**——第 09 条的代码与验收结论一字未动。】③ 清单未提的现网真症状——运行时失配检测（`POSITION_CALIBRATION_MISMATCH`）**只拒收、不回写状态**，设备被挪动后该雷达的报文被持续拒收（数据静默断流），界面仍显示「有效」。<br><br>**已做**：① V15 给 `device_point` 加 `invalidated_at`／`invalidated_reason`／`invalidated_by`，`calibration_status` 增 `INVALID`（V11 只定义了 ACTIVE／PENDING，于是设备移动后只能继续沿用 ACTIVE）；② 判定落在既有口径类 `RadarCoveragePolicy.poseChanged`／`pointMoved`（设备 9 项几何 + 测点经纬高 3 项；`code`／`objectId` 不纳入），**用 `compareTo` 而不是 `equals`**——库回读是 `NUMERIC(9,4)` 的 scale 4，JSON 反序列化 `232.0` 是 scale 1，用 `equals` 会把每一次「读出原样存回去」都判成位姿变了，这个 bug 只会在生产上、在每次保存时发作；③ 钩子放在新的 `CalibrationService`，由 `DeviceController`／`MonitorPointController` 覆盖 `update` 委托，**三个注解逐字重声明**（漏 `@PreAuthorize` 就把一个 ADMIN-only 端点悄悄变成「任何已登录用户」，漏 `@Valid` 就丢掉 `MonitorPoint.code` 上唯一的长度校验）；刻意的顺序是先写档案、后置失效——万一将来有人去掉 `@Transactional`，这个顺序的失败模式是**过度失效**（重新标定即可恢复），反过来是静默失效不足，正是本条要消灭的状态；④ 手动停用端点 `DELETE /devices/{id}/points/{pointId}/calibration`（ADMIN／MAINTAINER，幂等，与「解绑」那个硬删行的 DELETE 只差一段路径，破坏性完全相反）；⑤ 重新标定走显式 SQL `applyRecalibration`，**顺带修掉一个相邻的真 bug**：`valid_to` 一旦写过就再也清不掉（`updateById` 跳过 null 字段），没有它，「失效 → 从界面重新标定」会生出一条一出生就已过期的绑定。<br><br>**证据**：`13-calibration.sh` 53/53，含**两次反向验证**（把 `poseChanged` 临时改成 `return false` → §③ 那 4 条红；把 `pointMoved` 临时改成恒 `false` → §⑩ 那 5 条红、套件由 53 变 48/5；两次都实做并按备份逐字节还原）；`RadarCoveragePolicyTest` 15 条含 scale 回归（`232.0` vs `232.000` → 不变）与「after 全 null 不算变更」；`CalibrationServiceTest` 14 条含「重标定走显式 SQL 而不是 `updateById`」的回归。<br><br>**边界与仍缺**：接入路径**不**自动失效（保持只拒收，只补一条带设备码/测点码/上报值/库里标定值的 `log.warn`）——接入批次被契约要求可重试且幂等，把 `device_point` 的写操作藏进「开发环境关着的开关」后面的拒收理由里，会让同一个请求第二次返回不同答案，比它要修的 bug 更坏；前端只改了「不加就会显示错」的那处标签四态（有效／已过期／**已失效**／待标定，原来会把「已失效」显示成「待标定」），停用按钮与有效期输入控件属第 16 条。 |
| 10 | 后端 | **已修复**（2026-09-17，验收 `02-ingest-idempotency.sh`／`08-simulator.sh` 新增断言 + 单测 `IngestTimePolicyTest` 10 条） | **加强时间可信度**：区分采集时间、网关接收时间和平台实际接收时间；增加未来时间、时钟偏差检查，避免影响在线状态与告警。原缺口描述保留。**三处勘误**：① 「影响在线状态」**指错字段**——心跳读的是 `receiveAt`（`IngestService` 回写 `last_report_time`），`collectTime` 不影响在线状态；能钉住在线状态的是**未来的 `receiveTime`**：`DeviceStatusPolicy` 判 `lastReportTime.isAfter(now-5min)`，一个未来的值**永远满足**，设备从此永不离线。② 「未来 collectTime 会撑大延迟判据的分母、从而主动解除真实的延迟告警」——**这条此前转述给使用方的结论是错的，此处改正**：`DataQualityPolicy.ratio` 的分母是**窗口内全部行**，任何「不算延迟」的行都稀释分母（准点到达的新数据稀释、补报的行稀释、未来时间的行同样稀释），所以未来时间戳在这条判据上**没有独有的杀伤力**，本批**不动** `DataQualityPolicy`（加一条 `collectTime` 未来则不算延迟的显式守卫在今天是**行为等价的空操作**，只买到一行注释，却要碰一个有 49 条验收断言压着的判据文件）。它真正独有的危害是**钉住当前值**：`latestRowOf` 按 `collect_time DESC` 排序，2099 的采集时间**无条件胜出**，被钉住的是 `/points/{id}/latest`、项目快照与项目概览 KPI；而且前端 SSE 合并「丢弃 collectTime ≤ 当前最大值的事件」会让之后所有正常上报**全被丢弃**，不刷新页面回不来。③ 「区分采集/网关/平台三个时间」目前只有两分：**「网关接收时间」在全仓没有字段**（`IngestMessage` 没有、`docs/message-contract.md` 没有、表里也没有列），凭空用 `now()` 造一个只会得到一列「长得像数据、其实是 `receive_time` 副本」的东西，记为**待契约变更**。<br><br>**已做**：① 未来 `collectTime` → **拒收** `COLLECT_TIME_IN_FUTURE`，容差 300 秒；闸门放在**无条件**的校验路径上（`basicError`，紧跟 null 判定之后），**刻意不放进 `contractError`**——那里第一句是 `if (!strictContract) return null;`，而全部验收套件与 dev 都是 `strictContract=false`，放进去等于它**永远不会被跑到**、dev 也继续中毒；同一批的每一条对着**同一个** `now` 判（否则边界那条的结论取决于这批跑了多久）。② 未来 `receiveTime` → **钳制**到当前时间，不拒收：`receiveTime` 是**平台自己的字段**（契约写明「是唯一允许缺省取当前时间的字段」），因为一个头部字段写错就丢掉一条真实测量，是在丢掉平台本身就是权威的那份数据；钳制插在**严格模式的畸形判定之后**（插到前面，`invalid-receive-time` 那条契约负向用例会从 `REJECTED:INVALID_RECEIVE_TIME` 变成「收下并静默改写时间戳」）。**这一半才是真正要紧的那一半**——钉住在线状态的洞就是它。③ 查询侧兜底：`latestRowOf`／`latestRowsOfPoints` 加 `collect_time <= now+容差`（接入闸门保护**新数据**，查询上界保护**表里已有的**：手工 SQL、恢复回来的备份、上一个版本写入的行、部署与迁移之间的窗口），口径上不是新规则，而是既有那句「只有 `REALTIME` 的行才是当前值」的第三个从句。④ `measurement.created_at` 填成**平台写入本行的时刻**（列 V1 就有、一直空着），它给出 `created_at − receive_time` = 平台从网关交棒到落库的处理时延。⚠ **它不是网关接收时间**，这句话写进了实体 javadoc 与迁移注释，免得后人读错。⑤ 模拟器 `--clock-anchor start／end`，**默认改成 `end`**：`radar_simulator.py` 原先默认就在喷未来时间戳（时钟从 `now` 起步、每轮 `+= step`），而 `08-simulator.sh` 正是靠它才绿的；改完 08 的断言**一字不改**（逐条已核对），并新增一节「未来采集时间被拒收」——原本挡路的那个套件，变成守这条不变式的套件。保留 `--clock-anchor start`，让 08 能**故意**造未来时间戳。<br><br>**证据**：`02-ingest-idempotency.sh` §⑩ 同族断言**断言 reason 而不只是计数**（否则未来时间与 `INVALID_COLLECT_TIME` 不可区分），并含**反向过拟合断言**：`collectTime = now + 60s` 必须 `accepted=1`——没有它，一个「拒绝任何未来时间」的实现也能全绿；`08-simulator.sh` 新节 `rejected=1`／`reason=COLLECT_TIME_IN_FUTURE`；`IngestTimePolicyTest` 10 条打边界（恰好等于容差收、+1 秒拒、容差−1 秒收、过去收，钳制的缺失／超前 10 年／恰好容差／容差+1／落后 1 年），并含一条**可执行的不变式** `MAX_COLLECT_AHEAD_SECONDS >= OFFLINE_MINUTES * 60`。<br><br>**边界与仍缺**：真正的「网关接收时间」列（需与网关方改契约）；时钟偏差**告警**（清单说的是「检查」，本批交付的是检查：拒收／钳制 + 日志，做一个新告警成因需要新 `alarm_reason`、扫描器接入、快照形状与自己的套件）；不钳制经 `PUT /devices/{id}` 进来的 `lastReportTime`（那是 ADMIN 在显式声明心跳，是不同的威胁面）；`receiveTime` 早于 `collectTime` **不改写**（大负差只可能意味着设备时钟略快，而「采集时间明显偏早 + 接收时间正常」正是补报的正常形态）。 |
| 11 | 后端 | **已修复**（2026-09-17，验收 `12-ingest-concurrency.sh` 15/15 + 单测 `IngestServiceDuplicateTest` 2 条） | **并发重复上报**：担忧成立且已复现。约束名勘误：实际是 **`uk_measurement_device_msg UNIQUE (device_id, message_id, metric_code)`**（`V1__init_schema.sql:169`），本文档原先写的 `uk_measurement_message_metric` 是错的。`IngestService.ingest` 是 `@Transactional` + 逐条 insert，按 `messageIds` 的预取只挡得住**顺序**重发，两个并发重试会同时通过预取、各自 INSERT，撞约束后 PG 把整个事务置为 aborted → **整批回滚，连同一批里的其它合法测点一起丢**，客户端拿到 500 而不是可识别的 `DUPLICATE`。**实测（同一段探针，6 个并发请求各发 `[独有a, 共享, 独有b]`）**：修复前 1×200 + **5×500**、只落 2 行；修复后 6×200、13 行（PG 与 H2 各一次）。修法：按消息设 savepoint（`insertRowsGuarded`）+ 捕获 `DuplicateKeyException`/`CannotAcquireLockException` + **回查幂等键**区分「重复」与「失败」——回查命中才敢说 DUPLICATE，库里没有则重试一次、再失败原样抛出（宁可 500，也不能把没落库的数据说成「重复」，那等于静默丢一条真实测值）。**一个边界**：单个请求不可能撞出自己的唯一键冲突（循环里第 182 行按「设备+消息号」查内存集合、第 241 行加入同一集合），所以这个冲突在结构上必然发生在两笔事务之间，没有「不用并发就能确定性复现」的写法——这也是该缺陷能活到现在的原因。 |
| 12 | 后端 | **已修复**（2026-09-17，源码级取证；**本条是三处「额外关注」里证据最硬的一条**） | **账号停用与权限变更及时生效**：JWT 鉴权主要使用令牌中的身份和角色；需补齐停用、降权、密码重置后的令牌失效机制。**已做**：做成**令牌版本号**（token version），并把「回库核对」变成每次请求的固定动作。① 载体：`V13` 迁移给 `sys_user` 加 `token_version`（实体字段 `backend/src/main/java/com/monitor/auth/entity/SysUser.java:41`）；② 签发时写入：`auth/security/JwtUtil.java:25` `public static final String CLAIM_VER = "ver"`，`:40` `generateToken(Long userId, String username, String role, int tokenVersion)`，`:37` 的 javadoc 注明「之后该字段一旦被递增，此令牌即失效」；调用点 `auth/AuthService.java:33-35`（登录时带 `tokenVersionOf(user)`，`:67-69` 「缺省按 0：V13 之前建的行在迁移时被填成 0」）；③ 递增：`auth/mapper/SysUserMapper.java:19` `@Update("UPDATE sys_user SET token_version = token_version + 1 WHERE id = #{id}")` ——注释 `:15` 特意说明「**用 `token_version = token_version + 1` 而不是『查出来、加一、`updateById`』**」（后者在并发下会丢更新）；`AuthService.java` 的登出路径调用它，注释写明「作废该用户此前签发的**全部**令牌」；④ **校验：每次请求回库**——`auth/security/JwtAuthFilter.java:96-108` 从 claims 取 `CLAIM_VER`（`:97` 用 `instanceof Number` 模式匹配，缺 claim 或类型不对都按失效处理，`:98` 只有一条 debug 日志），`:107` `if (current != tokenVersion.intValue())` 判版本。**为什么必须回库**这段理由完整写在 `:25-40` 的 javadoc 里：纯无状态地信 claims 时「停用账号／降权／改密」在令牌剩下的寿命里（**默认 24h**）全部无效，而且 `SecurityUser#isEnabled()` **在 JWT 路径上永不参与判断**——它只在登录那一刻被看过，「于是删掉一个离职人员的账号，他手上的令牌还能继续用一整天」；现在每次请求回库核对三件事：**账号还在、`enabled` 仍为真、令牌版本与 `token_version` 一致**；⑤ **降权也顺带解决了**：同一段 javadoc 后半句「并且**用库中这一行构造主体**，所以角色取的是**当前**角色而不是签发时的旧角色——降权同样立即生效」。清单问的三个场景（停用／降权／密码重置）因此共用同一条机制，不必各写一套。<br><br>**证据**：源码级取证（`文件:行号` 已在①～⑤ 内逐条给出），本轮**未**找到针对它的专门验收套件或单测——见「边界」。<br><br>**边界与仍缺**：① **没有牙齿。** 本条的正确性完全建立在读代码上：目前**没有**一条断言是「递增 `token_version` 后旧令牌必须 401」。`01-archive-auth.sh` 覆盖的是登录/鉴权的基本面，`10-scope.sh` 覆盖数据范围，都不是这个语义。补一条端到端断言是下一步该做的，红法明确（把 `JwtAuthFilter.java:107` 那句版本比对去掉就该变 200）。② **每个请求多一次主键查询**——javadoc `:34-36` 自己记了这笔账与取舍：「这个系统是单机内部部署，这笔开销换『权限变更立即生效』是划算的；真要优化就加短 TTL 缓存，但那样『立即』会变成『最多 N 秒后』，得先把延迟写进文档」——**当前没有缓存，所以「立即」是真的立即**。③ javadoc `:38-40` 记着一个刻意的设计：**不**把「查不到用户」和「版本不匹配」区分成不同响应（两种情况都只是清掉上下文、由认证入口点统一回 401），理由是「区分开来等于告诉调用方『这个 token 的签名是对的、只是人没了』」。④ 令牌**寿命仍是 24h 且不可撤销单条**——本条解决的是「变更后旧令牌立即失效」，没解决「令牌本身容易被长期持有」；那属第 41 条（HTTPS／凭据轮换）与第 42 条（URL 里的令牌泄露）。 |

其中 **04、05、06、12** 是本轮整体审查中需要额外关注的条目，不能只处理三维页面。

## 二、前端：管理和日常操作需要补齐

| 编号 | 状态 | 需要完善的内容 |
|---|---|---|
| 13 | **大部分完成**（2026-09-17 晚三次更正，按用户口径**重划了责任边界**） | **用户与项目成员管理**：创建账号、停用账号、重置密码、分配角色、分配项目。<br>**2026-09-17 晚更正（一）**：本人那一半先落地——个人中心 + 自助改密（`PUT /auth/password`，V17 加 `phone`/`email`/`job_title`）。<br>**更正（二）**：管理员那一半按「管理员能改别人资料」实现了一版（建号 / 改资料 / 改角色 / 停用 / 逻辑删除，`15-users.sh` 23 条）。<br>**更正（三，取代前两版的边界）**：用户明确了责任划分，**据此重做**：<br>① **个人信息归本人**——注册时自己填（`POST /auth/register`，免登录，注册即登录），之后在个人中心自改（`PUT /auth/me`：姓名/公司/岗位/电话/邮箱）。**管理员改不到这些字段**（`PUT /users/{id}` 的 DTO 里只有 `role` 与 `enabled`，塞进去也会被忽略）。<br>② **权限与存续归管理员**——`GET /users` 看全部账号（不外泄 `password`/`tokenVersion`）、改角色与启用、**删除账号**（逻辑删除；删除后本人无法登录，**同名可以重新注册**——逻辑删除的行还占着唯一索引，注册路径会复用那行墓碑并 `token_version+1`）。<br>③ **注册不能自授管理员**：`role` 只收 OPERATOR/ANALYST/MAINTAINER；公司名自由文本，按需自动建组织。<br>④ **角色即身份**：登录不带角色参数，越权一律由后端 `@PreAuthorize` 拦；前端侧边栏按角色收敛（设备状态=ADMIN/MAINTAINER/OPERATOR，影像挂点=ADMIN/MAINTAINER/ANALYST，管理端/审计=ADMIN），与路由 `meta.roles` 同口径。<br>回归断言 `tools/acceptance/15-users.sh` **32 条**（全套 15 套件 / 527 断言）。<br>**仍未做（都需要外部条件或额外设计，登记在案）**：① **忘记密码的自助找回**——改密要验原口令，忘了只能重新注册或由管理员删除后重注册；真正的「找回」需要**邮件/短信通道**（要接外部服务）；② **注册验证/审批**——目前填个账号名就能用（这也是「删除后自己重新注册」成立的前提），要收紧得引入邀请码或管理员审批；③ **分配项目**（`project_member` 仍只有 `ProjectMemberInitializer` 种子写入，界面改不了成员关系——它决定对方能看到哪些项目的数据）；④ **组织管理**（改名/合并/删除），目前自由文本自动建组织。 |
| 14 | **已修复**（2026-09-17，`check-p0-stage1.mjs` 三项源码绊线 + 人工验证第 10、11 条） | **雷达配置表单不完整**：后端已有高程、天线高度、航向、俯仰、量程和视场字段，但当前管理表单没有完整提供编辑入口。**勘误（本条的成因与清单描述不同，且更坏）**：这不是「没提供入口」，而是**提供给了错的实体**——`AdminView.vue` 把 7 个**设备**几何字段填进了 `/v1/points` 表单。因为 Jackson `FAIL_ON_UNKNOWN_PROPERTIES=false`（与第 06 条那条诱饵同一个开关），提交会返回 **HTTP 200 且什么都不改**：界面看起来能填、保存看起来成功、值从来没变过。同一处 `altitude` 在测点页被声明了**两次**、挂着两个不同标签（「雷达基座高程」与「高程」），这是「字段放错实体」留下的化石。**已做**：① 测点页签（`views/AdminView.vue`）删掉那 6 个设备字段与重复的 `altitude`，只留**一个** `altitude`、标签「高程（m）」、挪到经度/纬度旁边；② 设备页签补齐 **7 个**几何字段——`altitude`（基座高程（m））／`headingDegrees`／`pitchDegrees`／`detectionRangeM`／`halfAngleDegrees`／`verticalHalfAngleDegrees`／`antennaHeightM`，位置在经度/纬度之后，并留注释点名 `RadarCoveragePolicy`、`INVALID` 后果、以及「留空不会把已有几何清掉」（`updateById` 跳过 null 字段）；③ **刻意不动 `cleanPayload()`**——留空不发 null，所以空着保存不是「清零」；④ `components/DeviceDrawer.vue` 基本信息页签补一个只读的「安装几何」`el-descriptions` 块（7 项 + 提示「改动任一项都会使已生效的标定失效，需重新标定」），一屏能看到「现在是什么」。字段名与 `asset/entity/Device.java` 逐字一致，所以这是**搬家不是重命名**。<br><br>**证据**：`frontend/scripts/check-p0-stage1.mjs` 项 29/30/31 三条**源码级绊线**（明确标注只证明「用了」不证明「对」）：points 页签的字段集里**不再出现**那 6 个设备键、`altitude` 在 points 页签**恰好一次**、devices 页签里 **7 个键都在**。本轮复跑该脚本 **35 项全部通过**（基线 25）。`npm run build` 通过（18.76s）。**人工验证第 10、11 条**（改航向角要真的变；只改名称不许把标定打成 `INVALID`）留给浏览器。<br><br>**边界与仍缺**：① **第 11 条是第 09 条的端到端回归**——它绿了才说明「从界面改设备」这条新路径没有把标定批量打失效（`RadarCoveragePolicy.compareTo` vs `equals` 那件事在生产上只会在每次保存时发作）；脚本层测不到，必须人工做。② 三个绊线是**读源码**的，改个变量名就可能让它们失效而仍「通过」——它们是防回归的网，不是行为证明。③ 设备**建档/删除**入口仍没有（本批只补了**编辑**已有设备的几何），第 13 条（用户与项目成员管理）、第 15 条（孪生配置页）仍空着。 |
| 15 | 功能补齐 | **数字孪生配置页面**：模型地址、版本、锚点、姿态、比例、初始视角和加载验证应能通过界面完成，不长期依赖手工调用 API。 |
| 16 | **已修复**（2026-09-17，`check-p0-stage1.mjs` 四项源码绊线 + 人工验证第 12、13 条） | **标定界面补有效期和失效提示**：当前界面主要按 ACTIVE+LOS 显示”有效”，还需要核对有效期，支持重新标定及停用操作。**勘误（本条实际是两件事，其中一件是静默的数据错误）**：「失效提示」那半由第 09 条那批做掉了（标签四态，原来会把「已失效」显示成「待标定」）。剩下一半的严重程度比清单写的高：标定对话框（`DeviceDrawer.vue`）**没有 `validFrom`/`validTo` 输入控件**，而 `openCalibration` 把行里已有的两个值**带进了表单**、`saveCalibration` 又把整个对象发出去——于是**从界面重标定会不知不觉沿用上一次的有效期**，用户以为自己设了「长期有效」，实际继承了一条几个月前的 `validTo`，产出一条一出生就已过期的绑定。另一半是 `api/monitor.js` 的 `invalidateDevicePointCalibration`（人工停用端点）**零调用点**——后端有这个能力，界面上点不到。<br><br>**已做**：① 对话框补两个 `el-date-picker type=”datetime”` 绑到已有的 `calibrationForm.validFrom`/`validTo`（`value-format=”YYYY-MM-DDTHH:mm:ss”`），占位文案写明「**留空 = 立即生效**」／「**留空 = 长期有效**」并带清除按钮——这一条同时消掉「沿用旧有效期」的坑；② 操作列 width 140→190，在「标定」与「解绑」之间插一个**停用**按钮（`type=”warning”`，解绑才是 `danger`），受 `canWrite && row.calibrationStatus === 'ACTIVE'` 门控；**文案刻意把两件事分开**：停用＝保留绑定与标定参数、只把状态置 `INVALID`（可重新标定恢复）；解绑＝**硬删行**，`azimuth`/`slantRange`/`lineOfSight` 一起丢；③ 停用走**确认框 + 原因下拉**（新增一个小 `el-dialog`：`el-select` + 说明 + 确认），选项取后端白名单**五值** `MANUAL`/`DEVICE_RELOCATED`/`TARGET_REMOVED`/`MAINTENANCE`/`SUSPECTED_DRIFT`、默认 `MANUAL`，确认后调 `api.invalidateDevicePointCalibration(deviceId, pointId, reason)` 并刷新绑定表；标签文案**复用已有的** `CALIBRATION_REASON_LABELS`（已含全部 5 个值 + 2 个后端自动原因），不新造一份；④ 顺手修一处文档错误：`DeviceDrawer.vue` 的函数 JSDoc 写「四态」而函数实际返回**五态**（已失效/待标定/未生效/已过期/有效），改成五态并把「未生效」那一档为什么此前不可达写进注释。<br><br>**一个刻意的技术选择**：**没有用 `ElMessageBox.prompt`**——它的 `inputType` 不支持 select，而自由文本会被后端白名单 **400**。理由写在代码注释里，免得后人「简化」回去。<br><br>**证据**：`check-p0-stage1.mjs` 项 32/33/34/35 四条源码绊线：对话框模板含 `validFrom` 与 `validTo` 的 v-model、`invalidateDevicePointCalibration` 在 `DeviceDrawer.vue` 里出现、白名单 **5 个值**齐全、复用 `CALIBRATION_REASON_LABELS`。本轮复跑该脚本 **35 项全部通过**。<br><br>**边界与仍缺**：① 四条绊线是**源码级**的（只证明「写了」不证明「点得通」）——「点停用选检修 → 标签变已失效且 tooltip 带原因/时刻/操作人」必须人工验（清单第 13 条）。② **本条与第 14 条共同让第 09 条的验收从「只能 curl」变成「浏览器可达」**，那句旧结论已在第 09 行就地更正。③ 有效期只是**能填了**，没有「即将过期」的提醒，也没有在前端校验 `validFrom < validTo`（后端会拦）。④ 停用按钮只在 `calibrationStatus === 'ACTIVE'` 时出现——对**已过期**的绑定（`EXPIRED`）界面上没有停用入口，后端其实允许；这是一个刻意的收敛（避免对一条本就没生效的标定做无意义留痕），若现场需要再放开。 |
| 17 | **已修复**（2026-09-17，`selfcheck` 乱序用例 + `check-p0-stage1.mjs` 接线绊线；**组件内竞态仍需人工验**） | **快速切换测点时防请求串数据**：曲线详情请求没有完整的过期响应保护，旧请求后返回时可能覆盖新测点结果。**勘误（一处伪优化，记下来免得后人重做）**：`watch(selectedId, syncMetricCode)` 与 `watch([selectedId, metricCode, granularity, rangeHours], loadDetail)` 都是 pre-flush，同一次 flush 里按创建 id 排序，所以 **`syncMetricCode` 先跑完，切测点本身只打一次请求**。真正会打两次的是 `loadMetrics()` 异步返回后调 `syncMetricCode()`，与 `loadPoints()` 设 `selectedId` 分处两个 tick。「改 watch 配置能省一次请求」是**伪优化**，结论不因此改变（必须有守卫）。**已做**：① 新增 `frontend/src/utils/requestGuard.js`（`createRequestGuard()` → `next()` / `isCurrent(token)` / `invalidate()`）。**为什么单独抽一个文件**：守卫的语义与 `ScreenView.vue` 的 `sceneLoadGeneration`、`monitor.js` 的复合守卫逐字相同，但**这两种写法都在 `.vue` / store 里，而本仓两个前端测试 harness 都加载不了 `.vue`**——抽成纯函数才测得到，这是本条唯一能被脚本真验的部分；② `PointsView.vue` 的**两个 loader 各一个守卫**（告警页签与曲线是两次独立取数，共用会互相顶掉），`loadDetail` 与 `loadAlarms` 各 **3 处**判定：写回前、**catch 分支里**、`finally` 里；③ **请求参数全部快照**（`pointId`/`code`/`gran`/`from` 在 `await` 之前取好）——否则请求与「它属于哪一代」对不上，这是「串数据」的另一半；④ `finally` 里 `if (guard.isCurrent(token)) loading.value = false`——旧请求的 `finally` 提前关掉**在途新请求**的 loading，正是「加载完了但图还是空的」的来源；⑤ `onBeforeUnmount` 里两个守卫都 `invalidate()`；⑥ **切换选择键时先清空**（`selectedId\|metricCode\|granularity\|rangeHours` 变化才清，手动刷新／同参重试**不**清，否则每次刷新图闪一下）——否则切点后到新响应落地之间，标题已经换了点号、表格还是上一个点的数据；⑦ **同类第三处一并修**：`views/home/HomeAnalyst.vue` 的 `pick(row)` 是同一个洞；这里的接线有个次序讲究——`seriesGuard.next()` 必须在 `if (!row?.pointId)` 早返回**之前**调用，否则「点了没有 pointId 的行」会让上一代守卫停在被认为在途的状态。<br><br>**证据**：`frontend/scripts/selfcheck.mjs` §④ 三条守卫语义断言（两个 `next()` 后第一个 token 失效；`invalidate()` 后全部失效；`next()` 自增不复用）**加一条乱序完成断言**——A 先发后回、B 后发先回，断言 A 的 token 失效而 B 的仍有效，落盘顺序为 `B`。`check-p0-stage1.mjs` 项 27/28 两条**接线绊线**：`PointsView.vue` 每个 loader 的判定处**恰好 3 处**、`HomeAnalyst.vue` 的 `pick` 也是 3 处且 `next()` 在早返回之前（**明确标注只证明「接上了」不证明「接对了」**）。本轮复跑：`selfcheck` **67 条 / 失败 0**（基线 35）、`check-p0-stage1.mjs` **35 项全部通过**（基线 25）。<br><br>**证据的反面（反向验证实做）**：① 去掉 `PointsView` `loadAlarms` 的 `finally` 守卫 → 绊线报 `AssertionError: loadAlarms 的守卫判了 2 处，应为 3（写回/catch/finally）`，退出码 1；② 把 `isCurrent` 改成恒 `return true` → `selfcheck` 3 条失败，乱序那条显示 `— B,A`（晚到的 A 覆盖了 B），退出码 1。两次都**实做并按快照逐字节还原**（8 个文件 `cmp` 全等，快照合并 md5 `b665885b0ca4bef939c512b1fb4932e6`）。<br><br>**边界与仍缺**：⚠ **组件内的接线只有绊线、没有行为测试。** 两个 harness 都加载不了 `.vue`（SSR 直调 setup 拿绑定会在 `useRouter()` 上炸），所以「守卫真的在浏览器里挡住了乱序响应」这件事**脚本层证明不了**，只能人工验——见 `docs/手动验证步骤_第14-16-17-18-19条_20260917.md` **第 1、2 条**（连点 5 个测点用 DevTools 限速放大乱序；切路由后 5 秒内旧响应到达）。（2026-09-17 补：原文指「文末手动验证清单」，但本文文末没有该清单——它当时只存在于方案文件里；现落在上述独立文档，条目编号不变。）**这一条不许因为脚本全绿就当成验完了。** 另：绊线是读源码文本的，改名/换写法可能让它们静默失效。 |
| 18 | **已修复**（2026-09-17，`selfcheck` 粒度/并发池/分段 + `check-p0-stage1.mjs` 六项；1000 点规模仍需人工验） | **回放加载优化**：目前逐点并发读取近七天原始数据；需要分时段、限制并发，并防止切换项目/测项时旧请求覆盖新结果。**规模实测（这是选型的唯一依据，不是估算）**：生产基线是 **5 秒采样**（`tools/production_simulator/README.md`：10 雷达／1000 点／2 测项／5s），`/points/{id}/series` 的 **`raw` 分支不抽稀、没有条数上限**，于是**单点 7 天 = 120,960 行**；7 点 = **84.7 万行 ≈ 38MB JSON / 85MB 堆**（分段只把单个响应降到 5.4MB，**总量不变**）；1000 点 = **1.2 亿行** → 必崩。原写法还有另外三个洞：用的是 `monitor.points`（**全部可见点**，而大屏画的是 `pointsOfProject`）、**不传 `to`**、无守卫且无条件写 `this.frames`。<br><br>**已做（三道闸，按出力排序）**：① **粒度降采样——总量问题的主力**。`pickGranularity(rangeHours, pointCount)` 导出为纯函数（**为了可断言**），按最坏采样密度估行数：`points × hours × 720 ≤ 200_000` → `raw`，否则 `points × hours ≤ 200_000` → `hour`，再否则 `day`。**必须把点数一起算进来**——只用窗口决定粒度，7 点项目能接受的窗口 1000 点项目接受不了，那一定会在最大的那个项目上崩一次。实测 `(24,7)→raw`、`(168,7)→hour`、`(24,1000)→hour`、`(720,1000)→day`。② **并发池** `mapWithLimit(items, limit, fn)`，`MAX_INFLIGHT = 4`。**刻意不用「每 K 个一组 `Promise.all`」**：分组会在每组末尾形成**波峰等待**（组里最慢的拖住整组，下一组才发车），池子是流水线、没有这道墙；单个请求失败各自 `catch(() => null)`，不因一个点失败而停摆。③ **按天分段** `SEGMENT_HOURS = 24`、**由近及远**，相邻段边界 `to = 下一段 from − 1ms`——后端 `from`/`to` 是**双闭区间**（`MeasurementQueryService.series` 用 `ge`/`le`），不减这 1ms 边界行会同时落在两段里、合帧时多出重复采样时刻。④ **阶梯可见**：多段路径下**最近一段到齐先成一次帧**（时间轴先显示近 1 天），其余段补齐后再重建一次（变成 7 天）——由近及远保证右边缘的「前值保持／过期」判定天然正确（更老的段不可能让某点的最后一次采样变新），所以先出的那一版**不会**出现「刚进来满屏数据过期」；单段路径（含所有降采样路径）不触发，只成一次帧。⑤ **代际守卫**逐字沿用 `monitor.js` 那套复合守卫形态（module 级 `WeakMap` + 按 store 实例计数 + `projectId` + `primaryMetricCode` + token），**多比一个 `metricCode`**。⑥ **`inflightKey`**：同一份窗口已在飞时不再发第二遍，`finally` 里**只清自己那一份 key、只关自己那一份 loading**。⑦ **换用 `monitor.pointsOfProject`**——回放取数必须与 `displayPoints → enrichedPoints → pointsOfProject` 同一集合，否则「拉了的点不画、画了的点没拉」。⑧ **窗口可选 + 粒度可见**（`ScreenView.vue` 时间轴区域）：新增 `rangeHours` state 与 `setRange(hours)`（写值后 `if (this.enabled) this.load()`），下拉四档近 6h／24h／3d／7d；旁边标 `GRANULARITY_TEXT` 的「按原始点／小时／天聚合」。**必须标粒度**——不标的话用户看到「7 天只有 168 个点」会以为丢了数据。<br><br>**证据**：`selfcheck.mjs` §⑤：`pickGranularity` 六条（四组实测 + **两条反向**：`(0,1)→raw` 零窗口不许抛也不许算出 `day`、`(24,1)→raw` 单点不许被无故降采样）；`mapWithLimit` 四条（结果顺序与输入一致、并发峰值 ≤ limit、一个抛错不中断其余、**牙齿**：8 个任务 limit=4，**只 resolve 第一个**时第 5 个必须已启动——用「每 4 个一组 `Promise.all`」冒充池子的实现会红）；`splitWindow` 四条。`check-p0-stage1.mjs` 项 22–26 六项：只拉本项目测点、`to`/`from`/`granularity` 都带、按天分段 7 段且 `segs[i].from === segs[i-1].to + 1`、切主测项守卫（迟到批次不改 `metricCode`/`frames`/`values`、loading 不被旧请求关掉）、`inflightKey` 去重（同一 key 连点两次只发一轮）。本轮复跑 **67 条 / 失败 0** 与 **35 项全部通过**。<br><br>**证据的反面（反向验证实做）**：把 `mapWithLimit` 换成「每 4 个一组 `Promise.all`」→ `selfcheck` 报池子那条失败、顺序显示 `— 0,1,2,3`（**注意：这个牙齿在 `selfcheck` 里，不在 `check-p0-stage1` 里**——我第一次跑错了套件，看到退出码 0 差点当成通过），实做后按快照逐字节还原。<br><br>**边界与仍缺**：① **真正的残留是「1000 点 = 1000 个请求」本身**（250 波往返），不是行数。降采样解决的是总量、池子解决的是瞬时压力，**请求个数没变**。要根治需要后端批量端点（一次取全项目 series）+ `DataScope` 断言 + 契约文档变更，单列一条、**本批不做**；取数已在 `mapWithLimit` 的 fn 里收口，将来换批量端点**只改那一处**。② **分段是兜底不是主力**（诚实记账）：降采样生效后 `raw` 只剩短窗口（7 点项目 `raw` 的上界约 40 小时），分段防的是「实际采样密度高于基线」时同一窗口行数翻数倍（1 秒采样 = 基线的 5 倍）。③ `ROW_BUDGET = 200_000` 的出处是「7 点 24h@5s ≈ 12.1 万行 ≈ 5.4MB 是浏览器还能一次吃下、还能在内存里驻留成帧的量级」——**这是一个判断，不是实测出来的浏览器上限**，换机器可能不同。④ **1000 点基线库上的真实表现（Network 并发 ≤ 4、首屏不崩、时间轴能拖）必须人工验**——见 `docs/手动验证步骤_第14-16-17-18-19条_20260917.md` **第 4 条**；脚本层测不到真实浏览器内存。（2026-09-17 补：原文指「文末手动验证清单」，但本文文末没有该清单——它当时只存在于方案文件里；现落在上述独立文档，条目编号不变。）⑤ `REPLAY_RANGES` 与 `PointsView` 的 `RANGES` 是**两份**下拉定义（一个是回放小时数、一个是曲线窗口），没有合并；当前值恰好重合，改一处忘另一处不会报错。 |
| 19 | **已修复**（2026-09-17，`selfcheck` 过期限时/优先级 + `check-p0-stage1.mjs` 小时级自适应牙齿） | **回放体现数据中断**：当前采用前值保持，没有明确的保持时限；长时间没有上报时应显示缺测或过期，不能一直显示最后一个值。**根因（比清单描述的多一处）**：`utils/timeline.js` 的 `buildFrames` 写值时不只没有时限，还**把采样时刻丢了**（`values[s.pointId] = i > 0 ? s.samples[i-1].v : null`），于是「前值保持」没有时限；`ScreenView.vue` 又把陈旧值的**采集时间写成 `replay.time`**——**把「这个值是 3 小时前的」伪装成「就是这个时刻的」**，这比不显示过期更坏；`constants/status.js` 的 `resolvePointVisual` 四组常量里**没有时间维度**。<br><br>**已做**：① **时限＝下限 + 自适应**（用户拍板）：`carryMs = max(5 分钟, 该序列采样间隔中位数 × 3)`。**必须自适应**——生产基线 5 秒，而**演示/验收数据是 1 小时**（`tools/acceptance/08-simulator.sh:69` 断言的相邻间隔就是 3600s），写死 5 分钟会让小时级数据在每两帧之间被判过期 55 分钟、**满屏灰**。② **5 分钟有出处、不是新发明的数**：后端判设备离线用 `DeviceStatusPolicy.OFFLINE_MINUTES = 5`，前端多个页面已成文约定「不重算这个口径、只引用它」（`DeviceView.vue`、`HomeAdmin.vue`）——回放的「过期」是同一件事在时间轴上的说法，用同一个数。③ **用中位数不用最大值**：一次长停机不该把整条序列的判据放大到「以后都算新鲜」，那正好把本条要显示的断流藏起来；单条采样的序列没有间隔可测 → 退回下限。④ `buildFrames(seriesByPoint, { maxCarryMs })` 返回 `{ t, ms, values, sampleMs, stale }`——**`values` 的形状一字不动**（消费方 6 处 + `values[pointId] === 3` 这类断言会全红），新增两个**平行表**：`sampleMs` 记「这个值来自哪次采样」、`stale` 是**稀疏表只记过期的点**（空对象＝全新鲜）。⑤ **`stale` 在 `buildFrames` 里算完，不让消费方拿 `sampleMs` 自己判**——**阈值只能有一个实现点**，否则 `ScreenView` 与 `pointLayer` 各判一次，同一个值会出两个颜色。⑥ **过期 ≠ 缺测**：值**保留**（清成 `null` 会丢掉「这个点最后的读数是 3.2mm」这条信息）。⑦ `frameDataCount` **语义不变**（有值的点数，含过期），新增 `frameFreshCount`（没过期的点数）——界面上的「N 个测点有数据」**只能数后者**，陈旧值算「有数据」正是本条原文说的虚高。⑧ `constants/status.js` 新增第五组常量 `FRESHNESS` 与一档 `stale`，位置**插在 `value === null`／非有限判定之后、超限判定之前**，优先级为：未解除告警（服务端事实）> 质量 FAULT/SUSPECT（服务端判）> 目标状态（服务端判）> **数据过期（前端按时间推）** > 数值超限（前端比大小）> 正常；放退化输入 `{stale:true, value:null}`（正常路径不产生）之后，是为了让它答「暂无数据」而不是「数据过期」——**这段理由写进了注释**，否则后人会随手挪。⑨ `cesium/pointLayer.js` 的 `paint()` 两态 → **三态**：`${num}（过期）` + muted 色（`#b8c0cc`）；**过期照常显示数值**——只说「过期」不显示数，值班的人还是不知道最后是多少；`showRing` 不变（过期不亮环），`resolvePointVisual` **函数本身不用改**，只是 `ScreenView.displayPoints` 把 `stale` 传进去。⑩ `ScreenView.vue`：采集时间改 `replay.sampleMsOf(p.id) ?? p.collectTime`（`formatTime` 走 `parseTime` → `new Date(ms)`，接受毫秒数）、`replayFrameInfo` 改用 `currentFreshCount` 并追加「（N 个已过期）」、`LEGEND` 加「数据过期（值为最后一次读数）」一行。⑪ store 侧 `currentHasData` → `currentFreshCount`，新增 `currentStaleCount` 与 `sampleMsOf`。<br><br>**证据**：`selfcheck.mjs` §②：**改写**了原来那条把「无时限前值保持」固化成正确行为的断言——保留原意「不能用未来值倒推」（`frames[1].values[1] === 1`）**并加钉** `frames[1].sampleMs[1] === 10:00 的 ms` 且 `frames[1].ms !== frames[1].sampleMs[1]`（后者防「取窗口最后一个值」的歪实现）；`gapped` 块用 `{ maxCarryMs: 5*60*1000 }` 打边界，证明**过期但值还在**、邻居不受影响、边界点、恢复后转新鲜，并断言 `frameFreshCount(gapped[1]) === 1 && frameDataCount(gapped[1]) === 2`（同一个帧、两个计数、答案不同——这正是⑧那件事的可执行形态）；`sparse` 块（**两个点错开 30 分钟**：点 1 在 :00、点 2 在 :30）证明默认自适应时限下**永不误报过期**；`resolvePointVisual` 新档 + **优先级三连**（`hasAlarm`→`alarm`、`state:'disappeared'`→`disappeared`、`quality:'SUSPECT'`→`suspect`）+ 退化输入 `{value:null, stale:true}`→「暂无数据」；**三条既有断言一字不改必须仍绿**（`{value:5,threshold:3}`→`over-threshold`、`{value:99}`→`normal`、`{value:null,threshold:3}`→`no-data`）。`check-p0-stage1.mjs` 项 26：小时级序列 + **两个错开 30 分钟的点**，断言 `frames[0]` 是 `freshCount 1 / value null / stale undefined`，`frames.slice(1)` 全是 2，且**逐帧逐点** `sampleMs[id] <= frame.ms`。（**为什么要两个点错开**：只用一个点的话每一帧都恰好落在采样点上，「前值保持」永远不会被走到、断言是**空转**的——这条牙齿第一次写出来就是空转的，是复查时发现的。）本轮复跑 **67 条 / 失败 0** 与 **35 项全部通过**。<br><br>**证据的反面（反向验证实做，两个方向都红）**：① 把 `carryMsOf` 改成**写死 5 分钟** → `selfcheck` 的「稀疏序列自适应」那条失败，**并且** `check-p0-stage1.mjs` 报 `AssertionError: 有过期的帧：[{},{"1":true},{"2":true},{"1":true},…]`——这个报错内容本身就是「满屏过期」那个症状的可读形态，两个套件都退出码 1；② 把 `stale` 档**挪到超限档之后** → `selfcheck` 的「优先级：过期压过超限」失败（**只红这一条**，`{value:null, stale:true}`→「暂无数据」那条仍绿——这是对的，两个次序约束本来就独立）。两次都实做并按快照逐字节还原。<br><br>**边界与仍缺**：① **「过期」是前端按时间推的，不是服务端事实**——它排在告警/质量/目标状态三档之后正是这个原因；设备真的离线时后端会另发设备告警，两者不冲突但不是同一件事。② `CARRY_FACTOR = 3` 与 `ROW_BUDGET` 一样是**判断值**，不是实测出来的最优。③ 降采样路径下（`hour`/`day`）每帧间隔变粗，`carryMsOf` 的中位数也跟着变粗——**这是设计意图**（粒度粗了，判据也该粗），但意味着**同一段时间在不同窗口档位下「过期」的判定可能不同**；这不是 bug，是「粒度标注必须显示」的又一个理由（见第 18 条⑧）。④ `LEGEND` 是**手维护数组**，脚本测不到「图例与判定是否一致」——第 5～9 条人工验证覆盖它。⑤ 第 20 条要的另外 7 态（设备离线、标定无效、接口失败……）**本条只加了「数据过期」一档**，统一状态表达仍是独立一条。 |
| 20 | **部分完成**（2026-09-17 核查；**8 态里 5 态进了统一口径，3 态仍在各自的维度里**） | **统一状态表达**：区分正常、告警、无数据、数据过期、目标失联、设备离线、标定无效、接口失败，不能统一显示灰点或横线。**已做（进了 `resolvePointVisual` 这个统一口径的 5 态）**：正常（`constants/status.js:142`）、告警（`:109-112`）、无数据（`:125-131`）、**数据过期**（`:135-137`，`FRESHNESS` 常量 `:45-47`，值由 `utils/timeline.js:109-127` 算、`views/ScreenView.vue:131` 传入——**这一档是本轮第 19 条刚加的**）、目标失联（`:119-121`）。消费方统一：`cesium/pointLayer.js:59/78/153/214`、`cesium/heatmapLayer.js:91/114`、`ScreenView.vue:612/718/736`。<br><br>**仍缺（3 态，各有各的原因）**：① **设备离线**——**不在 `resolvePointVisual` 里，只在设备维度表达**（`DeviceStatusPolicy.java:36-44` + `DeviceController.java:109-111` 列表推导、`ScreenView.vue:635` 的雷达小圆点、`HomeMaintainer.vue:52-55/112-117`）。**这未必是错**：离线是**设备**的属性、不是测点的，硬塞进测点的视觉解析反而会造出一个语义混乱的档；但清单要的「统一」目前确实没达到。② **标定无效**——只在**绑定维度**（`components/DeviceDrawer.vue:98-114` 四态：已失效／待标定／已过期／有效），同样不进测点视觉。③ **接口失败**——**不在**：`ScreenView.vue:199` 把 `store.error` 折成「加载失败」、`:520-522` 是全局错误浮层，而**此时测点自己仍按「暂无数据」的灰点画**——这正是清单原文说的「统一显示灰点」，**这一条是三项里唯一明确算 bug 的**（接口挂了和真的没数据，值班的人看到的是同一个灰点）。<br><br>**另有一处图例不全（顺带发现）**：图例只列 7 项（`ScreenView.vue:225-233`），**漏了 `resolvePointVisual` 会产出、但图例没画的两个档**——`fault`（数据异常，`status.js:113-115`）与 `suspicious`（目标可疑，`:122-124`）。所以**点上的颜色有、图例里查不到**，等于这两个状态对用户是「没有名字的颜色」。**图例是手维护数组、脚本测不到**，见第 19 条的同一处边界。<br><br>**结论**：`功能补齐` → `部分完成`。**下一步（按性价比）**：补图例那两项（改一个数组）、接口失败与无数据分开（`ScreenView.vue:199` 一处判断）成本都很低；设备离线与标定无效要不要进统一口径**需要先定语义**，不建议动手前改。 |
| 21 | 已确认 | **修正部署环境提示**：网络错误提示仍写死 `localhost:8080`，与当前的 8088 反向代理部署不一致。 |
| 22 | 功能补齐 | **档案批量维护**：测点、设备、测项、标定关系支持模板导入、预检查、错误行提示和结果导出，避免大量手工录入。 |
| 23 | **部分完成**（2026-09-17 核查；**6 个子项：1 项完成、4 项部分、1 项原状**） | **客户使用体验**：常用筛选条件保存、表格分页、标签避让、窄屏布局、危险操作确认、失败后可重试。**逐子项对账**：<br>① **常用筛选条件保存——原状**。全仓 `localStorage` **只存 token/user**（`utils/token.js:5-26`），没有任何筛选持久化；`views/AlarmView.vue:30` 的 `filters` 是普通 `ref`、不落盘。<br>② **表格分页——部分**。有的：`AlarmView.vue:184-190`、`AuditView.vue:178`。**没有的**：`AdminView.vue:387-409`、`DeviceView.vue:65-99`、`PointsView.vue:404-407`（这几个用 `max-height` 滚动代替分页）。<br>③ **标签避让——部分（是近似，不是避让）**。`cesium/pointLayer.js:111-112` 是**固定阶梯偏移**、`:183-190` 按 `groupIndex % 4` 推 `stackIndex`——**没有碰撞检测**，落在同一阶梯的标签**仍然会重叠**。清单要的是「避让」，实现的是一套「分层规则」。<br>④ **窄屏布局——部分**。`@media` 只出现在 `LoginView.vue:361`、`home/HomeAdmin.vue:183/219`、`HomeMaintainer.vue:265`、`HomeAnalyst.vue:261`、`StatTiles.vue:54`——**主工作页全部没有媒体查询**：`ScreenView`／`AlarmView`／`PointsView`／`DeviceView`／`AdminView`／`AuditView`／`MediaView`。也就是说**日常值班真正在用的那几页，一个都没有窄屏适配**。<br>⑤ **危险操作确认——已完成（本组最完整的一项）**：`AdminView.vue:351`（删除，且文案说明是**逻辑删除**）、`DeviceDrawer.vue:240`（解绑）、`:650-675`（停用标定确认弹窗，**本轮第 16 条刚加的**）、`MediaGallery.vue:92`、`AlarmView.vue:78`（误报/解除）、`AppLayout.vue:54`（登出）。<br>⑥ **失败后可重试——部分**。有重试入口的：`AdminView.vue:404-405`（「重试」按钮）、`ScreenView.vue:520-522`（「重新加载」）。**页内无重试入口的**：`AlarmView`／`PointsView`／`DeviceView`／`AuditView`／`MediaView`——它们只有 axios 拦截器弹错 + 轮询或手动刷新，而 `composables/usePolling.js:16-18` 的注释明说「错误展示归各页自管」。<br><br>**结论**：`功能补齐` → `部分完成`。**下一步（按性价比）**：⑤已经最完整、④的缺口最刺眼（主工作页零适配），建议先补④；①（筛选持久化）与⑥（页内重试）都是局部改动，可各自独立做。 |

已有设备绑定和标定窗口，可以继续完善，不需要重新做一套。

## 三、后端：数据与业务完整性

| 编号 | 状态 | 需要完善的内容 |
|---|---|---|
| 24 | 已确认（2026-09-17 核查确认为**原状**，未按本条改造） | **档案请求使用专门的 DTO**：当前通用 CRUD 直接接收实体，字段约束不足。补必填、坐标范围、合法枚举、编码规则，以及禁止客户端修改的字段。**核查结论：一条都没改。** ① **8 个档案控制器全继承通用 CRUD，入参就是实体**——`common/base/BaseCrudController.java:89`（create）与 `:97`（update）；两个覆盖者也照收实体（`asset/controller/DeviceController.java:133`、`project/controller/MonitorPointController.java:69`）；`ProjectController.java:16-36`、`SceneController.java:16-37` **连方法都没覆盖**，直接用基类的。② ⚠ **`@Valid` 对多数实体是空操作，而且文件自己承认这事**——`BaseCrudController.java:81-84` 的注释就写着这层意思。证据：`grep "jakarta.validation"` 在 entity 目录**只命中 `MonitorPoint` 一个**；`Project.java:14-21`、`Device.java:17-37`、`Metric.java:15-22` 以及 Scene／MonitorObject／Organization **全是零约束**。全仓唯一一条实体级约束是 `MonitorPoint.java:33` 的 `code` `@Size(max=64)`（且它是为 B-13 长点号加的，不是本条）。③ **客户端不该改但能改的三个字段**（这是本条最实在的部分）：**`createdAt`**——`common/base/BaseEntity.java:24-25` 只有 `@TableField(fill = INSERT)`、**没有 `update = false`**，而 `MyMetaObjectHandler.java:22-25` 更新时只填 `updatedAt`，所以**请求体里带 `createdAt` 会被 `updateById` 原样写回**；**`code`**——Device／Project／Metric 的 code **可随意改**，而接入正是**按 code 匹配档案**（`IngestService.java:130-137`），**改码即静默断流**（这条与第 25 条的「软删后测值不可达」是同一类静默故障）；**`Device.lastReportTime`**（`Device.java:36`）**可由 ADMIN 经 `PUT /devices/{id}` 直接写**，等价于伪造心跳——⚠ **这一条与第 10 条**「边界与仍缺」里那句「不钳制经 `PUT /devices/{id}` 进来的 `lastReportTime`（那是 ADMIN 在显式声明心跳，是不同的威胁面）」**是同一件事**：第 10 条当时判定它是**有权者的显式声明**、故不在那批处理，此处按本条（客户端不该改的字段）**再次登记**，两条的结论不冲突但需要一起看。④ **坐标与枚举没有范围约束**：`Device.java:23-26`（经纬高）、`MonitorPoint.java:37-39`（经纬高）、`Device.java:34`（`status`）、`Metric.unit` 均**无 `@DecimalMin/Max`、无 `@Pattern`**。<br><br>**一处必须说清的对照（免得误判成「已经有 DTO 了」）**：非档案类写入**确实都用了 DTO**——`project/dto/DigitalTwinSceneRequest.java:20-64`、`asset/dto/DevicePointCalibrationRequest.java:16-44`、`AlarmRuleRequest`、`LoginRequest`。所以现状不是「全仓没有 DTO」，而是**「新增功能用 DTO，档案 CRUD 用实体」两套并存**——本条要改的正是后面那一套，而它牵动 8 个控制器与全部前端档案表单，**是本清单里改动面最广的一条**，这也是它一直没动的原因。<br><br>**边界与仍缺**：① 唯一做对的一处是**主键由路径强制覆盖**，客户端改不了 `id`（`BaseCrudController.java:98`）。② `MonitorPoint.code` 的 `@Size(max=64)` 已经承重（`13-calibration.sh` 有「超长点号被 `@Valid` 拦下 400」的断言），**改本条时不许把它弄丢**——这正是第 09 条那批反复强调「三个注解逐字重声明」的同一个风险。 |
| 25 | **部分完成**（2026-09-17 核查；**「优先停用」这半天然成立，「关联检查」这半一条都没有**） | **删除与关联关系保护**：删除项目、设备、测点、测项前检查历史数据、标定和告警关联；实际业务通常应优先停用，而不是直接删除。**已做（而且比清单预期的好）**：**删除默认就是软删**——`common/base/BaseEntity.java:13, 21-22` 加 `application.yml:41-43` 的 `logic-delete-field: deleted` 由 MyBatis-Plus 接管，**7 个档案的 DELETE 全走** `BaseCrudController.java:103-109` → `deleteById` → `UPDATE deleted = 1`；查询链路自动带 `deleted=0`（`project/mapper/MonitorPointMapper.java:17-19`、`asset/mapper/DeviceMapper.java:18-21`）。所以清单那句「通常应优先停用而不是直接删除」**在档案这一类上已经是默认行为**——这是本条最好的一半。以「停用」显式替代删除的另有两处、但**不成体系**：`MonitorPoint.enabled`（`MonitorPoint.java:40`，接入会拒 `POINT_DISABLED`，`IngestService.java:423`）、`Device.status = FAULT`（`Device.java:34`）。<br><br>**仍缺（「检查关联」这半，一条都没有）**：① ⚠ **零前置关联检查**——所有 delete 端点都只是 `deleteById`，**没有任何一条「还有历史数据／标定／告警」的判据**（全仓 grep「有关联／不能删除／被引用」**命中 0**）。**更彻底的是：迁移里没有任何 FOREIGN KEY**（全部 `V*.sql` grep `FOREIGN\|REFERENCES` **为空**）——所以**库层也不会拦**，删是删得掉的。② **删了之后会怎样（三类，都是静默的）**：**历史测值**留在库里但查询路径不可达——`measurement` **无 `deleted` 列**且**无清理任务**（全仓 `@Scheduled` 只有 `SseBroadcaster.java:162`、`DataQualityMonitor.java:68`、`DeviceAlarmMonitor.java:66` 三处，没有一处是清理）；**绑定行不会被清**——`device_point` 是**硬表**，指向已软删测点的绑定仍在（`DeviceController.java:169-170` 查绑定**不带 deleted 过滤**）；**告警不跟随**——警情按 `point_id` 独立留存，点软删后 `AlarmService.java:298-320` 查不到点号（**列表里点号变 null**），而警情**仍挂「未解除」并继续占着 `open_key`**（第 32 条那个唯一索引）。最后这条尤其值得注意：**它会让那个键位永久被一条已无从追溯的警情占住**。③ ⚠ **破坏性硬删路径仍然在**（与第 09 条勘误①是同一处）：`DELETE /devices/{id}/points/{pointId}` 直接删 `device_point` 行（`DeviceController.java:272-282`；`DevicePoint.java:13` 明写「**硬删除，无软删除字段**」），方位角／斜距／通视**一起丢**；`alarm_rule` 也是**物理删**（`AlarmRuleService.java:96, 112-115`，注释明说删除后**未解除警情会一直占唯一键**）。④ **没有恢复出口**——软删的行**没有任何 API 能查、能恢复**，所以「软删」目前只做到了「不真删」，没做到「可回退」。<br><br>**结论**：`待验证` → `部分完成`（原清单标「待验证」，**核实为「有软删、但无任何保护与恢复」**）。**下一步**：③ 的两条硬删路径先加二次确认与「改为停用」的引导（改前端 + 一个端点），成本低；② 里「警情占键位」建议单独立项，它与第 32 条的 `open_key` 唯一索引直接耦合。 |
| 26 | 功能补齐 | **测量基准版本管理**：记录归零、换站、目标重装、设备重启造成的基准变化，避免累计位移和速率跨基准拼接。 |
| 27 | 功能补齐 | **设备数据来源管理**：明确一测点一个权威来源；多雷达观测同一位置时，增加来源选择、主备切换规则，不能简单混合。 |
| 28 | 已确认（2026-09-17 核查确认为**原状**，未动） | **接入响应增加消息标识**：返回每条消息的 `messageId/deviceId`，便于转换器准确关联失败项、部分重试和对账。**核查结论：一字未改，仍是原状。** 每条结果的字段集就是这五项——`pointCode / collectTime / status / quality / reason`（`backend/src/main/java/com/monitor/telemetry/dto/IngestResult.java:17-25`）；唯一的构造点是 `telemetry/service/IngestService.java:524-532` 的 `item(...)`，四个调用点 `:225 / :240 / :281 / :521` **全都只传这四个值**，`messageId` 与 `deviceId` 一个都没回。<br><br>**这条缺口的性质要说准（免得被当成「数据没存」）**：它是**纯出参缺口，不是数据缺口**——两个值在调用点**手边就有**（`:222` 已经用 `m.getDeviceId()+"\|"+m.getMessageId()` 拼出了幂等键），`messageId` 也确实落在 `measurement` 表里，只是从来没往响应里写过。全仓 `deviceId`/`messageId` **无任何出参暴露**；信封 `common/.../Result.java:16-19` 只有 `code/message/data` 三层。<br><br>**契约侧更空**：`docs/message-contract.md` 里 `grep "响应"` **命中 0**——**该文件根本没有响应章节**，所以不存在「契约写了但实现没跟上」，而是**两头都没有**。旁证：`docs/3D数字孪生_生产数据准备与上线实施指南.md:65` 自己就记着这是缺口（「接入返回项增加 `messageId/deviceId`……**当前只返回测点、采集时间、状态和原因**」）。<br><br>**边界与仍缺**：本条的修复需要**先改契约、再改实现**（响应形状是接入方依赖的接口），而改契约定标准又涉及与网关方的约定——这是它一直没动的原因，不是遗漏。**成本很低**（`item()` 加两个字段 + 契约补一节），但有**外部依赖**（接入方要按新形状解析），所以建议与第 27 条（数据来源管理）一起作为一次契约变更发布，而不是单独塞一个字段进去。 |
| 29 | 功能补齐 | **采集、显示、落库周期分离**：目前收到合法测值就落库。此前提出的实时显示、按 5/10 分钟保存，需要明确保存原始值还是聚合值，并保留告警所需证据。 |
| 30 | 已确认 | **历史查询限制与数据库聚合**：当前曲线按范围读出记录后在内存聚合；需要限制时间跨度、返回点数，并把大规模聚合下推数据库。 |
| 31 | 已确认 | **接入处理性能**：已有档案批量预取，但仍逐测项插入、逐测项评估告警；需要基于实际吞吐测试优化数据库访问。 |
| 32 | **已修复**（2026-09-16） | **并发告警与处置一致性**：担忧成立且是本清单里风险最高的一条。原 `AlarmEngine.findOpen` 是 check-then-act，`alarm` 表**无任何唯一约束**兜底，全仓无 `@Version`/`FOR UPDATE`——javadoc 里那句「同一 point+metric 至多一条未解除」只是应用层约定，多实例部署下完全失效。**已做**：① V14 给 `alarm` 加可空的 `open_key`（`P:<pointId>:<metricCode>` / `D:<deviceId>:<reason>`）+ 普通唯一索引，把约定变成库级约束，并对存量重复警情自动收敛（留最早一条、其余按 `recover` 关闭并留痕）；② 评估移入独立的 `REQUIRES_NEW` 短事务（`AlarmEvalTx`），使 `KeyLock` 的锁区间真正跨过提交点（只加锁不开短事务等于没加）；③ 升级/解除/处置全部改走带前置条件的 CAS，返回 0 行即视为「已被并发改动」，升级与解除**不写留痕、不广播**，处置返回 409；④ 触发的纯读路径保留一个只读预判，避免给每个测值都开一条新连接。**证据**：`AlarmConcurrencySemanticsTest`（6 条，先用真实迁移在 H2 上取证两条数据库语义）、`KeyLockTest`（7 条）、`AlarmEngineTest`（12 条）、验收套件 `tools/acceptance/11-concurrency.sh`（18 条**真并发**断言，牙齿经反向验证：去掉锁+不写 `openKey` 时 ① 红、CAS 改回无条件写入时 ② 红 5 条）。 |
| 33 | **部分完成**（2026-09-17 核查；**清单点名的 4 项只落了 1 项**） | **告警规则更贴近现场**：增加持续时间、连续次数、恢复迟滞和维护期抑制；是否需要短信等通知由业务决定。**已做**：**恢复迟滞**——`alarm/entity/AlarmRule.java:32` 的 `recoveryValue`，解除判定在 `alarm/service/AlarmEngine.java:257-264` 的 `shouldRecover`，并且**只有最初触发的那条规则能解除**（`AlarmEngine.java:207-213`，防交叉解除）。**额外**还有一条清单没要的**防刷屏**：`AlarmRule.java:34` 的 `repeatSuppressSeconds`（`AlarmEngine.java:292-302`）——但它是「别为同一条规则反复开警情」，**不是**维护期抑制，别混淆。<br><br>**仍缺（3 项，且有一项有个容易看错的陷阱）**：① **持续时间——列在、能力不在**。`windowMinutes` 列确实存在（`V1__init_schema.sql:187`、`AlarmRule.java:31`），但它是给 RATE／CHANGE 做**窗口聚合**用的，而 **RATE／CHANGE 在接口层被显式拒绝**（`alarm/service/AlarmRuleService.java:132-139`），引擎**只评估 THRESHOLD**（`AlarmEngine.java:232-234` 与类注释 `:68-71`）。所以**不存在「越限持续 N 分钟才告警」**——看到 `windowMinutes` 这个列名会误以为它已经有了，这是个陷阱。② **连续次数**：**无字段、无逻辑**（`alarm/` 包内 `grep -riE "consecutive\|连续次数"` = **0**，`alarm_rule` 的 DDL `V1__init_schema.sql:174-190` 无此列）。③ **维护期抑制**：**无**（`alarm/` 包内 `grep -riE "maintenanceWindow\|维护期\|maintenance_window"` = **0**；`maintenance_record`（`V1__init_schema.sql:123`）是**设备维修档案**，与告警抑制无关——名字像，但不是）。<br><br>**一处容易误判的地方**：设备离线 5 分钟、数据质量 15 分钟窗口这些**时间概念**确实存在，但它们住在两条**内置策略**里（`telemetry/DataQualityPolicy.java:39,42,45,48`），**不是可配置的测点规则字段**。所以「系统里有时间窗口」不等于「告警规则支持持续时间」。<br><br>**结论**：`功能补齐` → `部分完成`。**短信等通知**按清单原文「由业务决定」，未动。 |
| 34 | 已确认 | **审计记录增强**：记录关键配置修改前后值、失败操作和真实操作者；避免截断后生成不可解析的 JSON。 |

## 四、三维数字孪生专项

| 编号 | 状态 | 需要完善的内容 |
|---|---|---|
| 35 | **部分完成**（2026-09-17 核查；**5 项里 3 项到位、「高程基准」与「控制点验证」缺**） | **真实模型接入与配准**：统一坐标系、高程基准、模型轴向、单位和控制点验证，避免只凭目测调位置。**已做（3 项）**：① **坐标系**——`frontend/src/cesium/digitalTwinScene.js:16-32` 的 `coordinateMode` 只接受 `ENU/EMBEDDED`，且 **ENU 强制要求锚点**（缺锚点直接拒绝，不是静默退化）；锚点经 `eastNorthUpToFixedFrame` + `HeadingPitchRoll` 建 ENU 帧（`:34-58`）。② **模型轴向**——**显式声明**而非目测：`:65-66` 的 `upAxis: Z / forwardAxis: X`，并在资产里记录 `modelAxes`（`frontend/public/models/mountain-demo/scene-config.json`）——「轴向写下来」这件事本身就是本条要的「避免只凭目测」。③ **比例**—`modelScale` 统一缩放（`:53-57`）。库侧字段齐全且与前端同名：`asset_type/coordinate_mode/anchor_*/heading/pitch/roll/model_scale`（`V10__digital_twin_production_config.sql:12-23`，种子 `:44-59`）。<br><br>**仍缺（2 项）**：① **高程基准**——值**直接进 `Cartesian3.fromDegrees`**（`:37-41`），也就是当作**椭球高**用，全程**没有 geoid/EGM 转换**。在本地山体演示里这不是问题（场景是自己生成的、基准自洽），但换成真实测绘数据就会整体偏移若干米——文档自己也点明这个坑（`docs/3D数字孪生_生产数据准备与上线实施指南.md:118`）。② **控制点验证**——**完全没有**：`grep -riE "controlPoint\|控制点\|geoid\|EGM96"` 在 `frontend/src` + `backend/src/main/java` **命中 0**（唯一命中是 Cesium 自带 worker `frontend/public/cesium/Workers/decodeI3S.js` 里的第三方代码），**没有残差校验、没有残差报告产出**。这一项缺失意味着配准「准不准」目前**无法被证明**，只能眼看。<br><br>**仍缺（1 项，程度轻）**：**单位**——只有无量纲的 `modelScale`，配置里**没有单位/量纲字段**；因为整条链都是「模型自带单位 + 统一缩放」，当前不构成实际故障，但「这个 320 是米还是别的」在配置里查不到。<br><br>**结论**：`功能补齐` → `部分完成`。**下一步**：控制点验证是这 5 项里唯一「不做就没法验收」的——加一个「输入 N 组已知控制点 → 输出残差」的小工具即可，成本远低于它挡住的风险。 |
| 36 | 功能补齐（2026-09-17 核查确认为**原状**，未动） | **真实通视计算流程**：当前接收外部 LOS 结果，没有自动对任意现场模型计算遮挡；需要明确计算工具、证据与复核流程。**核查结论：清单那句「当前接收外部 LOS 结果」一字不差地仍然成立。** `line_of_sight` 只是一个**存库的布尔标记**（列 `device_point.line_of_sight`，`V11__radar_calibration_and_mountain_v2.sql:18`），**只能手填**——建绑定时给默认值（`asset/controller/DeviceController.java:194 / 228`），读出映射在 `project/service/DigitalTwinSceneService.java:172`，前端**只做展示**（`cesium/digitalTwinScene.js:241` 染色、`views/ScreenView.vue:755` 显示「无遮挡/被遮挡」）。<br><br>**演示资产另有一个离线计算器，但它不是本条的答案**：`tools/mountain_asset/generate_mountain_glb.py:214-224` 的 `line_of_sight()` 会采样高度场，产出机器可读证据 `frontend/public/models/mountain-demo/coverage.json`。**它是离线一次性生成演示数据的脚本，不是平台能力**——对任意现场模型、任意新绑定的目标都不适用。<br><br>**仍缺**：**平台内对任意现场模型的遮挡计算完全没有**——场景代码里没有任何 LOS 采样/射线求交。⚠ **一个容易误判的点**：`grep -riE "sampleTerrain\|raycast\|clipping"` 在 `frontend/src` 里**有命中**（`createViewer.js` 与 `ScreenView.vue:457`），但 `ScreenView.vue:457` 的 `sampleTerrainMostDetailed` **只是把立柱种到地形面上**（`cesium/pointLayer.js:133-150`），**与通视无关**——不要因为它命中就以为遮挡计算有了。复核流程目前**只写在建议里、不是实现**：`docs/3D数字孪生_生产数据准备与上线实施指南.md:172`、`docs/3D数字孪生_生产候选部署与验收.md:260`、`docs/3D数字孪生_V2双雷达标定与验收.md:18` 三处都只是描述应该怎么做。 |
| 37 | 已确认（2026-09-17 核查确认为**原状**，未动） | **覆盖图形与实际覆盖分开**：当前视场图形没有按地形裁剪；界面应明确理论视场和已验证目标，避免误解。**核查结论：两个子项都仍是原状。** ① **视场不是裁剪出来的，是解析算出来的平面扇形**——`frontend/src/cesium/digitalTwinScene.js:164-232` 用 `start` 点 + 下发的 `heading/halfAngle/pitch/verticalHalfAngle/range` **直接算端点**，`perPositionHeight` 多边形，**无地形采样、无裁剪**。② **界面上没有区分理论视场与已验证目标**——`grep -ri "理论视场\|已验证\|理论覆盖"` 在 `frontend/src` **命中 0**；雷达面板标题就叫「雷达覆盖」，只显示「N 个已绑定目标」（`views/ScreenView.vue:625-638`），目标连线只按 `ACTIVE && lineOfSight` 换色（`digitalTwinScene.js:240-242`），**没有任何「理论/已核验/未知」的图例或措辞**。<br><br>**一个「裁剪不可能发生」的结构性事实（比「没做」更值得记）**：默认就是**本地山体模式**，`createViewer.js:6` 默认 `mountain` → `LOCAL_SCENE_ENABLED`，`setupTerrain` 在 `:70-72` **直接返回 `skipped`**——**真正的地形只在 `globe` 模式下才加载**。所以在当前默认配置下，视场扇形**不是「忘了裁剪」，而是「没有可裁剪的地形」**。这条要让读者知道，否则会以为只是漏了一个 `clippingPlanes` 参数。<br><br>**一处死代码的旁证（引用时需说明）**：`frontend/src/cesium/mountainScene.js:75-76` 有一句很贴切的自我说明——「只表达覆盖方向与范围，**不冒充真实波束强度**」，扇面末端固定 `ground + 42`，是一张平面片。但 **`createMountainScene` 在当前代码里已无任何调用点，属死代码**（被它替换掉的旧实现）；这句话表达的设计意图仍然成立，但**别去那份文件里找活的行为**。<br><br>**旁证**：`docs/3D数字孪生_生产数据准备与上线实施指南.md:83` 把「区分理论视场、已核验通视目标、遮挡和未知区域」列为**需要新增**。 |
| 38 | **部分完成**（2026-09-17 核查；**「有字段有值」但三项能力全缺**） | **模型版本与历史标定关联**：更换模型后保留旧版本和标定依据；增加发布前文件完整性、依赖和哈希校验。**已做（登记面齐全）**：**版本号与哈希有字段、有值**——`digital_twin_scene.asset_version / asset_sha256`（`V10__digital_twin_production_config.sql:15-16`），种子 `mountain-demo-1.0.0` + 哈希（`:52-53`），V11 升到 `mountain-demo-2.0.0` + 新哈希（`V11__radar_calibration_and_mountain_v2.sql:83-84`）；入库格式**有校验**（`project/dto/DigitalTwinSceneRequest.java:36` 的 64 位 hex `@Pattern`）；界面显示版本（`views/ScreenView.vue:91-93`）；资产来源与哈希**成文**（`frontend/public/models/mountain-demo/ASSET_PROVENANCE.md`）；**数据集**另有真正的发布前哈希校验（`tools/production_simulator/validate_dataset.py:23-31`）。<br><br>**仍缺（三项能力，本条要的正是这三项）**：① ⚠ **哈希从来没有被真正校验过**——后端**只存取不计算**（`project/service/DigitalTwinSceneService.java:179 / 193` 就是读写字段），前端**全程无 `crypto.subtle`/digest**（`frontend/src` 内 grep **命中 0**），连生成器自己都**不产哈希**（`grep -i "sha256\|hashlib" tools/mountain_asset/generate_mountain_glb.py` = **0**）。所以 `asset_sha256` 现在是**一个手工填进去、没有任何东西会去核对的值**——它长得像校验，实际是**一条配置记录**（文档自己也这么说：`docs/3D数字孪生_生产数据准备与上线实施指南.md:70`「资产 SHA-256 当前主要作为配置记录」）。**这是本条最要紧的一句话**：不要因为列里有 `sha256` 就以为完整性校验有了。② **旧版本未保留**——V11 是 `UPDATE` **原地覆盖同一行**，`digital_twin_scene` **无历史表**（迁移里 `grep` 只有 V10/V11 命中），仓库内也**只剩一个 `.glb`**（旧模型文件本身也没留）。③ **标定依据未与模型版本关联**——`device_point` 有 `calibration_status/calibrated_at/valid_from`（`V11:20-22`）但**没有 `asset_version` 外键**，所以「这批标定是在哪个模型版本下做的」查不出来。<br><br>**另一处要说明的**：`validate_dataset.py` 的发布前校验是真的，但它的 manifest **只覆盖 `catalog.*` / `messages.ndjson.gz` / `contract-negative-cases` / `ground-truth`，不含 GLB**——所以它保护的是**模拟数据集**，不是模型资产。两者别混。<br><br>**结论**：`功能补齐` → `部分完成`。**下一步**：哈希校验最省事（模型发布时算一次、前端加载前比一次，或至少在生成器里产哈希）；「保留旧版本」需要 `digital_twin_scene` 历史表 + 资产按版本存目录，成本明显更高。<br><br>**2026-09-18 补记**：V18 把默认项目切到 `qingyuan-hillside-1.0.0`（离线真实地形资产）时**保留了** `mountain-demo-2.0.0` 的 GLB 文件与历史迁移，所以上面「仓库内也只剩一个 `.glb`（旧模型文件本身也没留）」这句**已不再成立**——现在仓库里有两份资产，V18 本身就是回退路径。（V19 又追加了 `qingyuan-hillside-2.0.0`，同样是"保留旧版 + 新增目录"；但**这三项能力仍然全缺**：`asset_sha256` 仍没有任何调用点去核对、`digital_twin_scene` 仍无历史表（V18 也是 UPDATE 同一行）、`device_point` 仍没有 `asset_version` 外键。本条其余结论不变。见 `docs/3D数字孪生_离线真实地形资产_20260918.md`。 |
| 39 | **部分完成**（2026-09-17 核查；**5 个场景里 4 个有释放、WebGL 上下文丢失完全没有**） | **长时间渲染稳定性**：反复切项目、回放、开关图层、模型失败、WebGL 上下文丢失时，检查内存释放与恢复。**已做（4 个场景有明确的释放/降级路径）**：① **卸载全清**——`views/ScreenView.vue:494-512` 的 `onBeforeUnmount` 依次释放 postRender/renderError 监听、clickHandler、两层图层、场景，**最后 `viewer?.destroy()`**（`:508`）。② **反复切项目**——`generation` 竞态守卫（`:288-335`），**迟到场景立即 `destroy()`**（`:293` 与 `:314`，不是等它自己回收）；场景 `destroy()` 会摘掉所有装饰实体与 primitive（`cesium/digitalTwinScene.js:344-347`）。③ **模型失败**——catch → `mountainState='failed'` + 错误文案 + **退回 `flyToPoints`**（`:327-334`），不是白屏。④ **回放**——`replay.dispose()`（`:500`、`stores/replay.js:344-347` 停定时器）。<br><br>**仍缺（清单点名的第 5 个场景）**：⚠ **WebGL 上下文丢失完全没有处理**——`grep -rni "webglcontextlost\|contextlost"` 全仓（排除 node_modules/dist/target）**命中 0**。目前只有通用的 `scene.renderError` → 横幅 + 「重新加载」**整页刷新**（`views/ScreenView.vue:382`、`:47-55`、`:519-525`），**恢复完全依赖 Cesium 内部处理与人工刷新**，没有 `contextrestored` 之后的重建路径。这是本条唯一「没有降级路径」的场景，而它恰恰是长时间运行时最会出现的那一个（显卡驱动重启、系统休眠唤醒、GPU 内存压力）。<br><br>**两个「不是 bug 但要知情」的行为**：① **开关图层不释放资源，且是有意为之**——`watch(heatOn → setVisible)` 只切 `show`，实体保留（`:473`、`cesium/heatmapLayer.js:131-133, 177-181`）；这是为了反复开关不重建，代价是关掉图层后内存仍然占着。② **模块级贴图缓存从不清理**（`heatmapLayer.js:42, 68`）——热力图纹理缓存挂在模块作用域，切项目/切场景都不会清，长时间运行会累积。<br><br>**结论**：`待验证` → `部分完成`（已从「待验证」推进到「已知哪些做了、哪个没做」）。**下一步**：加 `webglcontextlost` 监听（`event.preventDefault()` + 提示 + 重建 viewer）是明确可做的一步；贴图缓存清理建议与图层销毁绑在一起。 |
| 40 | **部分完成**（2026-09-17 核查；**「机制都在」但「优先真实地形」与「分块」没有实物**） | **模型精细度与性能平衡**：优先真实地形和目标辨识，再做材质、阴影、设施细节；大场景采用分块和 LOD。**已做（旋钮齐全，而且都落库可下发）**：① **LOD 与显存预算机制在**——3D Tiles 走 `maximumScreenSpaceError / cacheBytes / maximumCacheOverflowBytes / cullWithChildrenBounds / skipLevelOfDetail / preferLeaves`（`frontend/src/cesium/digitalTwinScene.js:74-82`）；资产类型**支持 `GLB / 3D_TILES` 两种**（`:20`，库列 `V10:12`）。② **四个旋钮落库并可下发**：`maximum_screen_error / maximum_memory_mb / max_heat_points / label_distance`（`V10__digital_twin_production_config.sql:27-30`）——不是写死的常量。③ **按距离裁剪**：标签用 `distanceDisplayCondition` 与 `setLabelDistance`（`cesium/pointLayer.js:117, 220-227`）；热力圈按值取前 N 个（`heatmapLayer.js:138-144`）。<br><br>**仍缺**：① ⚠ **「优先真实地形」未落地**——默认 `mountain` 模式下真实地形被**显式跳过**（`cesium/createViewer.js:6, 70-72`），地形状态条甚至只在 `!LOCAL_SCENE_ENABLED` 时才渲染（`views/ScreenView.vue:576`）。这与第 37 条是同一个结构性事实的两面：**当前默认配置下没有真实地形可谈**。② **「大场景分块」没有实物**——仓库内只有 320m×240m 的演示 GLB（`ASSET_PROVENANCE.md`：38,400 三角形），**没有任何 3D Tiles / `tileset.json` / DEM 资产**（`find -name "*.glb" -o -name "tileset.json"` 只有那一个 GLB 及其 dist 副本）。**分块能力要等项目方提供 3D Tiles 资产才会真正被走到**——代码路径在、资产不在，所以这条能力至今**从未在真实数据上跑过**。<br><br>**旁证**：`docs/3D数字孪生_生产数据准备与上线实施指南.md:82` 把「分块、LOD、3D Tiles……」列为 **P2 建议项**——清单把它归在「功能补齐」，文档归在 P2，两处口径不同，此处按清单的编号留在这里。<br><br>**结论**：`功能补齐` → `部分完成`。**注意这一条的「部分完成」性质特殊**：缺的不是代码，是**资产与开关**——一旦有了 3D Tiles 并把 `coordinateMode` 切到真实地形，已写的机制就能用上；在那之前它无法被验收。 |

## 五、安全、媒体和运维

| 编号 | 状态 | 需要完善的内容 |
|---|---|---|
| 41 | 功能补齐 | **凭据和访问控制**：HTTPS、接入来源限制、登录限流、凭据轮换；多网关接入时逐步改成独立凭据和设备范围授权。 |
| 42 | **部分完成**（2026-09-17 核查；**鉴权面收窄了，日志脱敏与短期票据都没做**） | **检查 URL 中的令牌泄露**：SSE 和图片请求使用查询参数传 JWT，需处理代理日志脱敏，并评估短期访问票据等方式。**已做**：① **把 query token 的适用范围收窄到只剩两条路径**——`backend/src/main/java/com/monitor/auth/security/JwtAuthFilter.java:52-54` 只对 `/api/v1/stream` 与 `/api/v1/media/*/content` 接受 `request.getParameter("token")`（`:122`），其余端点一律不认 URL 里的令牌。这是本条最要紧的一步：**「用查询参数传 JWT」这件事没有被消灭，但它的暴露面从「任何端点」缩到「两个必须靠 URL 的场景」**（SSE 与 `<img src>` 都没法带自定义头，这是浏览器限制，不是选择）。② 前端两处拼装点：`frontend/src/api/monitor.js:211`（SSE）与 `:271`（图片）。③ **后端自身的请求日志不落 token**——`common/filter/RequestLogFilter.java:34` 只记 `req.getRequestURI()`，**不含 query string**。<br><br>**仍缺**：① ⚠ **代理访问日志仍是敞开的**——`frontend/nginx.conf` 全文 83 行**没有 `access_log` 指令**，于是走 nginx 默认的 combined 格式，其中 `$request` **包含 query string**，`?token=` 会原样进 nginx 访问日志。全仓 grep `脱敏\|sanitiz\|redact\|access_log` **只命中本清单自己**（`:91`）——即这条缺口**没有任何实现**。这是本条当前最实在的风险：后端那条路堵上了，代理那条路没堵。② **短期票据机制完全没有**：grep `ticket\|短期票据\|oneTime\|one-time`（backend／frontend／docs）**命中 0**，清单说的「评估」也没留下评估记录。<br><br>**结论**：`已确认` → `部分完成`。**下一步最省事的一步是 nginx 那条**（加 `access_log` 并用 `map` 把 `token` 参数打码，改 `frontend/nginx.conf` 一处即可）；短期票据是设计题，需要与第 41 条（凭据轮换）一起定。 |
| 43 | 已确认 | **模型文件访问权限**：当前 `/models/` 是静态访问路径；模型若包含客户现场资料，需要与项目权限匹配。 |
| 44 | 已确认 | **媒体上传完整性**：目前主要依据 MIME/扩展名判断图片；应验证真实内容、尺寸，并处理文件落盘成功但数据库写入失败的孤立文件。 |
| 45 | 已确认 | **健康检查更真实**：当前健康接口直接返回 UP；增加数据库可用性等就绪检查，同时与进程存活检查区分。 |
| 46 | 功能补齐 | **监控与告警**：监控接收成功率、拒收原因、端到端延迟、SSE 连接数、磁盘、数据库容量及备份失败。 |
| 47 | **部分完成**（2026-09-17 核查；**清单点名的四类里只覆盖了两类，且「独立环境」这半没做到**） | **完整备份与恢复**：除数据库外，还包括媒体、模型、配置和转换器队列；在独立环境实际恢复验证。**已做**：① **数据库 + 媒体**——`tools/backup/pg-backup.sh:46` 是 `pg_dump`，`:53-70` 的 `--with-media` 把附件卷打成 tgz；`tools/backup/pg-restore.sh` 同样支持 `--with-media`。② **有一条真做的实测链**——`tools/backup/selftest.sh`：`:73-84` 备份 → `:86-104` 破坏（`:102` **真的删卷内文件**）→ `:106-113` 恢复 → `:115-133` **逐样查回**（连 PNG 魔数都验）。这是本条最有价值的部分：**不是「有脚本」，是「脚本被跑过、且验的是内容不是计数」**。③ 生产 compose 版另有 `tools/operations/backup_postgres.sh` / `restore_postgres.sh`（仅 DB）。<br><br>**仍缺**：① **模型**——`docker-compose.yml:114`／`docker-compose.production.yml:80` 里模型是**只读绑定卷**，没有任何备份脚本（模型若含客户现场资料，这一条还叠着第 43 条的权限问题）。② **配置**——`.env.production` **无任何备份**。③ **转换器队列**——**尚无实现**，`docs/3D数字孪生_生产候选部署与验收.md:338` 自己把它列为现场待完成；队列没做，备份自然无从谈起。④ ⚠ **「在独立环境实际恢复验证」这半没做到**——`tools/backup/selftest.sh:19` 自己写着「单独跑」（意思是「不并进 `run-all.sh`」），但它**仍然跑在同一个 compose 库上**，不是独立环境。这与第 51 条是同一个缺口的两面。<br><br>**一处文档过期（顺带发现）**：`docs/3D数字孪生_生产候选部署与验收.md:355` 仍称「媒体卷……需要使用主机/存储平台的快照另行保护」，而媒体卷**已经**由 `pg-backup.sh --with-media` 覆盖了——这正是第 55 条要清理的那类「已完成描述过期」。<br><br>**结论**：`功能补齐` → `部分完成`。**下一步**：模型卷与 `.env.production` 的备份（都是加一条 tar/复制，成本低）；「独立环境验证」需要另一套 compose，成本高，建议与第 51 条合并成一条「在干净环境跑一遍」的任务。 |
| 48 | 功能补齐 | **容量与保留策略**：制定分区、归档、降采样和清理机制；禁止历史数据无限增长。 |
| 49 | **部分完成**（**2026-09-16 补一条实测**；**2026-09-17 由「功能补齐」改判**，理由见行末核查） | **部署与升级规范**：部署前检查端口、配置、目录权限和空间；保留原生产配置，建立版本与迁移回退说明。<br><br>**回退这一半现在有一条硬结论：V14 之后不能退回旧后端镜像。** 实测旧镜像（jar 里只有 V1～V9）**能正常启动**并连上 V14 的库——Flyway 把 V10～V14 判为 Future 按默认 `*:future` 忽略，只留一条 WARN，所以**没有任何东西会拦住回退**。但旧代码的 `Alarm` 实体里没有 `open_key`（旧 jar 里 grep 命中数为 0）：它关闭警情走 `updateById`（跳过 null 字段）写不掉键位，新开警情时 INSERT 里也没有这一列。前者把键位永久留给一条早已解除的警情，回到新版本后该测点该测项再想开警情就撞 `uk_alarm_open_key`（实测 `23505`），而 `AlarmEngine.evaluate` 是 `catch (Exception e) { log.error }` ——**接入照常 200、测值照常落库，只是这条测项从此不再报警**；后者让唯一索引在回退窗口内对旧代码开的警情完全不设防。判据、症状与应急解封 SQL 见 `docs/3D数字孪生_生产候选部署与验收.md` §3.5。**仍缺**：部署前检查脚本、镜像 tag 固定、V10～V16 的 down 迁移。（**「确实回退过」时的重新打键去重脚本这一条已不再缺**：迁移 **V16** 就是它——幂等，跑第二次时五步全是空操作；当时那句「不能直接对全表 `UPDATE`，键位撞车会失败」判断是对的，V16 正是照它做的：先补 `metric_code`、再释放终态行上的僵尸键位、再按形状收敛重复，**最后**才打键，五步顺序承重（迁移是原子的，撞一次 `23505` 整个迁移失败、应用起不来）。见 `docs/3D数字孪生_生产候选部署与验收.md` §3.6。）<br><br>**2026-09-17 核查（一半有、一半没有，状态由「功能补齐」改为「部分完成」）**：**已有的那半——「版本与迁移回退说明」是实的**。上面引的两条硬结论**逐条核对为真**：`docs/3D数字孪生_生产候选部署与验收.md:111` 的 §3.5（「V14 之后**不要**回退到旧后端镜像」）与 `:173` 的 §3.6（「V16 会自动修好回滚窗口留下的键位问题」）**章节都真实存在**，两个迁移文件也都在（`backend/src/main/resources/db/migration/V14__alarm_open_key.sql`、`V16__backfill_alarm_open_key.sql`），部署顺序写在同文件 `:38-51` 的 §3。**这一半是本条最有价值的部分，且它写成的是「为什么不能回退」而不是「记得别回退」。**<br><br>**仍缺（三项，都在「部署前」这一侧）**：① **没有部署前检查脚本**——`tools/`（acceptance／backup／operations／…）全无 precheck/preflight，全仓 `grep "df -h\|端口占用\|目录权限\|磁盘空间"` **只命中本清单第 49 行自己**与 `tools/acceptance/README.md:80` 的一句注释。清单点名的四样（端口、配置、目录权限、空间）**一样都没有被脚本检查**。② **镜像 tag 未固定**——`docker-compose.production.yml` 里 backend/frontend **没有 `image:` tag**（全文只有 `:5 image: postgres:16`）。这一条与 §3.5「不要回退」直接相关：**tag 不固定意味着「回退」与「重新部署」用的是同一个未定值**，那条硬结论因此少了一层执行保障。③ **没有 down 迁移**——migration 目录 16 个文件**全是 `V*__*.sql`，无 undo/down**。所以 §3.6 的 V16 是「往前修的脚本」，不是「往回退的脚本」；真正要退回某个版本时**没有回滚路径**。<br><br>**下一步（按性价比排序）**：镜像 tag 固定（改 compose 一处，收益直接落在 §3.5 上）> 部署前检查脚本（把清单那四样写成一段 shell）> down 迁移（成本最高，且与 Flyway 的社区版限制有关，建议单独立项）。 |

容量问题值得提前处理：**1000 点、每 5 秒一次、每次两测项，相当于每天约 3456 万条测量记录。** 短时演示能跑，并不代表当前存储和查询方式适合长期维持这个规模。

## 六、测试与交付

| 编号 | 状态 | 需要完善的内容 |
|---|---|---|
| 50 | **已修复**（2026-09-17，13 套件 / 480 条后端断言 + 前端 67 + 35 条；**逐项对账见下**） | **自动化回归**：覆盖前端时间合并、告警汇总、回放，后端契约、标定、权限、告警和并发重复。保留并更新现有验收脚本。**已做（清单点名的 8 个领域逐一落到具体套件）**：**前端**三块由 `frontend/scripts/selfcheck.mjs` 覆盖——**时间合并**＝§① 实时推送（连接归属/幂等、measurement 并表、SSE 可见性收口），**告警汇总**＝§① 的「同一测点多条未解除警情的汇总」，**回放**＝§② 时间轴（多序列合帧、前值保持、**采样时刻与过期限时**、滑块往返）+ §⑤ 回放取数规模（清单第 18 条：粒度选择、并发池、分段边界）；另有 §③ 测项中立化与项目过滤、§④ 请求代际守卫。**后端**由 `tools/acceptance/` 的 13 个套件覆盖——**契约**＝`02-ingest-idempotency.sh`(43) + `03-query.sh`(27)，**标定**＝`13-calibration.sh`(53)，**权限**＝`01-archive-auth.sh` + `10-scope.sh`(107)，**告警**＝`04-alarm.sh` + `07-device-alarm.sh`(26) + `09-data-quality.sh`(49) + `05-realtime.sh`，**并发重复**＝`11-concurrency.sh`(18，真并发) + `12-ingest-concurrency.sh`(15)，其余 `06-media.sh`(32)／`08-simulator.sh`(26)。**「保留并更新现有验收脚本」这半句也做到了**：本轮第 17/18/19 条给 `selfcheck.mjs` 加了 §④ 与 §⑤ 两节、给 `check-p0-stage1.mjs` 加了项 22–35，「更新而不新建」——没有另起一套平行脚本。<br><br>**证据（本轮复跑的实测数字）**：`npm run selfcheck` **67 条 / 失败 0**（本轮基线 **35**）；`node scripts/check-p0-stage1.mjs` **35 项全部通过**（本轮基线 **25**，**注意：方案当时估的是「≥ 40」，实际落在 35——低 5 项，此处按实数记，不凑数**）；后端 `tools/acceptance/README.md` 的合计行 **13 套件 / 480 条断言**（2026-09-17 由 465 → 480）。`npm run build` 通过（18.76s）。<br><br>**边界与仍缺**：① **两个前端 harness 都加载不了 `.vue`**，所以「组件内的接线」只有源码级绊线、没有行为测试（见第 17 条的边界）——这是本条最大的覆盖缺口，且是**结构性的**（SSR 直调 setup 拿绑定会在 `useRouter()` 上炸），不是没写。② `check-p0-stage1.mjs` **不在 `package.json` 里**，不在 `run-all.sh` 里，也不是 CI 的一部分——它靠人记得跑；要真当回归网用，得先接进 `run-all.sh` 或加一条 npm script。③ 第 51／52／53 条要的**端到端链路**、**权限全矩阵**、**故障与长稳**仍空着，本条交付的是「每个领域的单元/契约/并发断言齐全」，**不等于**那三条。④ 验收套件**一次只能跑一份**（并排跑会互相杀后端，症状是 HTTP 000 的假红）——这是使用约定，不在脚本里强制。 |
| 51 | **部分完成**（2026-09-17 核查；**每一环都有人测，但没有一条脚本把 9 环串起来**） | **完整端到端测试**：建项目→建档→标定→上报→显示→告警→处置→历史查询→备份恢复。**已做（逐环对账，9 环里 8 环有覆盖）**：**建项目**——**只作为夹具**（`03-query.sh:85,122`、`07-device-alarm.sh:23`、`09-data-quality.sh:126` 都用 `POST /projects` 造前置数据），**没有针对建项目本身的断言**；**建档**——`01-archive-auth.sh:38-50` ④「新建测点立即可见」；**标定**——`13-calibration.sh`（`:111` 标定→`ACTIVE` 起，全篇 11 节）；**上报**——`02-ingest-idempotency.sh`、`08-simulator.sh`、`12-ingest-concurrency.sh`；**显示**——**只有后端数据面**（`03-query.sh` 的 latest/series + `05-realtime.sh` 的 SSE）；**告警**——`04-alarm.sh`、`07-device-alarm.sh`、`09-data-quality.sh`、`11-concurrency.sh`；**处置**——`04-alarm.sh:47` ③ 的 confirm→dispatch→research→resolve；**历史查询**——`03-query.sh` 全篇。<br><br>**仍缺（这条「端到端」的本体）**：① ⚠ **没有任何一条脚本把 9 环串起来走一遍**——13 个套件**各验一段**，每一段都从自己的夹具起步。「上一环的输出是下一环的输入」这个语义**从未被整体验证过**，而端到端测试要的正是它（各段全绿但接缝处错位，是这类测试专门要抓的）。② **备份恢复这一环在体系上就是断的**——它**不在** `tools/acceptance/run-all.sh:128` 的 `SUITES` 数组里，而是独立在 `tools/backup/selftest.sh`，且该脚本 `:19` 自己写明「**不并进 `run-all.sh`**」。所以这一环与前面 8 环**连执行的入口都不在一起**。③ **「显示」这一环没有前端渲染验收**——13 个套件**全是 curl 打 API**，没有任何一条验「页面上真的画出来了」。这与第 50 条的边界是同一个结构性缺口（两个前端 harness 都不加载 `.vue`）。④ **建项目没有独立断言**（只当夹具，不验它自己的返回与约束）。<br><br>**结论**：`待验证` → `部分完成`。注意这条的「部分完成」和别条不同：**不是能力做了一半，而是「零件全有、总装没做」**。第 50 条交付的是零件清单（13 套件／480 条），**本条的答案是一条串起 9 环的脚本**，且它需要能操作前端——这是它与第 50 条的分界线。 |
| 52 | **部分完成**（2026-09-17 核查；**6 个子项里 3 项有、1 项部分、2 项没有**） | **权限回归**：不同角色、无项目成员、跨项目 ID、共享设备、静态模型和 SSE 都要测试。**逐子项对账**（两个套件的分工不同，见下）：<br>① **不同角色**——**部分**。`01-archive-auth.sh` **基本不承担权限回归**（只用 admin，`:19,53-56`）；`10-scope.sh` 覆盖 admin／operator（李敏）／`outsider`（`:120,137,185`）；analyst 与 maintainer 两个角色落在**别的套件**里（`04-alarm.sh:169-187`、`13-calibration.sh:171-204`）。**四个角色都测到了，但没有一处把四个角色的权限矩阵并列验一遍**。<br>② **无项目成员**——**有**：`10-scope.sh:185-207` ⑤ 的 `outsider` 对照组**十项全 0 + 直达 403**（这是本清单第 05 条那个「不能删的对照组」的用途所在）。<br>③ **跨项目 ID**——**有**：`10-scope.sh:147-158` ③ 直达不可见资源一律 403。<br>④ **共享设备**——**部分**：`:272-311` ⑧ 验了「无归属设备只对 ADMIN 可见」「绑到项目 2 后对项目 1 仍不可见」。**未测**的是「同一台设备**同时**绑两个项目的测点」这个真共享形态——而第 27 条（设备数据来源管理）要解决的正是它。<br>⑤ **静态模型**——**完全没有**：全 acceptance 目录 `grep "/models"` **命中 0**。`/models/` 由 `frontend/nginx.conf:31-36` **静态服务、无鉴权**，所以这一项不是「没测」，而是**它根本没有权限可测**——这正是第 43 条（模型文件访问权限）登记的问题。**两项要连起来读**。<br>⑥ **SSE**——**有**：`10-scope.sh:338-432` ⑪「SSE 推送也按订阅者过滤」，三条真实订阅 + 正反两侧。<br><br>**仍缺的实质**：**「共享设备」缺跨项目共享形态**、**「静态模型」完全无覆盖（且当前无鉴权）**、**四个角色的矩阵没有并列验**。`01-archive-auth.sh` 名为 auth 却几乎不承担权限回归这件事值得记一笔——**别以为有它就等于权限测过了**。<br><br>**结论**：`待验证` → `部分完成`。**下一步**：静态模型那一项要**先做第 43 条**（给它加鉴权）才谈得上回归；共享设备那一项建议与第 27 条合并。 |
| 53 | 待验证 | **故障与长稳测试**：断网、后端重启、数据库不可用、磁盘不足、响应丢失、浏览器长时间运行。 |
| 54 | **已修复**（2026-09-17，源码级取证；**一处文档勘误见下**） | **构建和测试分开记录**：后端镜像构建跳过测试，需要独立执行测试；前端 build 成功不能代替页面功能验收。**已做**：两侧都写成文了。① **后端**：`backend/Dockerfile:38-45` 跳过单测，并**刻意用 `-Dmaven.test.skip=true` 而不是 `-DskipTests`**——注释逐字给了理由：后者**仍然编译 `src/test`**（「仓库现在有 4 个测试类了，早先那句『仓库当前没有 src/test』已过期」），于是 `go-offline` 与 `package` 都得把 JUnit/Mockito 这些 test 依赖也拉进来，而这个镜像里根本用不到它们（jar 不会包含测试类）；结论句是「**真正的闸门是 `tools/acceptance/run-all.sh`**（它对容器跑同样能过）……**镜像构建不该承担测试职责**」。② **成文记录**：`docs/3D数字孪生_生产数据准备与上线实施指南.md:37` 的表格里有一行「Docker 镜像构建｜后端 Dockerfile 使用 `-DskipTests`；**镜像构建成功不能代替单元测试**」——**两半句都对上了清单的要求**（构建跳过测试＋构建成功≠测试通过）。③ **前端**那半句（build 成功不能代替页面功能验收）由 `frontend/README.md` 的 `selfcheck` 一节承担：它明说 `selfcheck.mjs` 是「用 Vite 的 SSR 加载器把 store 与纯函数**真跑一遍**（不是读代码）」，与 `npm run build` 是两件事。<br><br>**证据**：源码级取证（`backend/Dockerfile:38-45`、`docs/3D数字孪生_生产数据准备与上线实施指南.md:37`、`frontend/README.md`）。本轮 `npm run build` 实跑通过（18.76s）——**这正是本条要防的那个混淆：build 绿了不代表第 17/18/19 条生效**，所以那三条的结论全部落在 `selfcheck`／`check-p0-stage1`／人工验证上，没有一条拿 build 当证据。<br><br>**勘误（文档与实际命令不一致）**：`docs/3D数字孪生_生产数据准备与上线实施指南.md:37` 写的是「使用 **`-DskipTests`**」，而 `backend/Dockerfile:45` 实际用的是 **`-Dmaven.test.skip=true`**。**原理（构建跳过测试）没写错，命令名写错了**，而这两个参数的差别恰好是 Dockerfile 注释花了五行解释的那件事（编译不编译 `src/test`）——所以这处不一致值得记一笔。**本轮不改那份文档**：它是交付期的快照类材料，且改它需要连带核对同一表格里的其它行；留给第 55 条（交付材料统一）一起做，此处只登记。<br><br>**边界与仍缺**：① 「独立执行测试」目前**靠成文约定与人工**，没有强制——`run-all.sh` 不跑单测（后端 4 个测试类：`RadarCoveragePolicyTest`／`CalibrationServiceTest`／`IngestTimePolicyTest`／`IngestServiceDuplicateTest` 等，都是**手动 `./mvnw test`**）。镜像构建与单测之间**没有任何自动连接**，忘了跑就没有任何东西会提醒。② 第 55 条要求的「全套交付材料一致」远未达成（端口 8080 vs 8088 的不一致已在第 21 条登记）。 |
| 55 | **部分完成**（2026-09-17 核查；**清单点名的 5 类材料里 3 类有、2 类没有，且有 3 处已知矛盾**） | **交付材料统一**：部署说明、数据字典、接口契约、初始化流程和测试报告保持一致，清理过期端口、命令和"已完成"描述。**已有的 3 类**：**部署说明**——`docs/3D数字孪生_生产候选部署与验收.md`、`docs/3D数字孪生_生产数据准备与上线实施指南.md`；**接口契约**——`docs/B侧接口契约_M0.md`、`docs/message-contract.md`、`docs/M0_接口冻结_致B_v1.md`；**测试数据**——`README.md:69` 与 `tools/acceptance/README.md:109`（480 条断言）。<br><br>**仍缺的 2 类**：① **「数据字典」没有文档本体**——它只作为交付物的**名字**出现在 `docs/3D数字孪生_生产数据准备与上线实施指南.md:357`（「各方确认的数据字典与映射版本」），**没有实际文档**。② **「初始化流程」与「测试报告」都没有独立文档**（全仓无对应标题）。<br><br>**已知的 3 处矛盾（「清理过期描述」这一半的具体内容，逐条登记）**：① **端口 8080 vs 8088**——`README.md:58-59,67` 写 8080（本地 Maven／容器后端），`.env.production:17` 的 `FRONTEND_PORT=8088` 与 `docs/3D数字孪生_生产数据准备与上线实施指南.md:406` 写 8088（生产反代前端）。**两处语境确实不同**（一个是后端直连、一个是前端反代），但**没有任何一处并列说明这件事**，读者只会看到两个数字。② **前端写死的 8080**——`frontend/src/api/http.js:60` 仍写死「连不上后端（`http://localhost:8080`）」，`frontend/src/views/LoginView.vue:36` 同样写死 `http://localhost:8080`，**与 8088 反代部署矛盾**。**清单第 21 条所述成立**（本次核查再次确认，两处命中），两处是同一个问题的两面。③ **`-DskipTests` vs `-Dmaven.test.skip=true`**——`docs/3D数字孪生_生产数据准备与上线实施指南.md:37` 写前者，`backend/Dockerfile:45` 用后者（`Dockerfile:38-42` 逐字解释了二者差别）。第 54 行已自标「留给第 55 条一起做」，**此处正式登记**。<br><br>**另有一处过期描述（本次核查新发现）**：`docs/3D数字孪生_生产候选部署与验收.md:355` 仍称「媒体卷……需要使用主机/存储平台的快照另行保护」，而媒体卷**已经**由 `tools/backup/pg-backup.sh --with-media` 覆盖（第 47 条）。**这是典型的「已完成描述」没跟上代码**，正是本条要清理的那一类。<br><br>**结论**：`功能补齐` → `部分完成`。**下一步（按成本排序）**：三处矛盾中①②是**改文案**（`http.js:60` 那处还可以顺手接 `VITE_BACKEND_ORIGIN`，`LoginView.vue:36` 已经在用这个环境变量了——**同一份配置两处写法不一致**，这是第 21 条的具体修法）；③与媒体卷那句是**改一处字面量**；「数据字典」缺的是**文档本体**，成本最高，需要有人把库表整理出来。 |

## 七、建议实施顺序

1. **先处理 P0**：页面可用、数据不回退、告警正确、身份可信。
2. **补管理能力**：账号、项目成员、雷达参数、模型配置、标定有效期。
3. **补生产数据流程**：心跳、补报、基准变更、来源管理及转换器接口。
4. **做容量、性能、备份恢复和真实数据验收。**
5. **最后提升模型和界面细节。**

目前没有必要立即拆成微服务或引入复杂集群；先把现有单体系统的正确性和运行能力做扎实。
