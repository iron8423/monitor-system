# A 后续阶段工作清单（阶段 1 收尾 → 阶段 2）

> 日期：2026-09-09 · 作者：A
> 依据：《双人分工实施方案v2》§6/§7/§8（A0–A5、阶段 1/阶段 2、里程碑 M0–M5）
> **更新（当日·协作模式定案）**：以 **B 的 GitHub 仓库 `iron8423/monitor-system`（私有，main）为唯一共享事实源**，A 侧全部历史/内容并入并上传；后续两人同步一律看仓库状态，不再 md 寄件。A 上传账号 = **`yanzu1024`**（待 B 加 collaborator），本机 SSH ed25519 密钥已生成待注册；合入方向 = 保留双方 commit、`git merge origin/main --allow-unrelated-histories`。阻塞：collaborator + 公钥注册（详见当日 A 日志晚段 6）。
> 范围：只列 **A（底座）** 的后续动作；B 侧仅在与 A 的衔接点标注。勾选状态随进展更新。

---

## 0. 一句话状态

A 侧**阶段 1 交付已完成并通过端到端验证**（A0–A4 + M0 的 schema/种子/契约起草 + M0 对表 D3–D8 落地），
git 已含首个提交 `221d96b`（A 交付基线，B 可拉分支协同）。
**尚未到「阶段 1 整体完成」**：M0 契约差 B 签字——B 已答**全部提问**，且《雷达标准消息契约_v1》原文**已进仓**（`docs/message-contract.md`，2026-09-09 随本轮提交）→ **A-3（V2 每点 2 测项 + 默认规则 ±3mm 双向）内容就绪、待 A 执行**（用户决策：本轮只提交文档，metric 重构后置）；~~共同分支/remote 线上推送路径仍待与 B 商定~~ → **已定（2026-09-09）：B 的 GitHub `iron8423/monitor-system` 单一事实源，A 并入上传；阻塞 = collaborator（yanzu1024）+ SSH 公钥注册**；且后端闭环验收（M2：超限→告警→处置→解除）依赖 B 的 ingest/模拟器/告警模块。

> **更新（2026-09-10 晚）**：B 转前端后其 M1/M2 后端部分由 A 就地补齐，**后端闭环已在 A 侧跑通**——验收套件扩到 **7 套件 / 156 条断言**且全绿（`--fresh` 空库 + 同库连跑三轮）。本轮定案并落地了 §2 的 **B-8～B-12** 五项（档案时间全局带时区、`latest` 次排序键、设备告警、等级升级、collectTime 非法拒收）。**验收第 5 条（设备告警）与第 3 条（等级升级）自此不再是缺口**；仍未覆盖的是第 8 条（Docker/PG，B-3/B-4）与各条的前端呈现。
>
> **更新（2026-09-10 夜）**：**验收链的第一环补上了**（A-8）。此前 01–07 全部用 curl 合成报文，等于绕过链子的起点——后半段验得很扎实，「模拟器」这一环却从没被跑过（`radar_csv_replay` 是回放器不是生成器）。新增 `tools/radar_simulator/` + `tools/acceptance/08-simulator.sh`，套件升至 **8 套件 / 173 条断言**（空库连跑两次 173/173）。**里程碑 §3 里 M1 的判据「模拟器 → 落库 → `latest/series`」至此才算真的成立**（此前只是「等价构造过」）。仍在缺口里的：~~② M0 契约的 B 正式签字~~（**已取消，见下条更新**）；③ 低电量/数据中断告警（**用户定案：暂不做**，见下）。
>
> **更新（2026-09-10 深夜）**：本机装好 Docker，**B-4（PG 迁移验证）已验完**——`postgres:16` 上 V1–V4 全部迁移成功、**173 条断言 173/173**、重启后端数据不丢。「本机无 Docker/PG」这条老卡点至此注销。**M0 签字流程已取消（用户定案）**：项目已用**单仓库（monorepo）单一事实源**——双方读同一份文档、同一份代码，git 历史本身就是「谁在何时同意了什么」。签字这个动作的全部价值是「在没有共享事实源时留一条双方同意的记录」，单仓之后它成了没有信息量的一层仪式。<br>**M0 的收口方式因此改为：仓库文档与代码保持一致**，不再走签字。落地三件事：① `M0_接口冻结_致B_v1.md` 就地作废（编号 D1–D10 仍以它为定义处，但表内数值以现行文档/代码为准，三处不符已逐条标注）；② `message-contract.md` 补上缺失的 `collectTime` 拒收规则；③ 现行事实源 = `B侧接口契约_M0.md` + `message-contract.md`。
>
> **更新（2026-09-10 深夜·二）**：**B-3 已完成**——`docker-compose.yml` + `backend/Dockerfile` + `.env.example` 落仓，`docker compose up -d` 起 db + backend，双双 healthy；**对容器跑验收 173/173**（`run-all.sh` 只认 `BASE`，一行未改）；`down` 再 `up` 后容器确系重建而数据不丢（计数 `49\|9\|16`、schema `v4` 不变，Flyway `Migrating` 0 次）。**§9 第 8 条的两句「一键」与「数据持久」都已实测通过**。至此 A5 收口，阶段 1/2 里 A 侧**只剩「前端呈现」这一半**（`frontend/` 仍是空的，等阶段 2 动工）。
>
> **更新（2026-09-10 深夜·三）**：**B-7 已完成**。它本来就该紧跟 B-3——跑 `postgres` profile 的不再是我临时起的进程，而是 `docker compose up` 出来的**部署产物**，洞随 artifact 一起交付了。修法：`application-postgres.yml` 显式关掉 H2 console，且 `SecurityConfig` 的放行**改为跟着这个开关走**（漏洞形态正是「开关与放行各写一份」）；另把 `frameOptions` 从 `disable()` 收成 `sameOrigin()`。实测 `/h2-console/*` 三个路径全部 **200 → 401**，对照面（health/swagger/未鉴权接口）行为不变，dev profile 下控制台仍可用，两个 profile 各 173/173。**A 侧阶段 1/2 的待办至此清空**，只剩前端。
>
> **更新（2026-09-11）**：**前端不再只有骨架**。当日发现 A、B **各自独立写了前端**（技术栈选型完全一致、6 个同名文件全部不同），用户定案**以 B 的阶段 1 骨架为基线**，A 把总览 / 测点与曲线 / 设备状态 / 告警中心 / 管理端五个页面适配过去，**只保留一条前端线**（A 那版的重复实现已丢弃）。主题跟 B 走亮色。详见 `docs/daily/2026-09-11-A.md` §53。<br>**同轮修掉两个后端缺陷**（真机验证时撞出来的，与选哪版前端无关）：<br>① **`/devices` 与 `/projects/{id}/summary` 对同一台设备给出相反结论**——`device.status` 这列的语义是「人工显式标注」（只有 `FAULT` 有意义），但种子里写的是 `ONLINE`，而基类的通用列表直接吐实体，于是列表说「在线」、`/devices/{id}/status` 与概览说「离线」（后两者按 `lastReportTime` 现算）。前端无法调和（要么把判据抄一遍、要么 N+1），故修在列表端点：`DeviceController` 覆盖 `list()`/`get()`，返回前换成 `DeviceStatusPolicy.statusOf(d)`（只改返回对象、不回写）。修后四处一致；验收 **173/173** 未受影响。<br>② **验收链第一环在 PG profile 上从未走过**——PG 库里 `measurement` 的 98 行全是验收套件的一次性测点残留，**种子测点 `P-HK%` 的行数为 0**（模拟器昨天灌的数据进了 H2，随进程没了）。对 compose 后端补跑模拟器（48 轮 × 30 分钟 × `--backdate-days 1`，`accepted=672`）后，`/points/1/series` 由 0 点变 **47 点**、总览「最大形变」由 — 变 **−1.2224mm**。<br>**此外注销「本机没有浏览器」这个卡点**：本机有 `google-chrome`，已用 `puppeteer-core` 驱动真实浏览器把五个页面点过一遍（守卫/登录回跳/曲线渲染/SSE/无控制台报错），工具装在仓库外。

> **更新（2026-09-11·二）**：**B 并入阶段 3a 3D 大屏**（`ddd3103`，Cesium）——`/screen` 是**顶层路由**、不套 `AppLayout`。A 侧本轮做两件事：<br>① **确认 SSE 归属**：B 提出现连接只有顶栏标签在用、没有页面订阅，计划 3b 收口到 Pinia store。**归 B**（`stores/monitor.js` 已是他的单一数据源，注释里写明「后续 SSE 实时增量也只 patch 这一份」；大屏才是主要消费者）。A 顺带发现一个坑并回给他：`/screen` 不在 `AppLayout` 之下，若连接的**发起**仍留在 `AppLayout.onMounted`，大屏永远连不上——发起必须放在比 AppLayout 和 ScreenView 都活得久的地方。**A 不碰 `AppLayout.vue` / `stores/monitor.js` / `api/monitor.js` 的 stream 部分**。<br>② **修 B 报的口径分叉（缺陷三）**：同测点同 `collect_time` 多行时（幂等键是 `device+message`，不含 collect_time，这是合法数据），`/points/{id}/latest` 与 `/projects/{id}/summary` 取到不同行。根因是「取最新一行」的排序**各写了一份**——`MeasurementQueryService#latest` 有 `id DESC` 兜底、`ProjectSummaryService#maxDeformation` 没有；而 `02-ingest-idempotency.sh` ⑪ 早在 `d2288ef` 就断言过这个次排序键，当时只修了被报告的端点、**兄弟查询漏掉了**。修法：把判据收进 `MeasurementMapper#latestRowOf`，两处共用（只补一句 `orderByDesc(id)` 也能修好当下，但那正是缺陷的成因）。**该缺陷在 compose 的 PG 上不复现**——`EXPLAIN` 显示走索引反向扫，同键下恰好先给到高 id；关掉索引逼成全表扫立刻分叉，H2 上一跑就红。回归断言落在 `03-query.sh` ⑨，套件升至 **8 套件 / 175 条断言**。<br>**一条方法论**：「在部署形态上没问题」不等于没问题，可能只是那个库的执行计划恰好蒙对了。

> **更新（2026-09-11·三）**：**前端构建产物进 compose，验收第 8 条闭合**。`frontend/Dockerfile`（node 构建 → nginx 托管）+ `frontend/nginx.conf` + `.dockerignore` 落仓，`docker-compose.yml` 那段注释块换成真服务，`.env.example` 补 `FRONTEND_PORT` / `VITE_CESIUM_ION_TOKEN` / `VITE_TERRAIN_MODE`。三个要点：① **vue-router 是 history 模式**，nginx 必须 `try_files $uri $uri/ /index.html`，否则 `/screen` `/points` 直接刷新落 404；② **SSE 要单独的 location**（前缀匹配里 `/api/v1/stream` 比 `/api/` 长，会赢），配 `proxy_buffering off` + `proxy_read_timeout 3600s`，用 `/api/` 那条会被缓冲、事件要等缓冲满才出；③ **Vite 的 `VITE_*` 是构建期注入**，Cesium ion token 只能走 Docker build arg，改了必须重建镜像。<br>**同轮前端三件事**：<br>① **管理端从只读改为读写**——这是验收第 7 条「平台管理端新建测点/规则 → 业务端无需改代码立即可见」缺的另一半。表格列由**响应数据推导**（写死列会在后端加字段时看不见），表单由声明式的 `TABS[].fields` 驱动（表列是「后端有什么」、表单是「允许人填什么」，后者必须显式，不能让前端把 id/createdAt 也提交上去）；空串统一转 null，一并解决 BigDecimal 解析 400 与「可空外键即不设」两件事。枚举值不是猜的，是从现行库/代码里核出来的。<br>② **修两个真 bug**（都在 B 的文件里）：`ScreenView` 的「退出大屏」`router.push('/overview')` 指向**没有注册的路由**，会落进 catch-all 的 NotFoundView；`stores/monitor.js` 的 `loadSnapshot` 失败时只置 `loading=false` 而不记错误，于是视图里 `!loadedAt → '加载中…'` **永远成立**——请求失败被显示成「还在加载」，一个不会自己结束的假象。<br>③ **修一个只有整页刷新才会撞到的 SSE 缺陷**（详见 `README.md` 里那条）：全局 SSE 原先只在 `onBeforeUnmount` 关闭，而整页卸载不触发它；nginx 非缓冲反代要等**第二次**心跳写失败才察觉客户端已走（第一次写进半关闭 socket 会成功），这段时间那条连接占着浏览器「单源 6 连接」的名额，**连刷 6 次后所有请求排队、界面僵死约 45 秒**。对照实验：拦掉 `/api/v1/stream` → 9 次导航全 4–8ms，不拦 → 第 6 次冻 47 秒；直连 8080 断开能被后端立即回收，**只有过 nginx 才滞后**。修法是 `pagehide` 里显式 `close()`（浏览器当场回收，不等对端）+ `pageshow(persisted)` 补重连（否则从 bfcache 后退回来 SSE 静默失效）。<br>**compose 形态端到端实测**：守卫拦下并回跳、登录回原目标、五个工作台页与 `/screen` 均有真实数据、跨页口径一致、管理端**新建 → 接口核对 → 删除**全通、全程零 4xx/5xx 零控制台报错。

> **更新（2026-09-11·四）**：**「角色 → 处置动作」从只在前端成立，改为后端强制**。起因是用户提问「四个角色登陆后界面都是一模一样的，这应该是不合理的吧」。按产品语义先讨论的原则，本轮前半段只取证，结论是**用户看到的表相没错，但病因不在他指的地方**：<br>① **差异确实只有三处**（四角色真登录实测）：菜单「管理端」仅 ADMIN 可见、直达 `/admin` 非管理员弹回、警情抽屉动作四套不同；**四个主页面（总览/测点/设备/大屏）四角色 100% 相同**，不点进抽屉看不出来。<br>② **真问题是这张表从来没在后端成立过**：`AlarmController.act` 无 `@PreAuthorize`、`AlarmService.act` 无角色分支，`ACTIONS_BY_ROLE` 只决定按钮显不显示。实测改前**以 maintainer 身份 POST `{"action":"confirm"}` 返回 200**，警情 PENDING→CONFIRMED——界面上根本没这个按钮。**前端藏掉入口不是权限**，这条原则这次在告警动作上被坐实。<br>③ **用户定案「先补后端，界面暂不动」**。落地：`AlarmConstants.ROLE_ACTIONS` 成为权威表（配 `canAct` / `actionsOf`，角色缺失或未登记一律 **false，失败即拒绝**，不设任何按管理员放行的回退）；`AlarmService.act` 加 `role` 形参并在服务层校验（越权 `BizException(403)`，不放 `@PreAuthorize`——允许哪个动作要等解析出请求体里的 `action` 才知道，写 SpEL 更绕且不好测）；`AlarmView.vue` 的 `|| ACTIONS_BY_ROLE.ADMIN` **开放回退改为 `|| []`**（同向失败）。<br>④ **顺序是「先认动作名、再认角色」**：不存在的动作仍走 400（契约既有语义，`04-alarm.sh` 早有 `非法动作 explode -> 400` 的断言）。这一条是**被既有断言当场抓住的**——顺序写反时它变成 403，套件直接报红。<br>全矩阵 24/24 落位（每个组合各用一条新鲜警情，避免撞状态机终态），已固化为 `04-alarm.sh` §⑪ 的 13 条断言；**套件升至 8 套件 / 188 断言**，H2（`--fresh` 空库）188/0、compose PG 上 04 套件 46/46。四角色抽屉动作经前端重建后复跑，**与改动前完全一致**（fail-closed 只影响未知角色）。<br>**同轮另修两处**：<br>⑤ **前端容器健康检查从来没通过过**（见第 5 节新增行）。<br>⑥ **顺带撞出第四个后端缺陷**：点号超过 32 字符的测点**建得出来但永远上不了数**（见第 2 节 B-13）。<br>

> **更新（2026-09-11·五）**：**四个角色各自有了落地页**，补上上一轮挂着的「呈现」这一半。`admin`→`/home/admin`（总览迁入）、`operator`→`/home/operator`（待确认队列）、`analyst`→`/home/analyst`（待研判队列 + **该测点形变曲线**）、`maintainer`→`/home/maintainer`（设备异常表 + 待处置队列），另有 `/home/none` 兜底页。**零后端接口改动**。<br>① **队列口径靠状态机推导，不是「分给我的」**：`Alarm` 没有负责人字段、全仓无按当前用户过滤的读端点，所以「我的警情」做不出来；但动作→目标状态的映射是确定的（`AlarmConstants`），于是值班员收 `PENDING`、研判员收 `CONFIRMED`、运维员收 `PROCESSING`。这是四页能各不相同且站得住的唯一依据。<br>② **两个会静默卡死的路由坑**：`/home` 必须用 **`beforeEnter` 而非 `redirect`**（route-level redirect 在全局 `beforeEach` **之前**求值，写成 redirect 会让未登录用户拼出 `login?redirect=/home/none`，**登录后永远落在兜底页**）；兜底页**不能用 `/screen`**（它不套 `AppLayout`，进去布局卸载、没有菜单，而大屏的「退出大屏」又跳回 `/home`，用户困在里面出不来）。四个落地页的 `name`/`meta.roles` 从 `ROLE_HOMES` **单一数组派生**（失配就是守卫级死循环，而 vue-router 的 30 次保护在生产构建下被摇掉，症状是卡死标签页而非报错），实测因此**刻意跑 compose 的生产产物**。<br>③ **顶栏那个「恒假的角色标签」**：病根是四个种子账号显示名**恰好等于角色名**，`roleLabel !== displayName` 恒假。改法是把显示名换成真人名（陈立/李敏/王越/赵安），**标签代码一字未动**；配套把 `createIfAbsent` 改成**存在则校正显示名**，否则已跑起来的库（compose 的 PG 卷）不会变——已在真实旧库上验过（四条日志 + 库内四行确认）。<br>④ **顺带撞出第五个缺陷**：设备状态 `FAULT`（人工标注、优先于推导离线，且 `DeviceAlarmMonitor.isOffline` 对它直接返回 false —— **不发离线告警**，在告警流里是哑的）在 `DeviceView` 的映射表里**没有条目，界面裸出英文串 "FAULT"**。设备状态枚举已收进 `utils/labels.js`（`FAULT` 标红档），两处共用；运维台异常表把 `FAULT` 纳入并按「故障 → 离线 → 低电」排序。<br>**实测**：前端构建 ✓；compose **生产构建**上四角色实测 **45 断言 / 0 失败**（四条 URL 互不相同、四个页面主区块互不相同、顶栏「真人名+角色」、5 个路由边界含「admin 访问 `/home/operator` 弹回且**未死循环**」与「token 在 user 丢 → `home-none` 且菜单还在」）；后端验收回归 **188/188**。<br>⑤ **另报一个未修的缺陷**（见第 2 节 B-14）：`alertCount` 漏算设备告警。
> **更新（2026-09-11·六）**：**B-14 当天修完**，独立提交（不复用落地页那个 diff）。`activeAlarmCount` 改为 `pointId IN (...) OR deviceId IN (...)`，设备侧由 `device_point` 反查（与 `onlineDeviceCount` 合用新抽的 `deviceIdsOf`）。**空集合的坑**：`in(空集合)` 会把条件整条丢掉，所以 `w.or().in(deviceIds)` 只在 `deviceIds` 非空时才挂——否则项目下没挂设备时会退化成「所有设备告警都算」。验收 **188 → 193**：`07-device-alarm.sh` 加 ①-b/②/③ 共 5 条（自带 project→scene→object→point 夹具，并把临时设备**绑到本项目临时测点**——不绑就是「无主设备」，数不到是对的）。**把实现改回旧写法实测会红**（①-b/② 失败、③ 因 0==0 仍过），断言有效。H2 `--fresh` **193/193**、compose PG 上 07 **26/26**、演示库 `/projects/1/summary` 的 `alertCount` **0 → 1**（该条正是 `DEVICE` 类型），原始症状在真实库上消失。

---

## 1. 阶段 1 内 · A 剩余清单（本周）

| # | 事项 | 阻塞 | 说明 |
|---|---|---|---|
| A-1 | **首个 git commit**（A 交付基线） | ✅ 已完成 | `221d96b feat(repo): 初始化通用监测管理系统（A0-A4 + M0 契约）` |
| A-2 | **M0 冻结收口**：发 B《M0_接口冻结_致B_v1》，收 Q1–Q5 答复 | ~~商定共同分支/remote~~ → 等 B 加 collaborator（yanzu1024）+ SSH 公钥注册 | 已发并收到 B 全部提问答复（2026-09-09，含 Q4/Q5 闭合）；剩 Q1 契约原文文件进仓：B 已作为 `message-contract.md` 提交共同分支，本仓无 remote → **已定（2026-09-09）：B 的 GitHub `iron8423/monitor-system` 单一事实源**，等 collaborator + SSH 后 A 执行 merge+push；demo 默认规则阈值决策已定（±3mm 双向，随 A-3 落） |
| A-3 | **D1/D2 收尾**（V2 测项种子/默认规则/契约对齐） | 无（内容就绪，由 A 排期） | **口径已定且原文已进仓**（message-contract.md）：每点 2 测项 `defo_mm`/`rate_mm_d`（无 X/Y/Z）、点号用 A 档案码、默认规则 metric_code→`defo_mm` 且**阈值双向 \|abs\| ±3mm**（gte +3.0/恢复1.0 + lte −3.0/恢复−1.0，均 warning）。message-contract §2 已由 B 契约原文覆盖。**用户决策：本轮不落 metric 重构，后置执行**（届时改 V2 metric 行 + alarm_rule 默认规则 + Metric 注释） |
| A-4 | schema/seed **A 唯一维护**：B 加表/加列需求走 A 统一出 V3 | 无 | 防两人同仓改迁移脚本冲突（§10 风险 1）；M0 冻结后 schema 视为冻结，只进 V3 不回头改 V1/V2（**已在执行**：V3 加设备告警列、V4 补升级档规则，均未回头改 V1/V2） |
| A-5 | **契约接口人**：配合 B 的 ingest 落库拆行、latest/series 返回、alarm_rule 字段答疑 | 随 B 开发节奏 | 只答疑/出 V3，不改已冻结 schema |
| A-6 | （可选）CesiumJS 可行性 spike | 无 | §10 风险 4：B 的 3D 大屏攻坚前置，A 有空档先验「真实地形 + 标点变色 + 点击弹窗」可行性，非必须 |
| A-7 | **验收套件落仓** | ✅ 已完成 | `tools/acceptance/`：**8 套件 / 212 条断言**（2026-09-11：173 → 175 加 `03-query.sh` ⑨；175 → 188 加 `04-alarm.sh` ⑪ 的 13 条角色越权断言；188 → 193 加 `07` 的 5 条 B-14 回归；193 → 200 加 `02` ⑫ 的 7 条 B-13 回归；200 → **201** 加 `04` 的套件卫生断言「跑完不留自建规则」；201 → **212** 加 `06` ⑦/⑧ 的 11 条影像删除断言。逐套件：01→19、02→34、03→25、04→47、05→12、**06→32**、07→26、08→17）+ `run-all.sh --fresh`（另起空库后端）+ README（脚本↔§9 验收条映射）。覆盖 §9 里后端可独立验证的部分。对应 M5 判据「一键过验收脚本」的后端部分。 |
| A-8 | **雷达数据模拟器**（验收链第一环） | ✅ 已完成（2026-09-10） | `tools/radar_simulator/radar_simulator.py`（Python 3 标准库，无第三方依赖）+ README。按 `message-contract.md` **连续造数**，不依赖任何外部数据——`tools/radar_csv_replay/` 是**回放器不是生成器**（要真雷达 CSV，默认还指向 B 的 Windows 桌面），拿不到数据就一步也跑不了，于是链子最上游一直没人跑过、套件全用 curl 合成报文。模拟器补上起点：`--step-minutes` 模拟时钟（形变按速率积分，避免墙钟反推出几千 mm/d 的速率）、`--inject-overlimit/duplicate/suspect/outage` 四种注入。新增 `tools/acceptance/08-simulator.sh`（17 条断言）把它纳入一键验收。 |

> B 侧阶段 1 任务：B0–B5（telemetry/模拟器/quality/alarm/realtime/media）→ 在 M1/M2 联调点与 A 汇合。

## 2. 阶段 2 · A 留后端收尾（§7.1 + A5）

| # | 事项 | 依赖 | 说明 |
|---|---|---|---|
| B-1 | 设备/质量闭环补全 | 阶段 1 与 B 的 health 边界（D4）定稿 | A 保留 `/devices/{id}/status`；`health`(DATA_ABNORMAL) 归属按对表结果 |
| B-2 | 媒体存储对接 | B 实现 `POST /media` | A 提供 `upload.dir`（application.yml 已配）与静态资源访问约定；`/media` 代码归 B |
| B-3 | **A5 · Docker Compose 一键启动** | ~~Docker 环境~~ → ✅ **已完成（2026-09-10）** | backend + postgres +（含前端构建产物）；env 注入 `JWT_SECRET` / `MONITOR_INGEST_KEY` / `PG_*`。<br>**落地**：`docker-compose.yml`（根目录）+ `backend/Dockerfile` + `backend/.dockerignore` + `.env.example`（`.gitignore` 加 `!.env.example`，否则被 `.env.*` 规则吃掉）。<br>**实测**：`docker compose up -d` → db/backend 双双 healthy（后端 45s 内，`start_period: 60s` 之内）；对容器跑 `run-all.sh`（默认就打 8080，未改一行脚本）→ **173/173**；`docker compose down` 再 `up` → 容器 ID 变了（确系重建）、计数 `49\|9\|16` 与 schema `v4` 前后一致、Flyway `Migrating` **0 次**、影像落在 `monitor-system_media` 卷且 owner=uid 10001。<br>**两个刻意取舍**：① `depends_on: db: condition: service_healthy` 而不是 `depends_on: [db]`——PG 容器起来后还有一段 initdb，只等「容器起了」会让后端抢跑、Flyway 直接连不上；② 不用 `container_name`——它是反模式（和项目名冗余、挡住扩容），顺带避开了与 B-4 遗留的 `monitor-pg` 容器重名冲突。<br>**仍未达**：验收第 8 条的「浏览器完整可访问」需要前端构建产物，而 `frontend/` 当时只有 README + `.gitkeep`（阶段 2 才动工），故**不放半成品 nginx 占位**，`docker-compose.yml` 末尾以注释块写明它该长什么样（node 构建 → nginx 托管 + `/api` 反代到 `backend:8080`）。<br>**2026-09-11 已补上**：`frontend/Dockerfile` + `frontend/nginx.conf` + `.dockerignore` 落地，注释块换成真服务，前端随 `docker compose up -d` 一起起（详见上方 2026-09-11·三 那条）。<br>**顺带修**：`README.md` 写的 `http://localhost:8080/swagger-ui` 是错的（实测 404，springdoc 配的是 `/swagger-ui.html`）；`frontend/README.md` 是 **GBK 字节**（`file` 报 ISO-8859，在 UTF-8 仓库里显示为乱码，违反 `.gitattributes` 约定），已转 UTF-8 且内容不变。 |
| B-4 | **PostgreSQL 迁移验证** | ~~A5/B-3~~ → ✅ **已验证（2026-09-10，手工 docker run）** | 切 `application-postgres.yml` 跑通；H2 内存库重启即清 → PG 持久化（对应验收脚本第 8 条「重启数据不丢」）。<br>**结果**：`postgres:16` 上 V1–V4 四个迁移全部 success；**173 条验收断言在 PG 上 173/173 全绿**；重启后端后 `measurement/alarm/monitor_point` 计数原样（49\|10\|17），迁移不重复执行。<br>**顺带扫掉的方言风险**：类型只有 `VARCHAR/BOOLEAN/NUMERIC`；`GENERATED BY DEFAULT AS IDENTITY` 是 SQL:2003（PG 10+）；分页方言是**运行时探测**（`new PaginationInnerInterceptor()` 未写死 `DbType`）；`series` 的 hour/day 分桶在 **Java 里**做而非 SQL 函数；全仓只有一条原生 `@Select`（`COUNT(*)`）。<br>**仍未做的**：`docker compose` 编排本身（B-3）——本次是手工 `docker run` 起的库，compose 文件还没有。 |
| B-5 | README 完善 | 阶段 1 演示可用 | 启动步骤 + 演示账号（admin/operator/analyst/maintainer，123456）+ 触发告警/断连/重复的演示脚本 |
| B-6 | OpenAPI 契约维护 + 联调响应 | 全程 | A 是「契约接口人」，联调优先级高于收尾（§10 风险 2） |
| B-7 | **生产 profile 收窄调试面**（H2 console / swagger） | ~~B-4 PostgreSQL 迁移~~ → ✅ **已完成（2026-09-10）** | `SecurityConfig` 有意放行 `/h2-console/**`、`/swagger-ui/**`、`/v3/api-docs/**`；`spring.h2.console.enabled: true` 写在**基础** `application.yml`。<br>已核实三点，威胁是现实的而非假设：① `H2ConsoleAutoConfiguration` 的生效条件只有 servlet 应用 + classpath 有 `JakartaWebServlet` + 该开关为 true（`javap` 查 3.5.16 的注解），**没有一条与数据源类型有关**；② H2 是 `runtime` scope → 各 profile 的 runtime classpath 上都有；③ `application-postgres.yml` 只覆盖了 datasource，**未**关该开关。<br>后果：切 `postgres` profile 后 console 仍注册，且**无需任何凭证**即可访问、可自行指定任意 JDBC URL（含 PG 库）。<br>落地要求：PG/生产 profile 显式 `spring.h2.console.enabled: false`，console 放行按 profile 收窄到 dev（swagger 视联调需要可保留）。<br>发现于 2026-09-10：借该 console 直写库验证告警引擎兜底时**无凭证即进入**。<br>**2026-09-10 夜补实测**（当时有 postgres profile 的后端在跑）：`GET /h2-console/` → **200**，直接给出 H2 Console 登录页；`GET /h2-console` → 302。此时 `org.postgresql.Driver` 就在 classpath 上（该后端正连着 PG），所以登录页那个「任意 JDBC URL」输入框能填 `jdbc:postgresql://…`。**威胁不是推理，是实测。**<br>**修复（同日）**：① `application-postgres.yml` 显式 `spring.h2.console.enabled: false`；② `SecurityConfig` 的 `/h2-console/**` 放行**改为跟着这个开关走**（`@Value("${spring.h2.console.enabled:false}")`）——B-7 的漏洞形态就是「开关与放行各写一份」，只关一处的话，哪天有人把开关拨回去调试、忘了改回来，就恢复成无凭证可达；③ 顺带把 `headers.frameOptions` 从 `disable()` 改为 `sameOrigin()`——`disable()` 是**全局**关掉点击劫持防护，而它存在的唯一理由（H2 控制台的框架页）只需要同源放行。<br>**修复后实测**（Docker Compose 部署形态，postgres profile）：`/h2-console/`、`/h2-console`、`/h2-console/login.jsp` 全部 **401**（原为 200）；对照面 `health` 200、`swagger-ui.html` 200、未鉴权 `/alarms` 401 不变；dev(H2) profile 下控制台仍 200 可用。两个 profile 各跑一遍验收均 **173/173**。<br>**swagger 有意保留放行**（原口径：联调期前端要读 OpenAPI，它只暴露接口形状）。 |
| B-8 | **档案 CRUD 时间统一带时区** | ✅ 已完成（2026-09-10） | 加 `config/JacksonTimeConfig`：注册 `LocalDateTime` 序列化器统一走 `Times.iso`，把被 `BaseCrudController` 原样返回的实体时间一次修净（8 个 `BaseEntity` 子类及其后新增字段全部覆盖）。同时删掉 `application.yml` 里**误导性**的 `spring.jackson.date-format`——它只作用于 `java.util.Date`，对 JSR-310 无效，正是这条配置让人以为时间格式已经配好了。01 套件加 3 条断言锁住（测点 / 项目 / 设备各一）。 |
| B-9 | `latest` 加次排序键 | ✅ 已完成（2026-09-10） | `orderByDesc(collectTime)` 后补 `orderByDesc(id)`。选 `id` 不选 `receive_time`：主键单调递增、等价于「后写库的覆盖先写的」，且没有 NULL 排序的方言差异（PG 的 DESC 把 NULL 排最前、H2 排最后）。02 套件加断言：同一 `collect_time` 两条时取后写的那条。 |
| B-10 | 验收第 5 条「生成设备告警」 | ✅ 已完成（2026-09-10，用户定案） | **形态：V3 给 `alarm` 加 `alarm_type`/`device_id` 并放开 `point_id NOT NULL`，复用同一张表**——设备告警因此直接走现成的状态机、处置动作、时间线留痕与 SSE，不必复制第二份警情域。**归属：A 做**（`DeviceAlarmMonitor`，`@Scheduled` 扫描，判据复用 `DeviceStatusPolicy.OFFLINE_MINUTES`；从不报数的设备不告警；等级取 `notice`）。07 套件 26 条断言覆盖。 |
| B-11 | 验收第 3 条「等级升高能升级」 | ✅ 已完成（2026-09-10，用户定案） | **语义：同一测点 + 测项未解除的警情至多一条，命中更高等级规则时就地升级**（`level` 抬高 + 时间线追加 `escalate`），不另开平行警情；升级只改等级、不改所属规则，恢复仍按最初触发那条规则判定。另发现**演示前提缺失**：种子原本两条规则都是 `warning`，没有更高等级可升——V4 补 `defo_mm gte +5.0/恢复 +2.0`（alarm）。04 套件 ⑨ 覆盖。 |
| B-12 | `collectTime` 非法兜底取 `now()` | ✅ 已完成（2026-09-10，用户定案） | `IngestService#parseTime` 解析失败静默取 `now()`：既污染数据，又**掩盖设备离线**——坏时间戳被盖上「刚刚收到」的章，而在线判定只认 `last_report_time`，设备一直在报垃圾却永远显示在线。改为 `collectTime` 非法即整条 `REJECTED`；`receiveTime`（平台侧时间）缺省才取当前时间。契约 §2 已注明知会 B。 |
| B-13 | **点号长度在两处不一致，33–64 字符的测点永远上不了数** | ✅ **已修（2026-09-14，V5）** | `monitor_point.code` 是 **VARCHAR(64)**（管理端建档照收），但 `measurement.point_code` 只有 **VARCHAR(32)**（上报时冗余写入），于是 33–64 字符的测点**建得出来、列表看得见、永远收不到数据也永远不产生警情**，一上报即 `HTTP 500`（PG 实测 `value too long for type character varying(32)`）。**发现路径**：写角色矩阵脚本时点号起长了（37 字符）撞出来的；验收套件一直没抓到，是因为它用的临时点号都在 32 以内。<br>**修法**（三处配套，缺一不可）：① 加 Flyway **V5** 把 `measurement.point_code` 放宽到 64 —— 已确认该列**没有索引**（`measurement` 上只有 `idx_measurement_point_time(point_id, collect_time)`），所以是纯 DDL、不涉索引重建；② `MonitorPoint.code` 加 `@Size(max=64)`、`BaseCrudController` 的 create/update 加 `@Valid`，把「能建不能收」变成**建的时候就 400**（`GlobalExceptionHandler` 已有 `MethodArgumentNotValidException` → 400 分支）；③ `02-ingest-idempotency.sh` ⑫ 加 7 条回归断言。<br>**验红**：把列宽改回 VARCHAR(32) 复跑 ⑫，3 条断言转红（上报 500）——断言确实有牙。**选 64 而非把 `monitor_point.code` 收到 32**：点号由现场设备/项目编码习惯决定，平台没理由拒绝一个自己允许建出来的点号。<br>**此前标注的理由已失效**：原文写「属 B 的 schema 与契约范围，不宜单方面改」——**2026-09-10 起 A/B 分工已合并**，schema 与契约现由同一人维护，该顾虑不再成立。 |
| B-14 | **`alertCount` 漏算设备告警**（`projectSummary` 的「未解除警情」少一块） | ✅ **已修**（2026-09-11 发现并当天修完，独立提交） | **缺陷**：`ProjectSummaryService.activeAlarmCount` 只判 `.in(Alarm::getPointId, pointIds)`。而 **`DEVICE` 类型告警的 `pointId` 是 `NULL`**（契约 §4：`POINT` 给 `pointId`、`DEVICE` 给 `deviceId`，另一侧为 null），SQL 里 `NULL IN (...)` 不成立 → **设备告警永远不计入「未解除警情」**。判为缺陷而非设计：`ProjectSummaryVO` 自己的注释写的是「未解除警情数（status 不属于 `RESOLVED`/`FALSE_ALARM`）」，**没有排除设备告警**；契约只给了字段名、没给排除语义。即实现没跟上它自己的定义。<br>**发现路径**：运维台复验时看到值班台 KPI「未解除警情」为 **0**，而正下方队列里排着 16 条 `PENDING`。其中 15 条指向**已回收的模拟器临时测点**（演示库历史数据，不计入是对的），但那条 `radar-001` 的**设备离线告警**不该被漏掉。<br>**改法**：抽出 `deviceIdsOf(pointIds)`（由 `device_point` 按测点反查，`onlineDeviceCount` 一并改用它，消掉一份重复），条件改为 `pointId IN (...) OR deviceId IN (...)`。**关键坑**：空集合下 `MyBatis-Plus` 的 `in(空集合)` 会把条件整条丢掉，若在 `and(...)` 里无条件挂 `w.or().in(deviceIds)`，项目下没有挂设备时会退化成「**所有**设备告警都算」——比不加这一支还错，故那一支按 `deviceIds` 非空才挂。<br>**验证**：`07-device-alarm.sh` 新增 ①-b/②/③ 共 5 条断言（项目夹具 + 把临时设备绑到项目临时测点）。**把实现改回旧写法实测会红**（①-b 与 ② 失败、③ 因 0==0 仍过），证明断言有效而非碰巧。H2 `--fresh` **193/193**；compose PostgreSQL 上 07 套件 **26/26**；演示库 `/projects/1/summary` 的 `alertCount` **由 0 变 1**（旧口径数出 0 条、新口径 1 条，且该条正是 `DEVICE` 类型），库里原始症状消失。 |
| B-15 | **影像模块没有删除端点** | ✅ **已修（2026-09-14）** | 用户定案「加 DELETE，软删」。落地：`V6__media_soft_delete.sql` 加 `deleted` 列；`DELETE /api/v1/media/{mediaId}`（ADMIN/MAINTAINER + `@AuditAction`），只标 `deleted=1`、**盘上文件保留**（与 `monitor_point`/`device` 同口径，可审计可恢复；代价是卷只增不减，回收磁盘另配离线策略）；重复删除 404 而非静默成功；契约 §7 补文档。前端加 `deletable` 开关（仅测点详情与 `/media` 总览页打开，**大屏浮窗刻意不给**）。**两个后果都消解**：套件 ⑧ 自己回收传的图；误传照片运维在界面上就能撤。**证伪过**：给 `MediaService.delete` 注入空实现后 06 套件转红 5 条。另发现并修掉 `--fresh` 不清理本机 `backend/data/media` 导致的累积（实测攒到 60 个文件）。 |

> ~~⚠️ 本机无 Docker/PG（此前确认）→ A5（B-3/B-4）需要一台能跑 Docker 的环境验证，落地时先补环境。~~
> ✅ **已解除（2026-09-10 夜）**：本机装好 Docker（`docker.io 29.1.3` + `docker-compose-v2 2.40.3`），并在 `postgres:16` 上把 B-4 验完（173/173 + 持久化）。
> 装机两个坑记在这里，换机器时照抄：① **别用官方 `get.docker.com` 脚本**——它固定去 `download.docker.com` 取 GPG key，本机被重置（`curl: (35) Recv failure`）；改走已配好的**清华 TUNA** 源装 `docker.io`。② **拉镜像必须配加速器**——`registry-1.docker.io` 直连同样被重置，可用镜像写在 `/etc/docker/daemon.json` 的 `registry-mirrors`。

## 3. 里程碑对表（M0–M5，标 A 的动作与判据）

| 里程碑 | 计划节点 | 内容 | A 的动作 | A 侧判据 |
|---|---|---|---|---|
| M0 契约冻结 | 阶段 1 · 第 0 天 | schema/种子/消息契约/OpenAPI/git 约定 | ✅ 已完成（D3–D8 落地，B 提问 Q1–Q6 全答） | ~~收 B Q1–Q3 → 两人签字~~ → **判据改为「仓库文档与代码一致」**（2026-09-10：单仓单一事实源，签字取消） |
| M1 后端骨架 | 阶段 1 · 第 2 天 | A：登录+档案 CRUD（✅ 已完成）；B：ingest+模拟器+quality（✅ 由 A 就地补齐） | 参与联调 | **模拟器 → 落库 → `latest/series`** ✅ 已达成（A-8；`08-simulator.sh` 断言） |
| M2 后端闭环 | 阶段 1 · 第 5 天 | A：asset/audit（✅ 已完成）+schema 稳定；B：alarm 状态机/realtime/media（✅ 由 A 就地补齐） | 支持 schema/契约 | **超限 → 告警 → 处置 → 解除全链** ✅ 已达成（04/08 套件断言） |
| M3 前端门面 | 阶段 2 · 第 8 天 | 登录+位移分析页 | 供曲线接口联调 | ✅ **前端曲线拉到真实数据**（2026-09-11：浏览器实测 canvas 1100×320、`P-HK01` 47 点）。3D 大屏已于 2026-09-11 由 B 并入（`ddd3103`，阶段 3a） |
| M4 前端闭环 | 阶段 2 · 第 11 天 | 告警中心+管理端；A：设备闭环+部署 | 开始 A5 | 告警处置 ✅（含时间线）；**新建测点/规则生效** ✅ 管理端已可读写（2026-09-11，建→接口核对→删已实测） |
| M5 演示就绪 | 阶段 2 · 第 14 天 | 影像挂点+实时；A：Docker Compose | ✅ A5 已完成（B-3，2026-09-10） | ✅ `docker compose up` 起 **db + backend + frontend** 三个服务，`run-all.sh` 一键过验收脚本（2026-09-14 最新：`--fresh` 空库 **212/212**，连跑两轮均绿）；前端构建产物**已进 compose**，浏览器端到端在同一形态实测通过 |

## 4. 验收脚本 8 条 · A 的参与面

- **第 1/2/3/5 条**（上报→刷新、幂等、超限告警、断连离线）：主体 B（模拟器/规则/quality），A 供 schema、种子、`/devices/{id}/status` 与消息契约。~~B 转前端后此处全部由 A 就地补齐~~ → **已补齐**：模拟器见 A-8（`tools/radar_simulator/`），第 2/3/5 条分别由 02/04/07/08 套件断言。
- **第 4 条**（确认→研判→处置→解除留痕）：B 的状态机 + A 的审计切面已支持动作留痕。
- **第 6 条**（无人机照片挂测点）：B（media），A 供存储目录约定。后端端点（上传/列表/内容，加上 2026-09-14 新增的**删除**共四条）由 `06-media.sh` **32 条**断言覆盖；**前端呈现于 2026-09-14 闭合**——测点详情「影像」页签、`/media` 总览页、3D 大屏点击浮窗三处共用 `MediaGallery`/`MediaUploader`，浏览器实测 18/18（含一次走真实 UI 的上传），证伪「去掉 `?token=`」时 8 条转红。见 `docs/daily/2026-09-14-A.md`。
- **第 7 条**（新建测点/规则 → 业务端可见）：A 档案 CRUD + 规则表结构（D6）已就绪。
- **第 8 条**（docker compose 一键 + 数据持久）：**A（A5）** → ✅ **已落地并闭合（B-3，2026-09-11 补上前端）**。「一键」与「数据持久」两句早前已实测；**「浏览器完整可访问」**这一面于 2026-09-11 补上——前端构建产物进 compose（`frontend/Dockerfile` + `nginx.conf`），`docker compose up -d` 后浏览器直开 <http://localhost>（`FRONTEND_PORT` 可改），SPA 直达路由、`/api` 反代、SSE 长连接均在容器形态实测通过。

## 5. 当前卡点 / 风险

| 卡点 | 影响 | 对策 |
|---|---|---|
| ~~git 无首个提交~~ | ✅ 已提交 | `221d96b`，B 可拉分支协同 |
| ~~M0 未签字~~ ✅ **已解除** | — | 签字流程**取消**（单仓单一事实源，git 历史即记录）；A-3 已完成；remote 已定并持续推送。收口方式改为「仓库文档与代码一致」，落地项见上一行 |
| B 转前端后 A 是后端唯一人 | 联调没人接 | A-5 契约接口人职责优先（§10 风险 2） |
| **compose 前端容器的 healthcheck 从未通过**（2026-09-11 发现并已修） | 容器长期显示 `(unhealthy)`；一旦有服务 `depends_on: {condition: service_healthy}` 指向前端，**整个栈会卡死在启动阶段** | 原写法 `wget -qO- http://localhost/`：容器 `/etc/hosts` 里 `localhost` 同时映射 `127.0.0.1` 与 `::1`，busybox 的 wget 先试 `::1`，而 **nginx 只 listen 了 IPv4 的 80** → 永远 "Connection refused"（实测 `FailingStreak: 299`，自建容器起就没绿过）。改为显式 `http://127.0.0.1/` 后立刻 healthy。后端那侧写 `localhost` 没事，是因为 Tomcat 默认双栈绑定——**同一份 compose 文件里两种写法看起来对称，实际只有一个是对的**，这类「对称的错觉」值得留意 |
| ~~本机无 Docker/PG~~ | ✅ **已解除** | Docker 已装（走清华源 + 镜像加速器）；B-4 已在 `postgres:16` 上验完（173/173 + 重启不丢数据）；B-3 的 compose 编排也已完成（`docker compose up` → 173/173 + down/up 不丢数据） |
| ~~`M0_接口冻结_致B_v1.md` 与现状不符~~ | ✅ **已处理（2026-09-10 深夜）** | 该文停在 09-09，D5/D6/D2 与现状不符（详见下方「已处理」）。<br>**处理方式：就地作废，不删不移。** 理由：**D1–D10 的编号定义只在这一份文件**，`message-contract.md`（「D2/D3/D7/D8」）与 `B侧接口契约_M0.md`（「见 §4-D6」）都在引用它，整体挪走会让引用悬空。故保留编号定义，在文首加作废横幅 + 在三处不符的行上就地标注。<br>**另行补**：`message-contract.md` 缺的 `collectTime` 非法即 REJECTED 一条已补进 §2「字段有效性」；`docs/README_文档管理说明.md` 已在索引里注明本文为历史参考。 |
