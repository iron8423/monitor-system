# 通用监测管理系统 · 全景与交接（2026-09-11 周五）

> 用途：① 给你（B）一个“全景、不慌”的说明；② 作为交接文档——**新对话打开这篇即可接续**。
> 更新：相对 09-09 版，**A 在 09-10 把阶段 1 后端全部做完了**；你的角色正式转为**阶段 2 前端（3D 大屏）**。

---

## 一、一句话

**做一套给客户用的“变形/安全监测管理系统”。** 首批接 **77GHz 点形变雷达**，做一个**能演示的 demo**：雷达数据进去 → 3D 大屏看到变形点 → 看曲线 → **±3mm 超限自动报警** → 确认/处置/解除留痕。

落地：清远电厂“天地一体化”；首个场景=灰库/库区边坡；界面向华测对齐。

## 二、最大变化（09-10）：阶段 1 后端已由 A 全部做完

A 承接了你原本 B3/B4/B5 的后端 + 自己的收尾：

- **测项种子**改 `defo_mm`/`rate_mm_d`（每点 2 项，14 行）；**默认规则**：`defo_mm` 双向 `±3.0/±1.0`（warning）+ `+5.0/恢复 +2.0`（alarm）。
- **ingest 落库修好**：唯一键加 `metric_code`、补 `point_id`/`point_code` 列、字段 `measureValue`；未知点号/设备整条 REJECTED。
- **新增 `alarm` 整包**（规则/警情/处置动作状态机）、**`media`**（上传/列表/内容）、**SSE `GET /stream`**、**`projects/{id}/summary`**、**`latest`/`series`** 查询。
- 修了：设备 `last_report_time` 回写、`deviceId` 校验、时间带时区、H2 console 安全（B-7）、500 被伪装成 401。
- **Docker**：`docker-compose.yml` + `backend/Dockerfile` + `.env.example`；**Postgres 迁移验证 173/173**；`docker compose up` 后验收脚本全过。
- **验收套件** `tools/acceptance/`（8 套 / 173 断言，全过）；**雷达模拟器** `tools/radar_simulator/`。

> 结果：**M1/M2 后端闭环 ✅**；阶段 1 后端验收全过。**M0 签字流程取消**（单仓库，git 历史即事实源）。

> 📎 A 侧剩余计划与逐条细节见仓内 `docs/后续阶段工作清单_A_v1.md`（A 已同步最新，22347 字节那份）。
> 其中 **A-6**（可选）：A 有空档会先做一个 **CesiumJS 可行性 spike**（真实地形 + 标点变色 + 点击弹窗）。你做前端前先问他有没有这个结果——能直接少踩坑。

## 三、你（B）现在只做一件事：阶段 2 前端

- `frontend/` 目前只有 `.gitkeep` + README（**空的**）。
- 你要做（**接口都已就绪，直接对接**）：
  1. 前端工程（Vite + Vue3 + Element Plus + ECharts）。
  2. 登录 + 项目/测点/设备/告警/媒体的工作台页面。
  3. **3D 大屏（Cesium，重点/门面）**：真实地形 + 监测点标点（状态色）+ 点击弹窗 + 时间轴/热力图，对齐两篇参考。
  4. 位移分析页（`latest`/`series` 曲线）。
  5. 告警中心（`alarms` + `actions` 状态机）、管理端。
  6. 无人机影像挂点（`media`）。

## 四、后端接口速查（已就绪，前端直接调）

- 认证：`POST /api/v1/auth/login`（admin/operator/analyst/maintainer，密码 `123456`）、`GET /auth/me`。
- 档案：`GET /api/v1/{projects,scenes,objects,points,metrics,devices}`（列表裸数组）。
- 测点数据：`GET /points/{id}/latest`、`GET /points/{id}/series?metricCode=defo_mm&from&to&granularity=raw|hour|day`。
- 概览：`GET /projects/{id}/summary`。
- 告警：`GET /alarms`、`GET /alarms/{id}`、`POST /alarms/{id}/actions`（confirm/research/dispatch/handle/resolve/misreport）、`/alarm-rules` CRUD。
- 设备：`GET /devices/{id}/status`。
- 实时：`GET /api/v1/stream?token=<JWT>`（SSE：connected/measurement/alarm）。
- 媒体：`POST /media`、`GET /points/{id}/media`、`GET /media/{mediaId}/content`。
- 文档：`http://localhost:8080/swagger-ui.html`；OpenAPI `/v3/api-docs`。

> 契约以仓内 **`docs/message-contract.md`** + **`docs/B侧接口契约_M0.md`** 为准（后者 09-10 已同步最新）。

## 五、跑起来（本机）

- 后端（H2）：`cd backend; .\mvnw.cmd spring-boot:run` → `http://localhost:8080`。
- 一键（Postgres + 后端）：`docker compose up`（需 Docker）。
- 验收：`tools/acceptance/run-all.sh`（8 套 / 173）。
- 模拟/回放：`tools/radar_simulator/`、`tools/radar_csv_replay/`。

## 六、下一步路线图（前端，B）

1. 搭前端工程骨架 + 登录（对接 `auth/login`）。
2. 总览/测点页（对接 `points` + `latest` + `series`）。
3. **3D 大屏**：Cesium 地形 + 标点 + 状态色 + 点击弹窗；再叠时间轴/热力图。
4. 告警中心（`alarms` + `actions`）、管理端、媒体挂点。
5. 对齐华测界面 + 参考两篇（TvT.js / Cesium）。

## 七、环境 / 常见坑

- **git 代理**：`git config --global http.proxy http://127.0.0.1:7890`（你已配；去代理用 `--unset`）。
- **JDK21**：`C:\Program Files\Eclipse Adoptium\jdk-21.0.12.101-hotspot`；改 `JAVA_HOME` 后重开终端；VSCode/mvnw 认 JAVA_HOME。
- **前端需 Node（≥20）**（阶段 2 装）。
- 那个 `[https://...](...)` 是聊天渲染网址，忽略即可；git 失败八成是代理。

## 八、时间线（缓解焦虑）

- **阶段 1 后端**：✅ 已完成（A）。
- **阶段 2 前端 demo**（登录+3D+曲线+告警）：兼职约 **1~2 周**。
- **真实场地/服务器/两张显卡**：那是工程化/AI 阶段，**不是你现在要操心的**。

## 九、给“新对话”的启动话术

> 请先读 `C:\Users\ASUS\Desktop\实习\监测系统\docs\全景与交接_20260911.md`（及仓内 `docs/message-contract.md`、`docs/B侧接口契约_M0.md`），了解现状：**阶段 1 后端已由 A 做完（含告警/媒体/SSE/查询/Docker），我现在做阶段 2 前端（Vue3 + Cesium 3D 大屏）**。然后带我一步步搭前端：先工程骨架+登录，再总览/测点/曲线，再 Cesium 3D 大屏，最后告警中心/管理端/媒体。前端要对接的后端接口都在文档里。

---

> **一句话（给你自己）**：后端 A 已经全做完、验收 173/173；你只剩**前端（重点是 Cesium 3D 大屏）**，1~2 周能出 demo。真实场地/服务器/GPU 不用你操心。**把前端做出来，就跑赢了。**
