# 通用监测管理系统 · 全景与交接（写于 2026-09-09 周三晚）

> 用途：① 给你（B）一个“全景、不慌”的说明；② 作为交接文档——**新对话打开这篇即可接续**。
> 读法：先看「一、一句话」「二、图景（别慌）」，再看「现状 / 交接 / 下一步」。

---

## 一、一句话

**做一套给客户用的“变形/安全监测管理系统”。** 第一批接公司的 **77GHz 点形变雷达**（盯边坡/灰库），做一个**能演示的 demo**：雷达数据进去 → 3D 大屏看到变形点 → 看曲线 → **±3mm 超限自动报警** → 有人确认/处置/解除留痕。

落地：**清远电厂“天地一体化”监测预警系统**；首个场景=清远电厂灰库/库区边坡；界面向**华测**对齐。

## 二、图景（别慌：现在只做“demo”）

| 你担心的 | 真相 |
|---|---|
| 真实场地装好了 | 装设备=硬件/现场的活，不是你。系统只换数据源：现在读 CSV，以后读真雷达接口/协议——适配已写好 |
| 服务器 | 只是把 demo 部署上去，技术栈现成，最后一步才做 |
| 两张显卡 | 以后做 AI（趋势预测/融合/视觉）才用，跟现在的 demo 无关，现在不用管 |
| 多传感器 | 现在只接点形变雷达；其它只是预留测项模型 |

> 你现在要做的=**一个能跑的 demo**（有边界、可做玩）。老师说的“简单/毕业生设计”就是指它。别把“产品化/工程化/现场/AI”这些大愿景套到自己头上。

## 三、现状（已完成 / 已验证）

### 已建 & 已验证（backend 已跑通）
- **仓库**：GitHub `iron8423/monitor-system`（私有），A+B 已合并到 `main`（`bec77fa`）。
- **后端**：Java 21 + Spring Boot 3.5.16 + MyBatis-Plus 3.5.17 + H2(阶段1)/Postgres(阶段2) + Flyway + jjwt + OpenAPI，端口 **8080**。
- **实测通过**：`/api/v1/health` UP；登录 `admin/123456` 拿 JWT；`GET /api/v1/points` = **7 个测点**（`P-HK01/02/03` 灰库、`P-BP01/02/03/04` 边坡）；`GET /api/v1/devices/1/status` = `radar-001 ONLINE 92%`；档案/审计/维护可用。
- **B1 ingest**：接口注册、实测可收数（传现有测项 `VEL` → `accepted=1 OK`）；**批量格式 `{items:[...]}` + 请求头 `X-Ingest-Key: dev-ingest-key`**。

### 已写好（B 侧）
- **B1 已对齐** `measurement` 表：`pointId/metricId/metricCode/deviceId/messageId/seq/schemaVersion/measureValue/unit/quality/attributes/rawRef/collectTime/receiveTime/createdAt`；`IngestService` 用 A 的 `MonitorPointMapper/MetricMapper` 把 `pointCode→point_id`、`metricCode→metric_id`；CSV 回放改用批量格式。
- **CSV 回放**：`tools/radar_csv_replay/radar_csv_replay.py`（读真雷达 CSV→标准消息→POST `/ingest`；`--ingest-key dev-ingest-key`、点号映射 `P-HK01…P-BP04`、`--inject-overlimit` 凑到 ±4mm）。
- **文档**：需求基线 v1.0、双人分工、消息契约、B侧接口契约、M0对表清单、给A待办、工作日志。

## 四、仓库与技术栈

```text
monitor-system/                 (# GitHub iron8423/monitor-system)
├── docs/      需求/方案/接口/日志/归档
├── backend/   Spring Boot 后端（A 底座 + B 的 telemetry/B1）
├── frontend/  占位（阶段 2 你做 3D）
├── tools/radar_csv_replay/    CSV 回放（Python）
└── README/.gitattributes/.gitignore
```

后端模块：`auth`(JWT/RBAC)、`common`(Result/BizException)、`project`(项目/场景/对象/测点/测项)、`asset`(设备/状态)、`audit`、`organization`、`config`(SecurityConfig/IngestKeyFilter/DataInitializer)、`telemetry`(**B1 ingest**)；`db/migration/V1__init_schema.sql`+`V2__seed_data.sql`。

## 五、关键文件位置

- 需求基线：`docs/通用多传感监测管理系统_需求分析与开发指引_v1.0.md`
- 分工/两阶段：`docs/双人分工实施方案.md`
- 消息契约：`docs/message-contract.md`、`docs/雷达标准消息契约_v1.md`
- B 接口契约：`docs/B侧接口契约_M0.md`
- **给 A 的待办**：`docs/给A的待办_V2种子对齐.md`（A 改种子用）
- 工作日志：`docs/daily/2026-09-09-B.md`
- B1 ingest：`backend/src/main/java/com/monitor/telemetry/`
- CSV 回放：`tools/radar_csv_replay/radar_csv_replay.py`

## 六、3 个对齐点（下一步的钥匙）

1. **A 改 V2 种子**：每点测项 5 项(X/Y/Z/DISP/VEL) → **2 项 `defo_mm`/`rate_mm_d`**；默认规则 `DISP≥10/5` → **`defo_mm` 双向 `±3.0/±1.0`、warning**。SQL 在 `docs/给A的待办_V2种子对齐.md`。
2. **B 已对齐 B1**（entity+service），等 A 改完种子即可收 `defo_mm`。
3. **前端仅占位**，阶段 2 由 B 做（Cesium 3D）。

> ⚠️ 现状：A 种子还是旧 5 测项 → B1 收 `defo_mm` 会 `REJECTED`。**A 改完种子，M1 才能通。**

## 七、接下来路线图（明确）

**阶段 1（后端收尾，B 写）**
1. 催 A 改 V2 种子（1 处）。
2. **M1 联调**：`py tools/radar_csv_replay/radar_csv_replay.py --date 20260827 --send --speed 5 --max-events 200 --inject-overlimit`（先 `.\mvnw.cmd spring-boot:run`）。
3. 写 **B3 质量** → **B4 告警**（alarm_rule/alarm/alarm_action 状态机，±3 规则）→ **B5 realtime(SSE `?token=`)+media**。
4. 补 **数据查询**：`GET /points/{id}/latest`、`/series`、`/projects/{id}/summary`。

**阶段 2（B 转前端）**
5. Vue3 + **Cesium 3D 大屏**（地形/标点/状态色/时间轴/热力图，对齐两篇参考）→ 位移分析页 → 告警中心 → 管理端。参考：`github.com/hawk86104/three-vue-tres`、`github.com/CesiumGS/cesium`。

## 八、时间线（缓解焦虑）

- **完成 demo**：**1~2 周（兼职）**（后端+B1 已铺好，剩 B3/B4/B5 + 3D前端）。
- **上服务器**：+约 1 周。
- **真实场地/多传感器/AI**：**几个月 + 一个团队**，不是这个 demo 阶段、也不是你一个人该做的。
- **11 月**：只要交**可演示 demo**，**11 月前绰绰有余（9 月底可有雏形）**。若被要求“真实场地+多传感+对接现场”，需跟老师/A **明确范围**——那属工程化阶段。

## 九、常见坑 / 注意事项

- **网络**：本机直连 GitHub 有时不通（有代理），git 失败就配置 `http.proxy`（浏览器能用说明有代理）；Maven Central 一般能下。
- **JDK**：JDK21 在 `C:\Program Files\Eclipse Adoptium\jdk-21.0.12.101-hotspot`；需 `setx JAVA_HOME` 后**重开终端**；`java` 命令可能仍显示 Java8（系统 PATH 靠前），**VSCode/mvnw 认 JAVA_HOME** 不受影响。
- **当前分支**：本机 local = `main`（`bec77fa`）；改完用 `git add -A && git commit && git push`。
- **别再复制带框号网址**（会变 `[https://...(https://)` 导致连接失败）——网址手动敲，或用已有 remote（`git fetch origin` 无需输网址）。
- **H2 内存库**：重启后端数据清空（DEMO 可接受；阶段2 切 Postgres）。

## 十、给“新对话”的启动话术

> 请先读 `C:\Users\ASUS\Desktop\实习\监测系统\docs\全景与交接_20260909.md`，了解“通用监测管理系统”现状与下一步；然后继续：先确认 A 是否已改 `V2__seed_data.sql`（测项 defo_mm/rate_mm_d + ±3 规则），若已改就帮我跑/修 B1 接入与联调，再按路线图推进 B3 质量 / B4 告警 / B5 实时+媒体，之后进阶段 2 的 Cesium 3D。

---

> **一句话（给你自己）**：你现在只需把“点形变雷达 → 3D → 曲线 → ±3mm 告警 → 处置留痕”这个 demo 跑通，1~2 周能做玩、11 月前能交付。真实场地/服务器/两张显卡统统不是你现在要操心的。**做小、跑通闭环，就赢了。**
