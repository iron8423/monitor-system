# 工作日志 2026-09-14 · A（收尾小缺陷批 + 影像挂点整块）

> 承接：09-11 收口了前端合并与三个后端缺陷（日志 53–80）。本周不再按「模块」排，
> 改**按需求 §11 的 9 条验收**排——那才是可核查的判据。逐条对表后缺口只剩四处：
> 大屏 3b、影像挂点、审计/维护记录/设备绑定、项目隔离与备份恢复。
> 用户定案：**功能面缺口优先** + **隔离与备份都做** + **数据中断告警翻案补上**；
> 大屏 3b 本周明确不做。
>
> 本日先清掉三处已取证的小缺陷（技术问题，按惯例直接修），再开工本周最大的一块功能面缺口。

## 一、三处小缺陷

### 81. 曲线阈值线两张图都标错了档位（同口径抄了两份）

`PointsView.vue:47-57` 与 `home/HomeAnalyst.vue:44-47` 各自写死了一份阈值：
都是 ±3mm、都标「告警 +3mm」，研判台还在脚注里复述了同一套口径。但 V4 后来加了
`defo_mm gte +5.0 / level=alarm` 这条规则——真正的「告警」档是 **+5**，
两张图上既**标错了名**、又**少了 +5 那条线**。

两份复制正是「该抽出去」的信号，所以修法不是改两处数字，而是抽
`composables/useThresholds.js`：拉 `listAlarmRules()`（`api/monitor.js` 补上封装），
按 `metric_code` 过滤，`operator` 映射成 `gte/lte` 两条线，label 直接用规则名 + 等级——
**不再造第三个名字**。只画「全局规则 + 本测点规则」，且只画 `enabled`。

两处「等规则页做出来再改成拉接口」的过期注释（含 `HomeAnalyst.vue` 的脚注）一并删掉：
规则页（`AdminView.vue` 的 `alarm-rules` 页签）早就存在了。

### 82. 改完之后发现阈值线**一根都画不出来**——上一版是视觉上的死代码

这条是浏览器实测撞出来的，逐层查下来不是取数问题（`/alarm-rules` 200、3 条规则都符合过滤），
而是 **ECharts 的 `markLine` 坐标走轴空间、且被绘图区裁掉，而轴范围只按数据算**。

演示库 7 个测点的 `defo_mm` 都在 **0.09–0.14mm**，阈值却在 ±3 / +5——阈值线全落在视口外。
也就是说这块功能一直是**视觉上的死代码**：代码里写着 ±3mm，屏幕上什么都没有。
**「标签错了」这个缺陷之所以一直没人发现，正因为看不见的东西谈不上被看错。**

修法：`SeriesChart` 按「数据 ∪ 阈值」算 y 轴 min/max（留 8% 余量）。**代价说清楚**：
0.1 量级的数据会被压成贴着零的平线。但这是如实的——它正确表达了「当前形变远低于阈值」；
只按数据缩放的话曲线起伏好看，却看不出离报警线还有多远，阈值线也就失去了意义。
形变监测里「离报警线还有多少」正是要看的东西，故选前者。

**实测**（compose 生产构建；数 canvas 像素而非读 option——应用是 Vite 打包的 ESM，
`window.echarts` 不存在，拿不到实例）：

| 场景 | 报警红 | 预警橙 |
|---|---|---|
| 带全部种子规则 | 130px | 260px |
| **admin 停用 +5.0 那条规则** | **0px** | 208px |
| 恢复规则 | 130px | 260px |
| 研判工作台（同一 composable） | 352px | 704px |

中间一行是关键：停用规则后红线消失而橙线仍在，证明线**真的来自接口**，
同时排除「整块图没渲染」这个假阳性。全程零控制台/网络错误。

> **测试前提的教训**：默认时间范围是近 24 小时，而演示库数据当时最新到 09-11，
> 图会渲染成 `el-empty` 而非 canvas，数像素得到 "no canvas"。
> 那是**前提没铺好，不是缺陷**——报缺陷前先把前提补齐。

### 83. B-13：点号 33–64 字符「建得出来、永远收不到数」

`monitor_point.code` 是 VARCHAR(64)，而冗余业务键 `measurement.point_code` 只有 VARCHAR(32)。
于是 33–64 字符的点号**建得出来、列表看得见、永远收不到数据也永远不产生警情**——一上报即 500。
PG 实测确认根因：`value too long for type character varying(32)`。

改前先用 40 字符点号复现，确认是 500 而不是静默丢数；随后三处配套修：

- **V5** 把 `measurement.point_code` 放宽到 64。已确认该列**没有索引**
  （`measurement` 上只有 `idx_measurement_point_time`），故为纯 DDL、不涉索引重建；
- `MonitorPoint.code` 加 `@Size(max=64)`、`BaseCrudController` 的 create/update 加 `@Valid`，
  把「能建不能收」变成**建的时候就 400**。`@Valid` 对没有约束注解的其余实体是空操作，
  所以放在基类是安全的；
- 契约 §2 注明 `pointCode` 上限 64。

**验红**（断言确实有牙）：把列宽改回 VARCHAR(32) 复跑 `02` ⑫，3 条转红（上报 500）。
两条 400 断言仍绿——它们测的是 Java 侧 `@Size`，与该列宽无关，两半确实互相独立。

选 64 而非把 `monitor_point.code` 收到 32：点号由现场设备/项目编码习惯决定，
平台没理由拒绝一个自己允许建出来的点号。

该条此前标注「属 B 的 schema 与契约范围，不宜单方面改」——**09-10 起 A/B 分工已合并**，
schema 与契约现由同一人维护，该顾虑不再成立，故本轮修掉。

## 二、验收套件自己造的垃圾（一类缺陷，不是一次事故）

### 84. 每跑一轮验收就往演示库攒几条「孤儿警情」

**现象**：演示库告警中心里 20 条未解除警情，其中 **18 条指向的是已软删的临时测点**
（`P-QRY-*` / `P-SIMQ-*` / `P-DUP-*` / `P-LONG-*` / `P-ROLE-*`）。点进去既没有测点、
也画不出曲线，值班工作台被这些历史垃圾整个占满。

**根因不在某一次跑，而是每跑一轮就多几条**：套件末尾回收临时测点用的是
`DELETE /points/{id}`，而 `monitor_point` 有 `deleted` 逻辑删除列、`alarm` 表
**没有**「测点被删则警情失效」这回事——警情只记 `point_id`，不做联表校验。
点一软删，那条警情就成了孤儿，却**照样排在待办队列里**。

**修法**（用户定案：产品行为不动，回收责任归造数方）：`lib.sh` 新增 `recycle_point()`，
删点前先把该点上非终态（≠ `RESOLVED`/`FALSE_ALARM`）的警情逐条 resolve 掉；
7 个套件里 **10 处裸删点**一律改为调它。03/07 的「项目链从叶子往上删」循环里
测点那一项也单独摘出来走 `recycle_point`。

**为什么不让后端按「点是否已删」过滤**：那会改掉契约语义——警情是历史事实，
不该因为档案变动而消失。这是产品决定，不是实现细节。

顺带订正 `tools/acceptance/README.md` 的断言数：04 套件实为 **47**（表里写 46），
合计 **201**（表里写 200）。

### 85. 同类垃圾的另一半：套件建的告警规则**没有回收**

`04-alarm.sh` 建规则、点/设备都回收了、**规则没有**。而 `alarm_rule` 表
**没有 `deleted` 列**（无逻辑删除，DELETE 即物理删），所以每跑一轮就永久留 2 条，
累计 **12 条**，在管理端「告警规则」页签里明晃晃排着。它们指向已软删的测点、
**因此不会触发**——是演示污染，不是正确性问题。同批清掉，并在 `04` 末尾补回收。

### 86. 演示库清理 + 重造

按用户定案「清演示库 + 套件回收」：

- 删掉 **18 条孤儿未结警情**（连带 30 行时间线——schema 里**零外键**，没有任何级联，
  必须手工配平，这也是 84 里「删点不留孤儿」必须在套件侧解决的原因之一）；
- 用户的范围只覆盖**未结**警情，所以另有 **40 条 RESOLVED 警情同样指向已删档案**，
  **刻意留在原地未动**（已上报）；
- 用 `tools/radar_simulator` 重造演示数据：7 个真实测点各 24 条整点样本（336 行，
  结束时间贴近当前，这样默认的「近 24 小时」窗口才有东西可画），
  外加 **3 条真实 PENDING 警情**（值班与研判四个角色这才都演得动）：

  | 警情 | 测点 | 等级 | 数值 | 说明 |
  |---|---|---|---|---|
  | 1071 | P-HK02 | warning | 3.6 | 命中 gte 3「形变正向超限阈值」 |
  | 1072 | P-BP02 | warning | 3.4 | 同上 |
  | 1073 | P-BP04 | **alarm** | 3.4 → **5.8** | 先按 gte 3 触发，再命中 gte 5 那条**就地升级**（时间线：`trigger` → `escalate`「等级 warning -> alarm」）。`trigger_value` 留在 3.4 是对的——B-11 定的语义就是「升级只改等级、不改所属规则」，恢复仍按最初触发那条规则判定 |

  复核时库里是 **4 条**未结警情，第 4 条（1074）是新建的 **DEVICE 类型离线告警**
  （`radar-001`）：模拟器停了之后 `last_report_time` 自然老化出 5 分钟窗口。这是**如实的**，
  不是缺陷——而且正好让运维台有东西可点；演示时跑起模拟器它会自动解除。

> **自伤记录**：第一次灌数用了**全 7 点**跑 `--inject-overlimit`，结果下一轮把注入的那个点
> 又按正常值报了一遍，**自动解除**刚造出来的警情（alarm 1069：触发 10:09:29 →
> 升级 10:09:29.78 → **恢复 10:09:31.3**，备注「defo_mm = -0.1379 已回落至恢复阈值内」）。
> 教训：注入必须 `--points <那一个点>`，否则自己踩自己。
> 另外 B/C 两次跑都用了同样的 `--backdate-days 0.03 --step-minutes 5`，
> 写出 224 行 `collect_time` 冲突的重复样本（同一时刻两个值，其中一个还是没告警的
> 5.8 尖峰）。整个窗口删掉重跑才干净。

## 三、影像挂点（闭合验收第 6 条）

### 87. 整块落地

验收第 6 条原文是「**点一个监测点**：最新值、历史曲线、照片/影像、告警全出来」——
主战场是**测点详情**，不是另起一个孤立页面。后端三条端点早就齐了
（`06-media.sh` 21 条断言覆盖），前端整块空白，所以这一块是纯前端。

- **`PointsView.vue` 重写为五个页签**：资料 / 曲线 / 数据表 / 影像 / 告警。
  这一下同时闭合三件事：验收第 6 条、`双人分工实施方案v2.md:237`（B8 要求测点信息/
  设备状态/维护记录页签 + 数据表）、以及影像的自然归属。页签**懒加载**（`loadedTabs`），
  且**换测点时要重拉已打开过的页签**——否则会出现「切到 B 测点，页签里还是 A 的照片和警情」，
  这是最容易在演示现场被问住的一处。
- **两个共用组件**：`MediaGallery.vue`（拉列表 + 拼地址 + 大图预览）与
  `MediaUploader.vue`（`el-upload` 走 `http` 实例，`FormData` 拼 `file/pointId/takenAt/note`）。
  三处形状不同（网格 / 分组网格 / 单张缩略图），所以**只把「拉列表 + 拼地址」收在组件里**，
  布局交给 `columns` 与 `max`——否则又变成一份口径抄三遍。
- **`/media` 总览页**：跨测点按测点分组浏览 + 上传。菜单项 `AppLayout.vue` 的
  `{ path: '/media', ready: false, stage: '阶段 5' }` 转正。
- **大屏浮窗加缩略图**：验收第 6 条明说「详情**与 3D 大屏**」，所以 `ScreenView` 的点击浮窗
  里也放一张（`max=1`、`hide-empty`——该点没图就整块不出现，不留空档）。

### 88. `?token=` 是这块唯一的坑，而且是静默的

`<img>` 发不出 `Authorization` 头，与 `EventSource` 是同一个约束，所以
`/media/{id}/content` 走 `?token=`——**这是契约里除 SSE 之外的第二处、也是最后一处 query 鉴权**。
忘了它就是满屏碎图，而且**每张图各自一个 401**，图片加载失败又不走 axios 拦截器：
控制台里只有一串裸 401，界面上没有任何提示。

前端因此统一走 `api/monitor.js` 的 `mediaContentUrl()`，三处调用点都不自己拼地址。

另外一个 Element Plus 的陷阱：`el-image` 的 `#error` 插槽会**替换掉** `<img>`，
所以取图失败时 DOM 里**一个 `img` 元素都不剩**——数 `.thumb img` 得到 0 而不是 N 张碎图。

### 89. 验证与证伪

**浏览器实测 18/18** 全绿，三处表面（测点详情影像页签 / `/media` 总览页 / 3D 大屏浮窗）
都覆盖，含一次**走真实 UI 的上传**（`el-upload` → `FormData` → axios → 后端落盘）。
断言不看「有没有 `<img>` 标签」，看它**真的解码出像素**（`complete && naturalWidth > 0`）——
`<img src>` 401 时标签照样在 DOM 里、`naturalWidth` 却是 0，
「元素存在」在图片这件事上等于没断言。

**证伪**：把 token 去掉 → **8 条转红**，错误明细 `取图 HTTP 401 http://localhost/api/v1/media/M1017/content`；
恢复 → 18/18 回来。

**证伪顺带查出我自己测试的一个错**：`✗/✓ 缩略图 src 都带 ?token=` 在**全图 401 的坏版本上
也亮绿**——因为 `[].every(...)` 恒为 `true`，而 el-image 的 error 插槽把 `<img>` 换掉了，
一张缩略图都没渲染。**这条断言恰好在最该变红的时候变绿**。改成先要求非空再 `every`。

另外两处测试自身的坑一并记下，避免下次重踩：

- 「大图能翻页」不能数 DOM 里的 `img` 个数：Element Plus 的 viewer 只渲染**一张** `<img>`，
  翻页时替换 `src`。按 `ArrowRight` 后断言 `src` 变了才作数。
- `/screen` 上**不能用 `networkidle2`**：AppLayout 的 SSE 是一条永不结束的连接，
  加上 Cesium 持续请求瓦片，「网络静默」这个条件根本不会成立，只能等到 30s 超时。
  必须 `waitUntil: 'domcontentloaded'`。

### 90. 演示库影像清理，以及过程中我自己犯的错

演示库当时攒了 **24 行 media**：10 张历次 `06-media.sh` 上传的 1×1 测试图（70 字节）、
加上我这次验证反复跑上传的十几张 240×160 图。清到只剩 **3 张**（P-HK01 两张、P-BP04 一张），
盘上文件同步删，并给留下的三张补了备注与拍摄时间。

**过程中我把两个文件名看错了**——`object_key` 那一列我没逐字对齐，凭 `find` 输出的印象
留下了 1018/1019 两行（已删）的文件，把 1021/1022 真正指向的两个文件删掉了。
清完做 API 复核才发现：`M1021`/`M1022` 取图 **404**，只有 `M1023` 是 200。

修法不是「再小心看一眼」，而是**改成一个可复核的不变量检查**：从 DB 读 `object_key`
与 `file_size`，从盘上读实际文件与大小，逐条比对，并反过来查孤儿文件。
对齐后 3/3 一致，三个内容端点全部 `200 image/png` 且 PIL 解码出 240×160，
无 token 仍然 401、不存在的 id 仍然 404。清理后用**只读**的浏览器脚本复核 **9/9** 全绿。

> 顺带记一个容易踩的路径口径：`object_key` 带 `media/` 前缀（`media/P-HK01/<uuid>.png`），
> 但盘上的实际路径是 `<UPLOAD_DIR>/P-HK01/<uuid>.png`——**`object_key` 不是路径**，
> 不能直接拼。写清理脚本时按它当路径就会「一条都删不掉」（我第一次就删了 0 个文件）。
> 代码里 `MediaService` 的注释已经写明 objectKey 只是对外逻辑键，但这一点在写运维脚本时
> 特别容易想当然。

> **为什么这次只跑只读复核、不再跑那 18 条**：那套每跑一次要往库里上传 3 张图，
> 而 media 模块**没有 DELETE 端点**，回收不了——跑一次就把刚清干净的演示库再污染一次。
> 这正是下面 91 要说的事。

## 四、三件定案（当日已问，用户已答）

### 91. 影像模块没有删除端点

契约 §7 只有上传 / 列表 / 内容三条，**没有 DELETE**。两个后果：

1. 验收套件 `06-media.sh` 每跑一轮就往库里留一张 1×1 测试图，**回收不了**
   （与 84 里给告警套件补的 `recycle_point()` 不同，那批至少有 `DELETE /points` 可用）；
2. 运维误传的现场照片没有任何界面手段可以删。

加删除端点属**契约变更**，按「产品语义先讨论」的惯例上报。

> **定案：「加 DELETE，软删」**——新增 `DELETE /api/v1/media/{id}`，只把行标成 `deleted=1`，
> **文件留在卷上**。用户给的理由是与 `monitor_point`/`device` 的既有逻辑删除口径一致、
> 可审计可恢复；代价是卷只增不减，需要另配清理策略。当日即落地，见下面第五节。

### 92. 40 条指向已删档案的 RESOLVED 警情

用户定案的清理范围是**未结警情**，所以这 40 条**刻意留着没动**。
它们不影响演示（不在待办队列里）。**定案：留着**——警情是历史事实，已结的留在库里不影响演示
（不进待办队列），维持上次定案，不动。

### 93. `docs/daily/` 里两份错位的过期副本

- `docs/daily/B侧接口契约_M0.md`（221 行，09-09 14:38）
- `docs/daily/M0对齐_差异清单_A视角.md`（09-09 15:41）

两者都是**旧版**，现行事实源是 `docs/B侧接口契约_M0.md`（251 行）。它们被误放进
`docs/daily/`（那里应该只放日志）。**定案：删掉**——确认是误放进 `docs/daily/` 的旧版，
git 历史里仍可追溯。已 `git rm`。

## 五、影像删除端点（定案的落地）

### 94. 后端：加一个属性，而不是加一条启发式规则

`DELETE /api/v1/media/{mediaId}` 挂在 `MediaController` 上，`@PreAuthorize('ADMIN','MAINTAINER')`
（与设备-测点绑定同口径：传错了该由传的人自己撤）+ `@AuditAction(action="删除影像")`。
`MediaService.delete` 只调 `mediaMapper.deleteById`——**不碰盘上文件**（V6 的注释写明了理由：
软删的全部价值就在可挽回，接口顺手删文件就把这个价值抵消了）。
先 `requireByCode` 再删，所以**删两次第二次是 404，不是静默成功**。

V6 加了 `media.deleted` 列。`Media` **不继承 `BaseEntity`**，但全局配置
（`logic-delete-field: deleted`）是按**字段名**认的，所以声明一个
`private Integer deleted;` 就自动生效——不必加 `@TableLogic`。这条记在实体注释里。

**顺手撞出来的一个坑**：审计行的 `target_id` 是空的。`AuditAspect.resolveTargetId` 只认
`Long` 和 `Identifiable`，而这里路径变量是编码后的 **字符串** `"M1024"`。
两条路：① 扫描「任何 String 参数」当 targetId——**否决**，那会让 `?keyword=xxx` 这类
查询参数悄悄变成 target_id；② 在 `@AuditAction` 上加一个显式开关
`targetIdFromStringArg()`（默认 false），**由声明者明说**。选了 ②。
「猜」看起来更省事，但它把决定权从写注解的人手里拿走，交给了一个碰巧的顺序。

### 95. 前端：`deletable` 默认 false，是为了让大屏那处**保持没有**

`MediaGallery` 三处共用，新增 `deletable` 开关。**默认 false**：只有测点详情与 `/media`
总览页显式打开它，3D 大屏浮窗**刻意不加**——那是「看」的场合，演示现场挂个删除按钮只会误触。
`canDelete` 再叠一道角色闸（`deletable && role ∈ {ADMIN, MAINTAINER}`）。
注释里写明**这只是不显示按钮、不是权限**：值班/研判真去调那个接口仍然 403。

确认框文案特意点明「文件仍保留在服务器上，误删可以找回」——不说清楚的话，
用户要么不敢删，要么真出事时以为没救。确认按钮文案改成「删除」而不是默认的「确定」。

### 96. 证伪：给 `delete` 注入一个空实现

新增的 11 条断言（`06-media.sh` ⑦/⑧）不能只看它们绿。把 `MediaService.delete` 改成
`if (true) { return; }` 重编译后跑：**通过 27 / 失败 5**，且红的正是该红的那几条
（删除后仍在列表里、取图仍 200、重复删除不是 404……）。恢复后重新编译干净。
失败的 5 条**都在我预期的集合内**，没有「不知道为什么红了」的噪声。

⑦ 里还有一条**只在非容器形态下断言**的：软删后盘上文件仍在。容器里跑时
`backend/data/media` 不是卷的挂载点，所以脚本对「查不到」只 `info` 不 `fail`——
这条断言在 compose 形态下是空转的，**不能当成已覆盖**。

### 97. 一个我自己犯的错：脚本里两次口径不一致

浏览器复核脚本第一版 14 条里红 2 条，看名字像是应用的问题（「新传的那张出现在页面上」
「页面上也少了一张」）。实际是**我自己的算术错**：`countMedia()` 只数 P-HK01，
而 `/media` 总览页是**跨测点**的（P-HK01 两张 + P-BP04 一张），拿单测点的数去比
必然差 1。改成 `countAllMedia()` 后同口径，红的就变绿了；另有一条是反过来——
测点详情页只显示本测点，我却拿全测点的数去比。

**两次都是同一个错误的两面**：比之前没问「这两个数是不是一回事」。
这与 90 里删错文件是同一类——**不是看错了，是没定义「怎样算对」**。

### 98. `--fresh` 不清上传目录，于是攒了 60 个孤儿文件

查盘时发现本机 `backend/data/media/P-HK01/` 下有 **60 个文件**，全是 70 字节的 1×1 测试 PNG。
根因清楚：H2 是 `jdbc:h2:mem`（**进程一停库就没了**）、种子也不含 `media` 行，
所以后端一退出，这个目录里的文件就**定义上全是孤儿**——拥有它们的那张表已经不存在了。

以前没显形，是因为……其实一直在显形，只是没人看。而**影像改逻辑删除之后，这个问题被固化了**：
套件删了行，但盘上文件按设计保留，于是每跑一轮 `--fresh` 净增 3 个文件。

修法不是「手工清一次」，而是**让 `--fresh` 把它和空库一起清掉**——
`--fresh` 的既有语义就是「一份全新的、空的状态」，留着上一轮的孤儿文件与这个语义矛盾。
清 60 → 跑一轮后 3 → 再跑一轮**仍是 3**（不再累积）。容器形态走 compose 命名卷，不受影响。

### 99. 演示库里 4 条软删残留

实测删掉的那张（M1024）加上三轮浏览器复核各删一张（M1025/26/27），共 4 条 `deleted=1`。
界面上看不见（列表与内容端点都过滤），但它们是我这轮验证的残留、从来不是真实内容，
所以**连行带文件一并物理清掉**，让演示库回到 3 张真实巡检照片。

清完按 90 的教训立刻做**双向对账**：库→盘无断链、盘→库无孤儿、3/3 完全一致。

## 验证

| 项 | 结果 |
|---|---|
| `npm run build` | ✅ 通过（19.42s；仅 chunks > 500kB 的既有提示） |
| `tools/acceptance/run-all.sh --fresh` | ✅ **212/212，失败 0**（01→19、02→34、03→25、04→47、05→12、**06→32**、07→26、08→17）；连跑两轮均绿，且上传目录稳定在 3 个文件（不再累积） |
| 影像浏览器实测（compose 生产构建） | ✅ **18/18**，三处表面全覆盖 |
| 证伪（去掉 token） | ✅ 8 条转红，错误明细 `取图 HTTP 401` |
| 清理后只读复核 | ✅ **9/9**，零控制台报错、零 4xx/5xx |
| media 行 ↔ 盘上文件不变量 | ✅ 3/3 一致，无孤儿文件 |
| 内容端点 | ✅ 三个全 `200 image/png` 240×160；无 token 401；不存在 id 404 |
| 阈值线像素实测 | ✅ 见 82 的表，含「停用规则→红线消失」的反证 |
| 影像删除 · 接口矩阵（compose PG） | ✅ DELETE 200 / 列表消失 / 取图 404 / 重复删除 404 / 无 JWT 401 / operator 403 / analyst 403 / maintainer 过闸；DB `deleted=1`；**卷上文件保留**；审计行 `admin \| 删除影像 \| Media \| M1024` |
| 影像删除 · 套件证伪 | ✅ 注入空实现后 **27/5**，红的是该红的 5 条；恢复后重编译干净 |
| 影像删除 · 浏览器（compose 生产构建） | ✅ **14/14**：ADMIN 两处有按钮、**大屏浮窗没有**（`deletable` 默认 false）、operator 看不到、真删一张后页面与接口都少一张、确认框含「文件仍保留」 |
| 演示库软删残留清理 | ✅ 4 条连行带文件清掉；**双向对账 3/3 一致**，无断链无孤儿 |
| 本机上传目录 | ✅ 60 → 3，再跑一轮仍 3 |

## 提交

| commit | 内容 |
|---|---|
| `736c587` | 阈值线改拉 `/alarm-rules` + 抽 `useThresholds` |
| `f868aa8` | B-13 点号上限两处对齐 64（V5 + `@Size` + `@Valid`） |
| `9844fb3` | 阈值线被 y 轴裁掉（`SeriesChart` 按「数据 ∪ 阈值」算轴范围） |
| `9ad7a73` | 套件 `recycle_point()` + 10 处调用点，孤儿警情不再新增 |
| `6582d2c` | 影像挂点整块（8 个文件：3 新增 5 修改） |
| 本批 | 影像删除端点（V6 + 后端 4 文件 + 前端 3 文件 + 契约 §7 + 套件 ⑥→32 断言）+ `--fresh` 清上传目录 + 文档收口 + `git rm` 两份错位副本 |
