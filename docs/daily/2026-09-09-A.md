# 工作日志 2026-09-09 · A（底座）

## 今日完成
- [x] 仓库初始化：`git init -b main`、`.gitattributes`（LF/CRLF）、`.gitignore`、`README.md`、目录结构（`backend/`+`frontend/`+`docs/`+`plan/`）
- [x] A0 工程骨架：Spring Boot 3.5.16 + Java 21 + Maven Wrapper（mvnw/mvnw.cmd），统一响应 `Result<T>`/`PageResult<T>`、全局异常、请求日志、`GET /api/v1/health`
- [x] M0 契约：Flyway `V1__init_schema.sql`（16 张表，含 B 的 measurement/alarm/media）+ `V2__seed_data.sql`（清远电厂、灰库3点+边坡4点、7测点、1雷达、1默认规则）+ `DataInitializer`（4 演示账号 admin/operator/analyst/maintainer，密码 123456）+ `docs/message-contract.md` + springdoc OpenAPI
- [x] A1 auth：JWT（jjwt 0.12.7）+ 无状态过滤器 + `SecurityConfig` + `POST /auth/login`、`GET /auth/me`、`POST /auth/logout` + 4 角色 RBAC（`@PreAuthorize`），401/403 已验证
- [x] A2 project+organization：`organizations/projects/scenes/objects/points/metrics` 通用 CRUD（读开放、写 ADMIN）
- [x] A3 asset：`devices` CRUD + `GET /devices/{id}/status`（最近上报时间推导在线/离线）+ 设备-测点绑定 + `maintenance-records`（MAINTAINER/ADMIN，operator 自动取当前用户）
- [x] A4 audit：`audit_log` + `@AuditAction` 注解 + `AuditAspect` 切面 + `GET /audit-logs`（仅 ADMIN）

## 遇到的问题 / 卡点
- start.spring.io 现只提供 Spring Boot 4.x，无 3.x → 改为手写 `pom.xml`（Spring Boot 3.5.16），仅从 starter 抽取 Maven Wrapper 文件。
- MyBatis-Plus 3.5.9+ 分页插件 `PaginationInnerInterceptor` 拆到独立模块 → 需额外引入 `mybatis-plus-jsqlparser`。
- 审计注解与审计实体同名 `AuditLog` 冲突 → 注解改名 `@AuditAction`。

## 明日计划
- 与 B 对齐 M0 契约（schema/消息/OpenAPI 逐条确认）。
- 待 B 提供 `telemetry` 后，联调「模拟器 → 落库」；作为契约接口人响应字段疑问。

## 需要对方配合
- 请 B 确认 `docs/message-contract.md` 字段口径（尤其 measurement 幂等键与 quality_flag 取值）。
- 请 B 确认 `V1__init_schema.sql` 中 B 侧表（measurement/alarm/alarm_rule/alarm_action/media）字段是否满足需要。

## 晚段：手动验收 + M0 对表（2026-09-09 下午）

### 手动验收（复测全通过）
按验收主线走了一遍：4 账号登录 / 档案 CRUD / RBAC(403/401) / 设备状态 / 维护记录(operator 自动填充) / 审计留痕 / 404-400 边界 / swagger。**抓出并修复 2 个真缺陷**：
1. **主键自增冲突**：V2 种子显式插 id(1~N) 而 identity 序列不前进 → 应用新建记录撞种子 id，一律 500。修复：V1 全部 identity 列加 `(START WITH 1000)`，种子段与应用段隔离。复测 organization/project/scene/point/metric/device create 均新 id≥1000。
2. **异常码错位**：未实现接口返回 500(应 404)、参数类型错返回 200(应 400)。修复：`GlobalExceptionHandler` 补齐 `@ResponseStatus`(400/404/401/403 与 body.code 对齐)。

### M0 对表（与 B 的接口契约对齐）
收到 B 的 `docs/daily/B侧接口契约_M0.md`(14:38)，关键词核对发现双方**契约不同源**（B 用 `defo_mm/rate_mm_d/RT1`，A 用 `X/Y/Z/DISP/VEL`，均引用仓库内不存在的《雷达标准消息契约_v1》）。
产出：
- `docs/daily/M0对齐_差异清单_A视角.md`：10 条差异(D1~D10) + A 侧改动清单 + 提问清单
- `docs/M0_接口冻结_致B_v1.md`：发给 B 的最终冻结口径（D1~D10 决策 + A 时间线 + 待 B 答复 3 项）
用户已确认 D1~D10 按 A 建议执行。D1/D2 落地等 B 提供雷达契约 & Q2/Q3 答复。

### 晚段 2：M0 A 侧不依赖 B 的改动落地（D3~D8）
- D7：新增 `config/IngestKeyFilter`，`/api/v1/ingest/**` 免 JWT 改验 `X-Ingest-Key`；`SecurityConfig` 放行 + 注入（并禁用 Boot 自动注册防双重执行）；`application.yml` 加 `monitor.ingest-key`（env `MONITOR_INGEST_KEY` 覆盖）。验证：无钥/错钥 401、对钥过 filter 后 404（controller 属 B 未挂）。
- D8：`JwtAuthFilter` 仅 `/api/v1/stream` 支持 `?token=` 取 JWT。验证：无/错 token 401、对 token 过鉴权后 404；query token 在非 stream 路径被忽略仍 401。
- D6：V1 `alarm_rule` 扩展为 point_id/metric_code/rule_type/operator/window_minutes/recovery_value/alarm_level/repeat_suppress_seconds。
- D5：V1 注释冻结告警枚举（notice/warning/alarm、状态含 FALSE_ALARM、动作 confirm/research/dispatch/handle/resolve/misreport）；V2 默认规则改为新列 + level=warning（metric_code 暂 DISP，D1 待对齐）。
- 契约同步：`message-contract.md` 新增 §5（拆分规则/attributes 落位/ingest 密钥/SSE token）。
- 待 B 答复 D1/D2（雷达契约、测项、pointCode）后：改 V2 测项种子 + 默认规则 metric_code + 本清单收尾。
