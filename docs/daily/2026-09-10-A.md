# 工作日志 2026-09-10 · A（承接 B 侧后端）

> 承接方式：B 阶段 2 转前端，其 M1/M2 后端部分由 A 就地补齐（用户决策）。工作区改动本轮一并推送。

## 今日完成

### 1. A-3 落地：测项重构 + 默认规则（`V2__seed_data.sql`）
- 测项由 X/Y/Z 等旧口径改为**每点 2 项**：`defo_mm`（累计形变 mm）+ `rate_mm_d`（速率 mm/d），7 点共 14 行（对齐 `docs/message-contract.md` D1：真实雷达无三分量）。
- 默认告警规则改为**双向 ±3mm**（全局规则 `point_id=NULL`）：① `defo_mm gte +3.0 / 恢复 +1.0`；② `defo_mm lte -3.0 / 恢复 -1.0`；均 `level=warning`、`repeat_suppress_seconds=300`。对齐《B侧接口契约_M0》§4 冻结口径。
- 复测：度量 API 读回 14 行测项，规则经 `/alarm-rules` 读回 2 条，值正确。

### 2. M1 阻塞修复：三处契约错位（读代码定位，非文档）
B 的 ingest 骨架落地后 `POST /ingest/measurements` 实际写不进库，逐层核对发现并修复：

| # | 问题 | 修复 |
|---|---|---|
| a | `measurement` 唯一键不含 `metric_code`，而 D2 要求一条消息拆 N 行共用 `message_id` → 第 2 个测项即撞唯一键 | V1 唯一键改 `(device_id, message_id, metric_code)` |
| b | 实体有 `pointId`、表无 `point_id` 列（`point_id NOT NULL` 落库即失败） | V1 补 `point_id BIGINT NOT NULL` + `point_code VARCHAR(32)`（D10：id 数值 / code 字符串） |
| c | 实体字段 `value` 与库列 `measure_value` 不一致 | 实体改名 `measureValue` |
| d | 契约要求的查询端点缺失 | 新增 `latest`/`series`（见下） |

`IngestService` 补点号解析（`pointCode` → `point_id`，未知点号整条拒绝并计入 `rejected`）。

> 注：M0 未签字且 H2 内存库每次启动重放迁移，故直接改 V1 无 checksum 冲突（用户决策「直接改 V1」）。

### 3. 异常约定修正：`BizException` 返回真实 HTTP 状态
发现 `handleBiz` 无 `@ResponseStatus` → 业务异常一律 HTTP 200，与 9-09 日志中「400/404/401/403 已对齐」的记录不符（**该记录不实**）。已修为 HTTP 状态与 `body.code` 一致（如 404 测点不存在 → HTTP 404）。复测：终态警情再处置 400、非法动作 400。

### 4. M1 查询端点（新增）
- `GET /api/v1/points/{id}/latest` —— 取该点最新 `collect_time` 所在消息，按同一 `message_id` 回捞兄弟行**重组全部测项**，附带 `quality/signal/position`。
- `GET /api/v1/points/{id}/series` —— `metricCode`（默认 `defo_mm`）+ `from/to` + `granularity`（raw/hour/day 均值分桶）；`unit` 从档案 `metric` 表取，回退 `mm`/`mm/d`；测点不存在 → 404。
- 新增 `common/util/Times`：统一 `Asia/Shanghai` 时区，输出 ISO8601 带时区（契约要求，区别于 A 既有端点的 ISO-local）。

### 5. M2 告警闭环（新增 `com.monitor.alarm` 整包）
实体/Mapper×3（`alarm_rule`/`alarm`/`alarm_action`）+ DTO×5 + Service×3（`AlarmEngine`/`AlarmService`/`AlarmRuleService`）+ Controller×2：

- `AlarmEngine.evaluate()` 挂到 ingest 落库成功后；**异常内部隔离**，绝不阻断接入主流程；跳过 `SUSPECT`/`FAULT` 与非有限值。
- 触发/恢复：`gte`/`lte` 方向判定；`recovery_value` 回落即自动 `RESOLVED` 并留痕 `recover`；`repeat_suppress_seconds` 抑制重复。
- 状态机（D5）：`confirm→CONFIRMED`、`dispatch→PROCESSING`、`research→OBSERVING`、`handle→PROCESSING`、`resolve→RESOLVED`、`misreport→FALSE_ALARM`；终态不再受理处置；非法动作 400。
- `GET /alarms`（level/status/pointId/from/to 分页）+ `GET /alarms/{id}`（触发快照 + 处置时间线）+ `POST /alarms/{id}/actions`；`/alarm-rules` CRUD（读开放、写 ADMIN）。

**验收（`/tmp/alarm-test.sh`，10 项全通过）**：超限触发 → 快照/时间线 → `confirm/dispatch/research/resolve` 全链 → 终态保护 400 → 非法动作 400 → 负向规则触发 → **回落到 0.5 自动解除** → 规则列表值正确 → 无 JWT 401。

### 6. 文档
删除失效旧稿 `docs/雷达标准消息契约_v1.md`（与冻结版在**测点编码**、**幂等键**上直接冲突，易误用），`docs/README_文档管理说明.md` 索引与说明同步更新。

## 遇到的问题 / 卡点
- **旧稿仍在仓内且与冻结契约冲突**：9-09 提交时把《雷达标准消息契约_v1》一并进仓，而它是被取代的草稿（四元幂等键 + `SLOPE-01` 编码），与 `message-contract.md` 打架 → 本日移除。
- **规则类型 `RATE`/`CHANGE` 未实现**（本段初稿结论**有误，已更正**，见「续段 13」）：原写「建了会静默不触发」，实测**相反**——`AlarmEngine.shouldTrigger` 根本不判 `ruleType`，只要 `threshold_value` 非空就按 `THRESHOLD` 语义评估，所以一条声明为「速率」的规则会拿**原始值**去比阈值并真的告警。错误是静默的，比不触发危险。已收敛（接口层 400 + 引擎层过滤）。
- `MeasurementQueryService` 内仍有自带的 `iso()/parseTime()`，与新增 `common/util/Times` 重复，待合并（不影响功能）。

## 续段：B 侧剩余接口（同日，用户决策「暂时不分 AB，直接往下做」）

### 7. `GET /api/v1/projects/{projectId}/summary` 项目概览
`{projectId, pointCount, alertCount, onlineDeviceCount, maxDeformationMm}`。三处口径按「当前态」取：
- `alertCount` = **未解除**警情（`status` 不属于 `RESOLVED`/`FALSE_ALARM`）；
- `onlineDeviceCount` = 绑定到本项目测点、且状态判定为 `ONLINE` 的设备（设备须经 `device_point` 绑到本项目测点才计入）；
- `maxDeformationMm` = **各测点最新** `defo_mm` 中绝对值最大者，**保留原符号**（负向形变同样计入）。逐点 `LIMIT 1`，扫描量只与测点数成正比。

计数一律**先判空再查**——MyBatis-Plus 的 `in(空集合)` 会静默丢掉条件，退化成全表计数。

### 8. `GET /api/v1/stream` SSE 实时推送（新增 `common/sse/SseBroadcaster`）
- 事件：`connected` / `measurement` / `alarm`（载荷结构按契约 §6 的**扁平**示例，测项直接作为顶层字段）。
- 三条硬约束：**不阻断主流程**（推送失败只摘连接，异常不外抛）；**不推幽灵事件**（在事务中调用则挂 `afterCommit`，回滚就不推）；**不无限增长**（订阅上限 100 + 超时回收 + 30s 心跳注释帧）。
- 警情事件在**触发、自动解除、人工处置**三处都发（驾驶舱角标要跟着变）。
- 实测：订阅 → 上报 → 超限，事件顺序 `connected → measurement → measurement → alarm`（见下「问题 2」）。

### 9. 媒体：`POST /api/v1/media` + `GET /api/v1/points/{pointId}/media` + `GET /api/v1/media/{mediaId}/content`
- 落盘 `<uploadDir>/<pointCode>/<uuid>.<ext>`（默认 `backend/data/media/P-HKxx/…`），`objectKey` 对外为 `media/<pointCode>/<uuid>.<ext>`。
- **文件名由服务端生成**（客户端原文件名只留档）：实测传 `../../evil.png` 落盘仍是 `<uuid>.png`，未穿越。
- **扩展名取白名单**（优先 Content-Type）：传 `text/plain` → 400；测点不存在 → 404。
- 读取时二次校验路径落在上传根目录内；`?token=` 放行（`<img>` 带不了请求头，`JwtAuthFilter` 由「仅 SSE」扩为「SSE + `/media/*/content`」，其余路径仍只认 `Authorization`）。
- `media` 表补 `object_key/mime_type/file_size/taken_at/note` 列。

#### 9b. `mediaId` 改为字符串编码（用户决策「按字符串来」）
契约 §7 写的是 `"M001"`，此前按 D10 返回数值 `id`。现改为契约形态：对外只给编码，`url` 同用编码。
- 编码由主键**派生**（`MediaCode`：`M` + 3 位左补零，超 999 自然加宽），不单独存列——影像没有人工录入的业务码，派生可保证「一个 id 恒对应一个编码」，无需回填、也不会两列写不一致。
- 主键起点仍随其余 7 张表的 `START WITH 1000`，故首个编码是 `M1000`（契约示例 `M001` 是格式示意）。
- 读取路径 `{mediaId}` **同时容错裸数字 id**（`/media/1000/content` 等价），便于排障时直接拿库里的 id 试。
- 边界实测：`M1000`/`1000`/`m1000` → 200；`MABC`/`M`/`M-1`/`M99999999999999999999` → 404（解析失败按不存在处理，不回 500）。

### 10. 顺带修掉的三个真缺陷
1. **ingest 从不回写 `device.last_report_time`** → 设备状态（A 的在线判定）只认这个字段，实际一直在报数也永远显示离线；项目概览的在线数同理失真（种子用 `CURRENT_TIMESTAMP` 掩盖了 5 分钟）。现于落库成功后按消息的 `receiveTime`（无则当前时间）回写。
2. **ingest 不校验 `deviceId`** → 契约 §2 明写「deviceId/pointCode 不存在 → REJECTED」，此前只校验了点号。现补上（设备按 `code` 解析，如 `radar-001`）。
3. `.gitignore` 补 `data/`、`backend/data/`——上传的影像原本会被提交进仓。

### 11. IDE 24 条警告的定性
`VSCode` 问题面板的 24 条**全部来自 JDT**（`.vscode/settings.json` 的 `java.compile.nullAnalysis.mode: automatic`），
`./mvnw -o clean compile -Dmaven.compiler.showWarnings=true` 的 javac 输出为 **0 条**，构建本身干净。分三类：

| 类 | 数 | 处置 |
|---|---|---|
| `JwtAuthFilter` 覆写方法未标注 `@NonNull`（参数与父类声明不符） | 3 | **真问题**，补 `org.springframework.lang.NonNull` 修正 |
| Lombok getter × MyBatis-Plus `LambdaQueryWrapper` 的 method-ref「Null type safety」 | 17 | 按仓库既有约定（18 个类已在用）在 `IngestService`/`MeasurementQueryService` 加类级 `@SuppressWarnings("null")` |
| Java(1102)「category 'null' 未被分析」——由 `@SuppressWarnings("null")` 自身产生的提示级信息 | 4 | 保留：随上述约定必然出现，非错误 |

> 未改 `java.compile.nullAnalysis.mode`：那是仓库既有的工程设置，关掉等于全项目**回避**而非**修复**。

### 12. 契约示例与实际响应对齐（用户决策「按你的建议执行」）
改**文档**而非改代码，因为两处都牵涉 A 的公共信封，改代码会波及 A 的全部列表端点：
- §4 `GET /alarms` 示例 `items` → `records`（+ `pageNum`/`pageSize`），对齐 A 的 `common.PageResult`。
- §7 测点影像列表示例 `{items:[...]}` → 裸数组，对齐 A 非分页列表（如 `GET /projects`）的既有风格。
- §7 `mediaId` 示例 `"M001"` → `"M1000"`，并注明「不透明字符串，别解析数字部分」。

前端因此只需认一种分页形状、一种列表形状。

### 13. `RATE`/`CHANGE` 收敛 —— 以及过程中发现的一个静默错误行为
**先纠正本日初稿的错误结论**：初稿写「建 RATE/CHANGE 会存下来但不触发」。实测**相反**。

`AlarmEngine.shouldTrigger()` 只判 `threshold_value` 是否为空，**完全不看 `ruleType`**；而 `validate()` 又强制 `value` 非空 —— 于是任何类型（含 RATE/CHANGE）的规则都会被按 `THRESHOLD` 语义、拿**原始测值**去比阈值，并真的触发告警。

> 初稿的「验证」只确认了 warn 日志打出来，没有验证行为 —— 所以漏掉了。日志能打出，恰恰因为它和评估是两条独立的代码路径。

**探针（修复前，`defo_mm=12.0`）**：建 `RATE gte 10.0` 后上报 12.0 → 产出的警情快照里 `ruleName='实验-RATE规则'`、`threshold=10.0`、`level=alarm`。规则类型被彻底忽略，而用户以为自己建的是「速率」规则。

**收敛为两层**（缺一不可）：
1. **接口层**：`validate()` 只接受 `THRESHOLD`，其余 400，文案指向「速率请对 `rate_mm_d` 建 THRESHOLD 规则」（雷达本就上报该测项，引擎再算一遍会出现两个速率口径）。移除已无用的 `warnIfNotEvaluated`。
2. **引擎层**：`isEvaluable()` 过滤，只评估 `THRESHOLD`；对非 THRESHOLD 规则按 ruleId 去重记一条 warn。这层是防**绕过接口直接写库**的遗留行 —— 只拦接口的话，库里已有的 RATE 行仍会按原始值告警。

**验证**：借 `/h2-console`（dev 开着）直写一条 `RATE gte 10.0 / level=alarm` 规则，上报 `defo_mm=12.0`：
- 修复后：只产出 2 条警情，分别来自两条 THRESHOLD 规则；**直写的那条未触发**，且日志有 `规则类型 RATE 不参与评估，将被忽略: ruleId=1001`；
- 对照修复前（同一手势）：该规则**确实触发**了 `level=alarm` 的警情。

**若将来要做 `CHANGE`**，实现前需先定 5 件事（否则做出来大概不是产品想要的）：① 变化量定义（窗口内 `max−min`／`当前−窗口起点`／窗口斜率）；② 滚动窗口 vs 固定分桶；③ 窗口内 `SUSPECT`/`FAULT` 样本再过滤一次（现有闸门是逐条的）；④ 窗口内样本数下限（否则服务重启后第一帧就可能误报，历史是空的）；⑤ 与 `repeat_suppress_seconds` 如何叠加。性能上无需加索引，`idx_measurement_point_time (point_id, collect_time)` 已覆盖。

### 回归验收（全部通过）
- M2 告警闭环 10 项回归：绿。
- B 侧三件套：概览 404/401 边界、SSE 鉴权（无/错 token 401）、媒体 400/404、内容读取（带 token 与带头均 200，取回字节与原图 `cmp` 一致）。
- 新增：未知设备 `nope-999` → `rejected 1 / REJECTED`；设备 `last_report_time` 由启动时刻被回写为消息 `receiveTime`。
- 新增（告警套件 ⑪⑫）：建 `RATE`/`CHANGE` 规则 → 400；建 `THRESHOLD` 规则 → 0。
- 新增（引擎兜底）：借 `/h2-console` 直写 `RATE` 规则后上报超限值 → 该规则不触发，日志留 warn（见续段 13）。

### 14. 时间工具去重 + 补 M1 查询端点回归
- `MeasurementQueryService` 里私有的 `ZONE`/`iso()`/`parseTime()` 与 `common/util/Times` **逐行等价**（当时先于 `Times` 写就），已删除改为调用 `Times`；全仓检索确认无第三处重复。纯重构，行为不变。
- 顺带发现 `latest`/`series` **此前没有任何回归覆盖**（告警套件与 B 侧套件都没测），而这次动的正是它们的时间格式化 —— 补 `/tmp/m1-query-test.sh` 10 项：latest 重组测项 + ISO 带时区、series 三种时间入参写法、`unit` 从档案取、hour/day 分桶、非法时间/非法 granularity → 400、测点不存在 → 404、无 JWT → 401。

### 15. 更正一处此前记错的「缺口」
初稿把「CSV 回放器的 `TARGET-<n>` 回退」记成缺口（理由：档案 7 个测点、映射只 5 条）。**该推断的依据不成立** —— 我是从种子的 7 个测点推的，而 CSV 档案根本不在仓内（在雷达机的 `web_存档`）。
据 `tools/radar_csv_replay/README.md`：推荐演示日 `--date 20260827` 为 **Target1~Target5** 有变形数据（"正好 5 个监测点"），映射表 5 条**与之正好对应**，是设计而非缺口。
仍值得留意的一点：`--point-prefix TARGET-` 的回退会生成 ingest **必然拒绝**的编码，且只在「accepted 0 / rejected N」里体现。是否改成遇未映射目标直接报错退出，待定（无档案无法验证其他日期是否存在 Target6+）。

## 续段 2：验收套件落仓（同日，用户「继续往下进行」）

### 16. 把 `/tmp` 里的验收脚本搬进仓：`tools/acceptance/`
起因：M5 的判据是「`docker compose up` 一键过验收脚本」，但**仓里一个验收脚本都没有** —— 我此前写的三个还在 `/tmp`（重启即失，且 `alarm-test.sh` 非幂等：固定 messageId 撞幂等键、固定测点撞防刷屏窗口，跑第二遍必挂）。

落仓为 `tools/acceptance/`：`lib.sh`（公共库）+ 六个套件 + `run-all.sh` + `README.md`。**6 套件 / 117 条断言**，覆盖 §9 验收脚本里后端可独立验证的部分：

| 套件 | 断言 | 对应验收条 |
|---|---|---|
| 01-archive-auth | 16 | 第 7 条（新建测点立即可见）+ 档案/鉴权 |
| 02-ingest-idempotency | 18 | 第 1、2 条 |
| 03-query | 23 | 第 1 条（latest/series 数据面） |
| 04-alarm | 27 | 第 3、4 条（含防刷屏） |
| 05-realtime | 12 | 第 1、3 条的推送侧 |
| 06-media | 21 | 第 6 条 |

`run-all.sh --fresh` 另起一个后端（**新端口** 18080 + H2 空库）跑完自动停。为什么是新端口而不是重启 8080：旧 JVM 没退干净时，新进程因端口占用悄悄退出，健康检查却由旧进程应答 —— 就在一份陈旧代码/脏库上跑完全套还全绿（**这个坑当天真踩过**，见续段 2 之前的记录）。换新端口则端口被占就启动失败，健康检查永远等不到。退出码 `0` 全过 / `1` 断言失败 / `2` 环境问题。

**两个刻意设计**（都被实测逼出来的）：
- **造数一律落在本次新建的临时测点**。种子规则 `repeat_suppress_seconds=300`（V2 实测），同点同规则 5 分钟内只要产生过警情**含已解除的**就不再触发 —— 沿用固定种子点会让「5 分钟内重跑」必然查无警情，那不是代码错而是防刷屏真生效了。改成每轮新点后，**同库连跑三轮均 117/117**（实测）。
- **三个上报函数统一 messageId 打头**。原先有个自动生成 messageId 的 `ingest`，与 `ingest_id` 参数个数不同，被按后者签名调用 → messageId 当点号、点号当时间，整条请求变垃圾，连锁出一片 401。已删掉那个函数。

### 17. 顺带查出并修掉的两处时间缺陷（都直接顶着验收第 5 条）

**① ingest 丢时区偏移（已修）** —— `IngestService.parseTime` 用 `OffsetDateTime.parse(s).toLocalDateTime()`，**只留墙上时间**；而查询侧的 `Times.parse` 是 `atZoneSameInstant(ZONE)`，两侧口径不一致。
后果：设备用 UTC 报 `06:00Z`，存成 06:00 再被当 `+08:00` 回读，**整错 8 小时**。更要命的是 `last_report_time` 同理 —— 落库落到 8 小时前，而 `DeviceStatusPolicy` 的在线窗口只有 **5 分钟**，于是**设备一直在报数却永远显示离线**，正好是 ingest 那段注释声称要防的事。
实证：入 `2026-08-27T06:00:00Z` → 修复前回读 `2026-08-27T06:00:00+08:00`，修复后 `2026-08-27T14:00:00+08:00`。
修法：`atZoneSameInstant(Times.ZONE)`，并补一条无偏移写法的兜底（与 `Times.parse` 的容忍度对齐）。**未动**解析失败时静默取 `now()` 的行为 —— 改成抛异常会变更对 B 模拟器的接口语义，留待商量。

**② 设备状态时间不带时区（已修）** —— `DeviceStatusVO.lastReportTime` 是裸 `LocalDateTime`，Jackson 按 ISO 序列化成 `2026-08-27T15:05:00`，**没有偏移**，违反契约 §0「时间 ISO8601 带时区」（而 `alarms.triggeredAt` 等走 `Times.iso` 是带 `+08:00` 的）。
修法：改成 `String` + `Times.iso(...)`，与 `AlarmVO` 的既有写法一致。前端尚未开始写，此时对齐成本最低。

### 18. 一并查出、但**未擅自改**的几处（需定夺）

| 项 | 事实 | 建议 |
|---|---|---|
| **档案 CRUD 的时间全都不带时区** | `BaseEntity.createdAt/updatedAt` 同样是裸 `LocalDateTime`，被 `BaseCrudController` 原样返回 → `GET /projects`、`/points` 等**所有**档案端点的时间都无偏移 | 与 ② 同源但**影响面是整个响应契约**。全局改（注册 Jackson 序列化器统一走 `Times.iso`）能一次修净，但会动所有时间字段的线格式。前端未开始写，是改的窗口期 —— 请定 |
| `latest` 在 collect_time 相同时取哪一行是不确定的 | `orderByDesc(collectTime).last("LIMIT 1")` 无第二排序键；同一时刻两条（不同 messageId）时"最新值"随数据库返回顺序而变，兄弟行也随该行的 messageId 走 | 加 `receive_time` 或 `id` 作次排序键，让"最新"确定 |
| 契约 §4 示例 `"lastAction": "触发"` 与代码不符 | `AlarmService#toVO` 直接取 `action_type`，实际是 `trigger`/`recover`/`confirm` 等英文动作串 | 改契约示例（同 §4/§7 前几处） |
| `duplicates` 与 `accepted` 量纲不同 | `accepted` 按**落库行数**累加（`+rows`），`duplicates`/`rejected` 按**消息条数**（`+1`）。一条 2 测项消息被去重时是 `duplicates=1` 而非 2 | 契约 §2 未写明，建议在文档里点明量纲 |
| SSE 线格式 `event:measurement`（冒号后无空格） | 契约 §6 示例写 `event: measurement`（有空格） | 两者都符合 SSE 规范，纯观感；套件已用 `event: *` 容忍 |

### 19. 验收 8 条里后端仍未实现的两处（此前只在口头提过，这里记档）
- **第 5 条「生成设备告警」**：全仓唯一的 `@Scheduled` 是 SSE 心跳；`OFFLINE` 只在 `DeviceStatusPolicy` 出现（**读时算出来**，拉）。`/devices/{id}/status` 已实现、离线与恢复都能正确反映，但**没有任何东西生成设备告警**。而清单 §4 写的是「第 1/2/3/5 条**主体 B**」，A 该供的 status 已供 —— 属**接口两侧都假定对方生成**的一条缝。
- **第 3 条「等级升高能升级」**：`AlarmEngine` 在同规则已有未解除警情时只判恢复，`if (open != null) { shouldRecover ? 恢复 : 什么都不做 }`，**没有升级路径**；不同 level 是不同规则，会各自成警情。防刷屏那半句已由 04 套件 ⑦ 覆盖并在本日通过。

## 明日计划
- 定 18 表里那两处影响面大的：档案时间是否全局带时区、`latest` 是否加次排序键。
- 第 5 条「设备告警」归谁做、做成什么形态（是否进警情列表、什么 level）需与 B 对一次。
- 视需要补 `RATE`/`CHANGE` 窗口聚合（已定案收敛，见续段 13）。
- 前端联调所需的档案/告警筛选维度按需补。
- 回放器未映射目标是否改为「报错退出」而非静默回退（见续段 15）。

## 需要对方（B）配合
- **拉取本日提交后重新对表**：A 改了 `telemetry` 包的实体字段（`value`→`measureValue`、补 `pointId`）、落库逻辑与 `measurement` 表结构（唯一键 + `point_code`），并新增 `latest`/`series` 两个端点。B 若在本地改过同名文件，请以本仓为准。
- **`pointCode` 必须是档案点号**（`P-HK01…P-BP04`）：ingest 现在按点号解析 `point_id`，**未知点号整条拒绝**（计入 `rejected`）。CSV 回放适配器的 `TARGET-<n>` 映射需确认已落到档案码。
- **`deviceId` 同样必须存在**（`radar-001`）：本日起 ingest 也校验设备，未知设备整条拒绝。回放器 `--device` 默认值已是 `radar-001`，无需改。
- ~~媒体 `mediaId` 与契约示例不一致~~ → **已定案**（用户决策「按字符串来」）：`mediaId` 现返回编码字符串（如 `"M1000"`），`url` 为 `/api/v1/media/M1000/content`；读取接口兼容裸数字 id。前端按**不透明字符串**处理即可，不要解析数字部分（编码格式若变，只有服务端受影响）。
- **另发现两处「契约示例 vs 实际响应」的形状差异**（均未擅自改动，因涉及 A 的公共信封）：
  1. `GET /api/v1/alarms` 返回 `{total, pageNum, pageSize, records}`，契约 §4 示例写 `{total, items}`。`PageResult` 是 **A 的公共信封**（`common/PageResult`，A 的全部 CRUD 列表端点都在用），改字段名会波及 A 的所有列表接口 —— 建议**保留 `records`、改契约示例**，前端只用一种分页形状。
  2. `GET /api/v1/points/{pointId}/media` 返回裸数组，契约 §7 示例写 `{items: [...]}`；A 的非分页列表（如 `GET /projects`）也是裸数组 —— 建议同上，按 A 的既有风格统一。
- ~~确认 `RATE`/`CHANGE` 是否 M1 就要~~ → **已定案（用户决策「按你的建议执行」）：收敛为只接受 `THRESHOLD`**。建 RATE/CHANGE 返回 400，报错文案直接指向「速率请用 `rate_mm_d` + THRESHOLD」。契约 §4/§8 已同步标注。若将来要做 `CHANGE`，需先定 5 件事（变化量定义 / 滚动或分桶 / 窗口内质量过滤 / 样本不足下限 / 与 `repeat_suppress_seconds` 叠加），详见续段 13。
