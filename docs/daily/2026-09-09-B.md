# 工作日志 2026-09-09 · B（角色B）

## 今日完成
- [x] 明确项目：通用多传感监测管理系统；首批数据源=毫米波点形变雷达；落地清远电厂灰库/库区边坡。
- [x] 文档管理：需求基线升为 v1.0；旧稿归档；新增 B 侧接口契约、M0 对表清单、消息契约。
- [x] 与 A 完成 **M0 对表冻结**：合同/幂等/枚举/鉴权/设备状态归 A/默认规则（双向 ±3mm、恢复 ±1、warning）。
- [x] 摸清真实雷达 CSV：监测数据在 `web_存档\目标信号强度-YYYYMMDD\TargetN-RSSI-*.csv`（含累积形变）；推荐 demo 日期 `20260827`（Target1~5）。
- [x] 编写 **CSV 回放适配器**（`radar_csv_replay.py`）：读真 CSV→标准消息；`X-Ingest-Key`；点号映射 `P-HK01…P-BP04`；`--inject-overlimit` 对齐 ±4mm；幂等 `device_id+message_id`。
- [x] 编写 **B1 ingest Java 骨架**（`backend/src/main/java/com/monitor/telemetry`）：DTO/实体/Mapper/Service/Controller，含校验、幂等、按测项拆行、quality、attributes、落库。
- [x] 环境：查询到本机仅 Java8；JDK21 已由用户安装（`Eclipse Adoptium\jdk-21.0.12.101-hotspot`）；需用户 `setx JAVA_HOME` 后重启生效。

## 遇到的问题 / 卡点
- 本沙箱无法联网下载 JDK（TLS `SEC_E_NO_CREDENTIALS`），且提权被策略禁止 → JDK 由用户本机安装。
- 沙箱改注册表（`setx JAVA_HOME`）被拒 → 需用户在自己的 PowerShell 里执行。
- A 仓库暂无 remote，尚未有共享远程；需今日建 GitHub 私库并统一分支。
- `java` 命令仍解析到 Java8（系统 PATH 里 Java8 靠前）；靠 `JAVA_HOME` 给 VSCode/mvnw 用 Java21。

## 明日 / 下一步计划
- 拿 A 的 `measurement` 表列名，对齐 B1 的 `Measurement.java`（README_B1 第 1 条）。
- 打通 **M1 联调**：`py radar_csv_replay.py --date 20260827 --send --speed 5 --max-events 200 --inject-overlimit`。
- 继续阶段 1：B3 质量 → B4 告警 → B5 realtime/media。

## 需要对方（A）配合
- 提供 `measurement` 表列名、共享 remote 地址、分支名。
- 确认默认规则与 `--inject-overlimit` 参考值（±3 触发 / ±1 恢复 / warning；注入目标 ±4mm）。
