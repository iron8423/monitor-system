# 工作日志 2026-09-11 · A（前端合并 + 两个后端缺陷）

> 承接：A 侧阶段 1/2 后端待办已于 09-10 清空（B-3/B-4/B-7 收口），本轮开工做前端。
> 当日发现 **B 也在写前端**（`9e97924 feat(frontend): 阶段1 前端骨架 + 登录`），用户定案
> **「以 B 的骨架为基线，把 A 的页面并进去」**，故本轮没有产生第二条前端线。

## 一、前端合并

### 53. 撞车与合并口径

上午按「前端尚未有人动工」的判断独立搭了一版前端（18 个源文件、5 个页面、已在浏览器里点过），
提交前发现 B 当天已把**阶段 1 骨架**推上 `main`（19 个文件：登录页 / 路由守卫 / Pinia store /
token 工具 / EChart 封装 / AppLayout / HomeView 占位）。两版技术栈选型完全一致
（Vue3 + Vite + Element Plus + ECharts + pinia + vue-router、`/api` 走 Vite 代理、`admin/123456`），
共有的 6 个同名文件**全部不同**——是各写各的。

用户定案：**以 B 的骨架为基线**（它已在 `main` 上、且 README 里定义了阶段 2–5 的交付清单），
把 A 的页面适配过去。具体处置：

| 文件 | 处置 |
|---|---|
| `api/http.js`、`api/auth.js`、`utils/token.js`、`stores/user.js`、`layout/AppLayout.vue`、`views/LoginView.vue`、`views/NotFoundView.vue`、`index.html`、`vite.config.js`、`main.js`、`App.vue`、`jsconfig.json` | **保留 B 版**。B 的 `http.js` 401 处理比 A 版讲究（注释里记着「排除整个 `/auth/**` 会导致脏 token 清不掉」这个踩过的坑），登录页的分栏品牌区也更完整——没有理由覆盖。 |
| `api/monitor.js`、`utils/labels.js` | **并入 A 版**（B 尚未写这两块）。`monitor.js` 的路径前缀改为 `/v1/...` 以对齐 B 的 `baseURL: '/api'`。 |
| `components/SeriesChart.vue` | **重写为「业务层」**：只负责把测点时序翻译成 `option`，渲染交给 B 的通用 `EChart.vue`。拆两层的理由：`option` 的构造是业务（阈值线、单位、点密度），`init/resize/dispose` 是通用管道，混在一起将来加第二张图就得抄一遍生命周期。 |
| `views/HomeView.vue` | **替掉 B 的阶段 1 自检脚手架**（B 自己注明那是临时的：「确认无误后我们就进入阶段 2」）。新内容是总览：KPI + 测点 + 最近警情。 |
| `AnalysisView` → `PointsView.vue` | 并入，路径按 B 的阶段规划改名 `/points`（测点与曲线）。 |
| `DeviceView.vue` / `AlarmView.vue` / `AdminView.vue` | 并入。管理端是新增菜单项，B 原菜单里没有。 |
| `stores/auth.js` | **丢弃**，改用 B 的 `stores/user.js`（B 把 user 也持久化了，A 版只在内存）。 |

**路由与菜单**按 B 的阶段清单落地：`/home` `/points` `/devices` `/alarms` `/admin` 启用；
`/screen`（阶段 3）与 `/media`（阶段 5）保持 disabled——不放点进去是白页的入口。

**主题**：B 的骨架是亮色（`--mk-*`），A 那 5 个页面是深色。用户定案**跟 B 走亮色**，
所以页面里的 `m-panel`/`m-muted`/`m-mono` 等自绘样式统一改成 `mk-*` 通用件并写进
`styles/index.css`（放在全局而不是每个组件各写一份，否则同一个「面板边框」在五个页面里迟早漂成五种颜色）。
3D 大屏 `/screen` 将来若单独走深色，不受此影响。

### 54. 依赖：echarts 5.6.0 中危 XSS → 6.1.0

B 的 `package.json` 锁 `echarts: ^5.6.0`，实装 5.6.0，`npm audit` 报 **1 个中危**
（`GHSA-fgmj-fm8m-jvvx`，Apache ECharts 跨站脚本，修在 6.1.0）。A 那版先前已用 6.1 验证过
兼容，故直接提到 `^6.1.0`，`npm audit` 归零、构建通过。
**实际可达性低**（图表数据来自自家后端，工具提示里没有用户可控字符串），但这是免费修，没理由留着。

## 二、两个后端缺陷（真机验证时撞出来的，与选哪版前端无关）

### 55. 缺陷一：`/devices` 与 `/projects/{id}/summary` 对同一台设备给出相反结论

**现象**（浏览器实测）：设备页显示 radar-001 **在线**（最后上报 `2026-09-10T16:37`，已过 18 小时），
同一时刻总览 KPI「在线设备」显示 **0**。两个数都是后端给的。

**根因**：`device.status` 这一列的语义是**人工显式标注**（只有 `FAULT` 有意义，
在线/离线由 `lastReportTime` 推导，见 `DeviceStatusPolicy#statusOf`），但 V2 种子里写进去的是 `ONLINE`。
而 `BaseCrudController` 的通用列表**直接吐实体**，于是列表返回存量值、永久不变；
`/devices/{id}/status` 与项目概览的在线设备数则都按策略**现算**。

**为什么必须修在后端**：前端无法调和——要么把 5 分钟判据在浏览器里抄一遍（判据就此分叉，
而告警是按后端判据发的），要么按设备数逐个去调 `/{id}/status`（N+1）。
`DeviceView.vue` 的注释里明确拒绝前者，那是对的，所以修在列表端点。

**修法**：`DeviceController` 覆盖基类的 `list()` 与 `get()`，返回前把 `status` 换成
`DeviceStatusPolicy.statusOf(d)`（只改返回对象，**不回写**——这是读时推导，不是把推导结果持久化）。
`statusOf` 对 `FAULT` 是幂等的，所以显式标注不受影响。

**实测**（compose / postgres profile）：

| 端点 | 修前 | 修后 |
|---|---|---|
| `GET /devices`（列表 status） | `ONLINE`（存量值） | **`OFFLINE`**（现算） |
| `GET /devices/1`（详情） | `ONLINE` | **`OFFLINE`** |
| `GET /devices/1/status` | `OFFLINE` | `OFFLINE`（对照，未变） |
| `GET /projects/1/summary` → `onlineDeviceCount` | `0` | `0`（对照，未变） |

修后三者一致。灌入模拟器数据设备回到 ONLINE 后，列表/详情/状态/概览**同时**变成 ONLINE 与 1。

**回归**：验收套件 173/173 全过。已确认套件只对 `/devices/{id}/status` 与 `/devices/1` 的
`updatedAt` 断言，**未**断言列表里的 `status` 值，故本次收窄是安全的。

### 56. 缺陷二：验收链的第一环在 PG profile 上从未走过

**现象**：前端测点页列出 7 个种子测点，曲线却是空的——`GET /points/1/series` 返回 `points: []`。

**查库**：`measurement` 有 98 行，但 `point_id` 全是 1001–1017、`point_code` 全是
`P-ING-…/P-QRY-…/P-ALM-…` 这种**验收套件自建的一次性测点**；种子测点 `P-HK%` 的行数是 **0**。

**根因**：模拟器昨天灌的数据进了 H2（dev profile，内存库），随进程一起没了；
compose 跑的是 PG，库里只剩套件残留。`tools/acceptance/08-simulator.sh` 虽然补上了「模拟器」这一环，
但它跑在**空库 `--fresh`** 或临时测点上，**从没在 PG 的种子测点上跑过**。
也就是说：「模拟器 → 曲线出点」这条链在 dev 上验过，在部署形态（PG）上没验过。

**处置**：对 compose 后端跑一次模拟器，造一段覆盖 24 小时的历史曲线
（48 轮 × 30 分钟、`--backdate-days 1`），`accepted=672 / rejected=0 / duplicates=0`。

**实测结果**：`/points/1/series` 由 0 点变 **47 点**（`P-HK01`，`2026-09-10T11:25` → `2026-09-11T10:25`）；
总览 KPI 由「最大形变 —」变 **−1.2224mm**、「在线设备」0 → 1。

> 顺带澄清一个容易看错的地方：`maxDeformationMm` 是**带符号**的（`ProjectSummaryService#maxDeformation`
> 取各测点最新值中绝对值最大者，负向形变不取绝对值），所以它可以是负数。
> −1.2224 经逐点核对是对的（`P-BP02` 最新形变 −1.2224，绝对值最大）。
> 但卡片上「最大形变」配一个负数会让人以为算错了，故在标签旁加了一个问号提示说明口径。

## 三、浏览器端到端验证（本轮把「没浏览器」这个卡点注销了）

本机有 `/usr/bin/google-chrome`，故用 `puppeteer-core` 驱动**真实 Chrome** 把页面点了一遍
（工具装在 `/tmp/fe-verify`，**刻意放在仓库外**，不污染 `package.json`）。结果：

| 检查 | 结果 |
|---|---|
| 未登录访问 `/points` | ✓ 守卫送到 `/login?redirect=/points` |
| 登录后 | ✓ 回跳到被拦下的 `/points` |
| 总览 | ✓ KPI 全为真实值（测点 7 / 未解除 0 / 在线设备 1 / 最大形变 −1.2224mm）、最近警情 5 行 |
| 测点与曲线 | ✓ **canvas 1100×320、曲线 47 点**（见下）、最新值面板为真实测项 |
| 设备状态 | ✓ 1 行真实数据；status 与概览一致 |
| 告警中心 | ✓ 20 行真实数据 |
| 管理端 | ✓ 7 个页签各自拉到真实资源 |
| SSE | ✓ 顶栏「实时已连接」（`/stream?token=` 连接建立） |
| 控制台 / 网络 | ✓ **无控制台报错、无 4xx/5xx** |

**踩到的一个测试自身的坑**（记下来免得复用脚本时再中招）：B 的登录表单**已预填** `admin/123456`，
脚本若再 `type()` 一遍会变成 `adminadmin` → 401。第一版脚本正是这么写的，看到 401 差点当成应用缺陷；
改用 curl 直接打登录接口（200）才定位到是脚本的问题。**「测出来的失败」要先自证不是测量工具坏了**——
和 09-10 那次瓦片探测全部 FAIL 是同一类错误。

## 四、第二轮：B 并入 3D 大屏；修 summary / latest 的口径分叉

### 57. 仓库状态：3D 大屏由 B 并入（`ddd3103`）

B 在 A 的合并（`efaec38`）之上推了 `feat(frontend): 并入阶段3a 3D 大屏（Cesium）`，
按同一口径执行：他**没有**把自己那版重复的阶段 2 页面并回来（避免两套总览并存），只并了阶段 3a。
`/screen` 是**顶层路由**，不套 `AppLayout`——占满整屏、自带 HUD。

**顺带发现 B 的 3b 计划里有个坑**（已回给 B）：`/screen` 不在 `AppLayout` 之下，
而当前 SSE 连接的发起在 `AppLayout.onMounted`。若 3b 只把连接**搬进** `stores/monitor.js`
却仍由 AppLayout 触发，则大屏（SSE 的主要消费者）永远连不上——直接进 `/screen` 或在该页刷新都拿不到。
连接的**发起**必须放在比 AppLayout 和 ScreenView 都活得久的地方（路由守卫里调一次，
或两处都调幂等的 `connect()`），断开挂在登出上。**这块 A 不碰，归 B**，避免撞车。

B 另外报了 `/summary` 与 `/latest` 的最新值口径不一致（下条），那属 A 的后端。

### 58. 缺陷三：`/projects/{id}/summary` 与 `/points/{id}/latest` 同刻多行取到不同行

**现象**：同一测点在同一 `collect_time` 落两行时，`/points/{id}/latest` 取到后写的那条（222.2），
`/projects/{id}/summary` 取到先写的那条（111.1）。两行是合法的——幂等键是 `device_id + message_id`，
**不含 collect_time**，设备重传换了 messageId、或两台设备覆盖同一点都会这样，不是「重复上报」。

**根因**：「取最新一行」的排序在两个查询里**各写了一份**：

| 查询 | 排序 |
|---|---|
| `MeasurementQueryService#latest` | `collect_time DESC, **id DESC**` |
| `ProjectSummaryService#maxDeformation` | `collect_time DESC`（漏了兜底） |

这个次排序键不是新问题——`02-ingest-idempotency.sh` 的 ⑪（`d2288ef` 加的）早就断言过它，
但当时只修了**被报告的那个端点**，兄弟查询漏掉了。

**为什么难复现**：只在 `collect_time` 相同时才分叉，且分不分叉取决于**执行计划**。
compose 的 PG 上不分叉——`EXPLAIN` 显示走 `Index Scan Backward using idx_measurement_point_time`，
同键下反向扫恰好先给到高 id，于是「看着一切正常」。把索引关掉逼成全表扫，立刻分叉：

```
summary 口径 -> 1819 / 111.100000
latest  口径 -> 1820 / 222.200000
```

H2（内存库、小表）本来就是全表扫，所以 `run-all.sh --fresh` 上**一次就红**。
这条值得记：**「在部署形态上没问题」不等于没问题，可能只是那个库的执行计划恰好蒙对了。**

**修法**：把「最新一行」的判据收进 `MeasurementMapper#latestRowOf(pointId, metricCode)`，
两处都调它。只给 summary 补一句 `orderByDesc(id)` 也能修好当下，但那正是这个缺陷的成因——
判据有两份，改一处漏一处。收成一处后，将来第三个「取最新值」的调用者不会再各写一份排序。

**回归断言**：`03-query.sh` 新增 ⑨——同刻两行 111.1 / 222.2，断言 latest 与 summary 取到同一行。
修复前实测红（期望 `222.2`、实得 `111.1`），修复后 **175/175 全绿**（断言数由 173 增至 175）。
收尾时把两个形态都补跑齐了：compose 的 PostgreSQL（`docker compose up` 后直接 `run-all.sh`）**175/175**，
H2 空库 `--fresh` **连跑两次均 175/175**（同时验了套件的可重复性——临时测点每轮新建的约定仍然成立）。
夹具自带 `project→scene→object→point` 四级临时链，是让断言**自足**：该临时项目下只有这一个测点，
最大形变必然等于它，不依赖库里其它测点当时恰好是什么值。

> 写这条断言时我一开始把理由写错了，记下来：我以为「删测点不删 measurement」会污染正式项目。
> 实测**删测点是逻辑删除**（`deleted=1`），全局逻辑删除配置（`application.yml` 的
> `logic-delete-field: deleted`）让它和它的 measurement 行对所有查询不可见——项目 1 账下有
> 30 个软删测点（含带数据的），`maxDeformationMm` 仍是 −1.2224、`pointCount` 仍是 7。
> 所以不会污染，隔离纯粹是为了断言自足。**先按代码/实测核对再写结论，不要按直觉写注释。**

### 59. 浏览器复验（B 的合并进来之后、A 的修复进容器之后）

先把 compose 后端**重建成当前代码**再验，否则验的是旧容器。重建后确认跑的是新代码的办法：
`docker compose cp backend:/app/app.jar` 出来，用 python 的 `zipfile` 读 class 常量池，
看 `MeasurementMapper.class` / `ProjectSummaryService.class` 里有没有 `latestRowOf`。

> 中间踩的坑，今天第二次同类：先在容器里 `unzip` + `strings` 探测，返回「未找到 latestRowOf」，
> 差点写成「容器里是旧代码」。实际是**容器里没有 `unzip`**，报错被 `2>/dev/null` 吞掉、
> 走了 `||` 分支打印了那句吓人的结论。换宿主机 python 解开后，**对照组**（`countByMessageId`、
> `maxDeformation`、`latest`）全部命中，说明探针有效，`latestRowOf` 才确实存在。
> **报错信息必须能看见**，`2>/dev/null` 会把「工具坏了」伪装成「被测对象坏了」。

| 检查 | 结果 |
|---|---|
| 未登录访问 `/home` | ✓ 守卫送到 `/login?redirect=/home` |
| 登录后 | ✓ 回跳 `/home` |
| 总览 | ✓ KPI 全真实（测点 7 / 未解除 0 / 在线设备 0 / 最大形变 −1.2224mm） |
| 测点与曲线 | ✓ canvas 1100×320 |
| 设备状态 / 告警中心 / 管理端 | ✓ 1 / 20 / 1 行真实数据 |
| 跨页口径（§55 的修复在新容器上复验） | ✓ 设备页「在线」0 行 = 总览「在线设备 0」 |
| **B 的 3D 大屏 `/screen`** | ✓ canvas 已建、7 测点带真实值（`P-BP02 −1.22mm` 与总览 −1.2224mm 对得上）；本机无 Cesium token，页面显示「无地形 兜底底图」——**走降级，未白屏** |
| 控制台 / 网络 | ✓ **无控制台报错、无 4xx/5xx** |

三个页面（总览 / 测点 / 大屏）对 `P-BP02` 给出的值是同一个，§58 的口径修复在部署形态上成立。

## 五、第三轮：前端产物进 compose；管理端读写；一个只在新部署形态上现形的 SSE 缺陷

### 60. 前端构建产物进 compose（验收第 8 条闭合）

`frontend/Dockerfile`（`node:22-alpine` 构建 → `nginx:1.27-alpine` 托管）+ `frontend/nginx.conf`
+ `frontend/.dockerignore` 落仓，`docker-compose.yml` 末尾那段注释块换成真服务，`.env.example`
补 `FRONTEND_PORT` / `VITE_CESIUM_ION_TOKEN` / `VITE_TERRAIN_MODE`。三个不补就会踩的点：

| 点 | 不这么做的后果 |
|---|---|
| `try_files $uri $uri/ /index.html` | vue-router 是 **history 模式**，`/screen` `/points` 直接输地址或刷新一律 404 |
| SSE 单独 location（`proxy_buffering off` + `proxy_read_timeout 3600s`） | 前缀匹配里 `/api/v1/stream` 比 `/api/` 长、会赢；用 `/api/` 那条会被缓冲，事件要等缓冲满才出 |
| Cesium token 走 Docker **build arg** | `VITE_*` 是 Vite **构建期**注入的，不是运行期环境变量；改了必须重建镜像 |
| `public/cesium/` 进 `.dockerignore` | 它由 `postinstall` 在容器内生成，被宿主机的旧副本覆盖会版本错配 |

### 61. 管理端从只读改为读写（验收第 7 条缺的另一半）

验收第 7 条的原话是「平台管理端**新建测点/规则** → 业务端无需改代码立即可见」——
上一轮只做到了「业务端立即可见」，管理端还是只读，等于缺一半。

- **表格列由响应数据推导**（各资源字段不同，写死列在后端加字段时就看不见了），
  **表单则由 `TABS[].fields` 声明**。两者刻意分开：表列是「后端有什么」，表单是
  「允许人填什么」，后者必须显式，不然前端会把 `id`/`createdAt` 也提交上去。
- **空串统一转 null**：数字字段在 `el-input` 里清空后是 `''`，直接发出去后端按 `BigDecimal`
  解析会 400；而空串对「可空外键」的语义恰恰是「不设」。一处转换同时解决两个问题。
- 枚举值不是猜的，是从现行库/代码里核出来的（`scene.type`、`object.type`、`point.type`、
  `device.type`、`operator`、`level` 各取实际在用值）。
- **提交前先读权限**：写操作全部走后端 `BaseCrudController` 的 POST/PUT/DELETE，它们都带
  `@PreAuthorize("hasRole('ADMIN')")`。

### 62. 两个真 bug（都在 B 的文件里，按授权范围一并修掉）

| 文件 | 缺陷 | 后果 |
|---|---|---|
| `views/ScreenView.vue` | 「退出大屏」`router.push('/overview')`，而 `/overview` **没有注册路由** | 点下去落进 catch-all 的 `NotFoundView`，一个 404 页 |
| `stores/monitor.js` | `loadSnapshot` 失败时只置 `loading=false`，**不记错误** | 视图里 `!loadedAt → '加载中…'` **永远成立**，请求失败被显示成「还在加载」——一个不会自己结束的假象 |

第二个尤其值得记：**「加载中」和「加载失败」在界面上长得一样**，而前者会自己结束、后者不会。
修法是失败的把原因记下来，视图优先显示它。

### 63. nginx 反代下 SSE 连接泄漏，导致整页刷新 6 次后界面僵死（本轮最难的一个）

**现象**：`verify-compose` 跑到管理端新建那一步，下拉**打开了但是空的**，`/health` 往返 **44807ms**。

**排查过程**（每一步都先把「是不是测量工具坏了」排掉）：

1. 先怀疑测试助手点不开 Element Plus 的下拉 → 但诊断显示下拉**确实打开了、高度 40px、0 个选项**，
   说明是数据源空，不是点不开。**先纠正了自己的错误猜想。**
2. 追网络：请求**发出去了、4.5 秒没回来**，而且整页所有请求都排队。
3. 采样连接数：**恰好 6 条被钉死再不释放**——Chrome 单源并发上限正是 6。
4. 读后端日志：**`/health` 在后端只花了 1ms**，浏览器却量到 44807ms。→ **后端是快的，卡在浏览器那侧。**
5. 对照实验（同一次数、同样的路径，只改一个变量）：

   | 变量 | 结果 |
   |---|---|
   | 路径换成不走 `AppLayout` 的 `/nope`（**无 SSE**） | 8 次导航全 3–6ms，不卡 |
   | 拦掉 `/api/v1/stream`，其余导航不变 | 9 次导航全 6–8ms，不卡 |
   | 不拦 | **第 6 次冻 47 秒** |

6. 分辨 nginx 还是后端：同样「连 2 秒就杀」，
   **直连 8080 → 后端立刻回收**（在线 6 重置回 2、3、4）；
   **过 nginx → 一条都不回收**（在线 1→6 只增不减，等 35 秒也不动）。

**根因**：`AppLayout` 的全局 SSE 只在 `onBeforeUnmount` 里关，而**整页卸载不会触发它**——
文档是被丢弃的，Vue 没有机会做清理。此时浏览器发的 FIN 仍要等对端收尾，可 nginx 的**非缓冲**
反代只能靠「向客户端写失败」察觉，而**第一次写进半关闭的 socket 会成功**，于是要等第二次心跳
（后端 `HEARTBEAT_MS` 30s × 2）才收口——正好对上 45–52 秒。这段时间那条半关闭的 socket 占着
浏览器「单源 6 连接」的名额，**连刷 6 次之后所有请求全部排队，界面看起来就是死了几十秒**。

**修法**（`AppLayout.vue`）：`pagehide` 里显式 `close()`——浏览器当场回收名额，不必等对端；
并补 `pageshow(persisted)` 重连——否则从 bfcache 后退回来 SSE 会**静默失效**，页面看起来一切正常、
只是数据永远停在那一刻。修后同一实验：9 次导航全 3–8ms。

**一条方法论**：这次的失败信号是「下拉是空的」，而真因在三层之外（浏览器的连接池）。
**「看起来像 UI 的问题」先别改 UI**——把「谁慢」量化出来（这里是浏览器 44807ms vs 后端 1ms），
范围会自己收窄。

### 64. compose 形态端到端复验（全绿）

| 项 | 结果 |
|---|---|
| 未登录访问 `/home` | ✓ 被守卫拦下并回跳 `/login?redirect=/home` |
| 登录后 | ✓ 回到被拦下的 `/home`（不是无脑进首页） |
| 五个工作台页 | ✓ 表格 5 / 7 / 1 / 20 / 1 行，测点页 canvas 1100×320，均非空白 |
| 跨页口径 | ✓ 设备页「在线」0 行 == 总览「在线设备 0」 |
| **管理端新建 → 接口核对 → 删除** | ✓ 行数 7→8、`/api/v1/points` 查得到、删除后消失 |
| B 的 3D 大屏 `/screen` | ✓ canvas 已建、7 测点带真实值、走「无地形 兜底底图」降级未白屏 |
| 「退出大屏」 | ✓ 落在 `/home`（§62 的修复生效，不再是 404） |
| 控制台 / 网络 | ✓ **无控制台报错、无 4xx/5xx** |

## 五之二、仍未做

- **影像挂点 `/media`（阶段 5）**。
- **SSE 的页面级消费**（B 的 3b）：当前只有顶栏「实时已连接」标签在用连接，没有页面订阅事件。
  归 B，理由见 §57。
- **后端 `SseBroadcaster` 的僵尸订阅回收偏慢**：它靠「心跳发送失败」摘除，而过 nginx 时
  写失败要等第二次心跳（见 §63），所以断开后在线数最多滞后约 60 秒。`MAX_CLIENTS=100`
  有足够余量，正常使用攒不满，暂不动；若要收紧，把 `HEARTBEAT_MS` 从 30s 调小即可。
- ~~前端产物尚未进 compose~~ —— 已完成（§60）。
- ~~管理端写操作（当前只读）~~ —— 已完成（§61）。
- ~~3D 大屏 `/screen`（阶段 3）~~ —— 已由 B 并入（§57）。
  底图/地形可达性已于 09-11 实测（Cesium ion、Mapzen 地形、ArcGIS/Carto 底图可达；
  **OSM 瓦片与天地图不可达**，选底图时避开）。
